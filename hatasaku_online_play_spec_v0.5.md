# 畑作 オンライン対戦仕様書 v0.5

作成日：2026-09-24  
状態：Phase 1 実装メモ付き。v0.4 に退席・観戦・24時間GC・ロビー設定の再描画修正を追記。  
対象：インターネット上で知人どうしがブラウザから同じ卓を遊ぶための **ホスト層**  
前版：`hatasaku_online_play_spec_v0.4.md`

ゲームのルールは `hatasaku_rules_*.md` を正とする。  
ローカル／LAN のエンジン・UI の挙動は `hatasaku_software_spec_*.md` を正とする。  
本仕様はそれらを置き換えない。公開用の「卓・接続・身元」だけを定める。

---

## 1. ゴール

### 大きなゴール（将来）

iPhone / Android のアプリとしても遊べるようにする。本仕様のホスト層（卓・席・再接続）はその土台とする。

### 小さなゴール（本版の到達点）

1. 協力メンバーに URL（または短いコード）を伝えて、Web でオンライン対戦を試せる。
2. 無料のホスティング（長時間プロセス向け）上で動かす。課金・課金機能は不要。
3. パスワード付きの会員登録はしない。端末に保存するトークンで席を識別する。
4. ブラウザを閉じたり他アプリに切り替えても、同じ席に戻って試合を続けられる。
5. スタート前のロビーで、リーダーがモードや人数などを変えられる。集まらなかった席は CPU になる。
6. 人間が2人以上いる卓では、制限時間内に動かないと CPU がその席の手を代行する。

### 非ゴール（本版）

- 不特定多数向けのマッチング、公開ロビー、フレンド検索
- アカウント（メール／OAuth）、プロフィール、ランキングの永続
- 課金、広告、ストア提出
- 観戦専用席、リプレイ共有
- サーバ再起動後も卓を必ず復元する保証（後述。本版はメモリ上で足りる）

---

## 2. 用語

| 用語 | 意味 |
|---|---|
| 卓（テーブル／インスタンス） | 1マッチ分のゲーム状態。エンジンの `Table` に相当する単位 |
| ロビー | マッチ開始前の待機状態。設定変更と参加者待ちができる |
| 入場窓 | 卓作成から一定時間。この間だけ新規参加（席の確保）ができる |
| 参加コード | プレイヤー間で共有する短い識別子（または同等の URL クエリ） |
| 端末トークン | ブラウザ（または後のアプリ）に保存する不透明な識別子。ログインではない |
| 席トークン | ある卓の特定の席に紐づく再開用の秘密。端末に保存する |
| 設定人数 | ロビーで決める総プレイヤー数（人間＋CPU）。ルールどおり 3〜5 |
| ロビーリーダー | ロビー中の設定変更・開始の権限を持つ参加者。参加順で決まる（§5.2） |
| 手番制限時間 | 人間の手番（または確認待ち）に対する制限。切れると CPU が代行する（§5.5） |

---

## 3. フェーズ

| フェーズ | 内容 | 備考 |
|---|---|---|
| Phase 1 | 本仕様どおりの Web 公開（友人試験） | 今の到達点 |
| Phase 2 | PWA 化や通知など、モバイルブラウザの体験改善 | 任意 |
| Phase 3 | ストアアプリ（WebView / Capacitor 等） | 大きなゴール。ホスト API は可能な限り流用 |

仕様の変更はフェーズをまたいでよい。変更したら版を上げ、差分を変更履歴に書く。

---

## 4. ユーザフロー（Phase 1）

1. 誰かが「卓を作る」。初期の設定人数・モード・シーズン数などは仮でよい（ロビーで変えられる）。
2. サーバが卓（ロビー）を作り、**参加コード**と参加用 URL を返す。作成者は最初の参加者になり、**ロビーリーダー**になる。
3. 作成から **10分間**が入場窓。このあいだ、設定人数までの空きがあれば他の端末がコード／URL で参加できる。
4. ロビー中、**ロビーリーダーだけ**がモード・設定人数・シーズン数などを変更できる。変更は参加者全員にすぐ見える。
5. リーダーが「開始」する（または同等の操作）。この時点で席にいる人間はそのまま人間席、**まだ埋まっていない枠は CPU** になる。  
   例：設定人数 5、人間が 2 人だけ → 人間 2 + CPU 3。
6. 開始後は設定を変えられない。入場窓が終わっていれば新規参加も不可。すでに席を持つ人の再接続は試合終了まで可能。
7. 入場窓だけ終了してまだ開始前なら、既存参加者の再接続とリーダー操作は続けられるが、新規参加は不可（§5.3）。
8. 試合は入場窓とは独立に続くが、**開始から24時間**でサーバが卓を破棄してよい（§5.7）。
9. 切断しても席は保持し、同じ席トークン／端末で再開できる（試合中。ロビー中の扱いは §5.2・§5.4）。
10. 試合中に「やめる」でホームに戻れる。他の人間が残っていれば離席扱い（CPU代行）。最後の一人なら一時停止。コードで復帰または観戦が可能（§5.4）。
11. マッチ終了後、卓は破棄してよい（または短時間で消してよい）。

---

## 5. 要件

優先度：**MUST**＝Phase 1 必須、**SHOULD**＝できれば、**MAY**＝後回し可。

### 5.1 身元（アカウントの代わり）

| ID | 優先 | 要件 |
|---|---|---|
| A1 | MUST | ログイン・パスワード・メール登録は求めない |
| A2 | MUST | 初回アクセスで端末トークンを発行し、ブラウザに保存する（localStorage 等） |
| A3 | MUST | 表示名は端末ごとに覚え、毎回の入力を強制しない。初回または変更時だけ入力してよい |
| A4 | SHOULD | 表示名の重複は卓内で区別できればよい（「あなた*」等の既存 UI を流用可） |
| A5 | MAY | 後のフェーズで OAuth 等に載せ替えてもよい。そのとき端末トークンからの移行を定める |

### 5.2 ロビーとリーダー

| ID | 優先 | 要件 |
|---|---|---|
| L1 | MUST | マッチ開始前はロビー状態とし、リーダーが設定を変更できる |
| L2 | MUST | 変更できる項目は少なくとも **モード（基本／上級）・設定人数（3〜5）・シーズン数**。CPU 数は開始時に自動決定する（設定人数 − 人間数） |
| L3 | MUST | ロビーリーダーは、**いま席を確保している参加者のうち、参加が最も早い人** |
| L4 | MUST | リーダーがロビーから抜けた（席を放棄した）とき、残っている参加者のうち次に早い人がリーダーになる。一時切断の猶予中はリーダーをすぐ移さなくてよい（§5.4 の猶予と整合） |
| L5 | MUST | リーダー以外は設定を変更できない。開始操作もリーダーだけ |
| L6 | MUST | 開始時点の人間数が 1 以上なら開始してよい。設定人数に満たない分は CPU |
| L7 | MUST | 開始時点の人間数が設定人数を超えない。設定人数を人間数未満に下げる操作は、余剰の人間がいるあいだは拒否する（または先に退出が必要） |
| L8 | MUST | 開始後は設定変更不可 |
| L9 | SHOULD | 全員に、現リーダー・設定・参加者一覧（人間／空き→開始後CPU）が見える |
| L10 | SHOULD | リーダーはロビー中に卓をキャンセル（破棄）できる |
| L11 | MAY | リーダーの明示的な委譲 |

### 5.3 卓の作成と入場

| ID | 優先 | 要件 |
|---|---|---|
| T1 | MUST | 任意の協力メンバーが卓を作成できる（事前の「運営アカウント」不要） |
| T2 | MUST | 卓作成時に参加コード（推測困難な短い文字列）または同等の参加 URL を発行する |
| T3 | MUST | 入場窓は作成から **10分**。経過後は新規の席確保を拒否する |
| T4 | MUST | 入場窓終了後も、すでに席を持つ端末の再接続は、ロビー中および試合終了まで許可する |
| T5 | MUST | 試合そのものに入場窓由来の時間制限はかけない（無制限） |
| T6 | MUST | 設定人数は 3〜5（ルールのプレイ人数）。人間＋CPU の合計 |
| T7 | SHOULD | 入場窓中は残り時間と、人間の空き（設定人数 − 現在の人間数）が分かる |
| T8 | MAY | 入場窓終了で自動開始するかは後で決めてよい。本版の既定は **リーダーが開始するまで待ってよい**（窓終了後もロビー継続、新規参加のみ不可） |

### 5.4 接続と再接続

| ID | 優先 | 要件 |
|---|---|---|
| C1 | MUST | 席の所有は WebSocket 接続そのものに結びつけない。席トークン（または同等）で識別する |
| C2 | MUST | 試合中は、切断しても席は解放しない（猶予中は「一時切断」） |
| C3 | MUST | 同じ席トークンで再接続すると、同じ席・同じ盤面視点に戻る |
| C4 | MUST | 他端末／他人が同じ席に割り込めない（トークンがないと不可） |
| C5 | SHOULD | 試合中の切断では、手番制限時間（§5.5）が切れるまでは勝手に席を放棄しない。制限が切れたあとの代行は §5.5 に従う |
| C6 | SHOULD | 再接続後、クライアントは最新の公開ビューを受け取り、操作を再開できる |
| C7 | SHOULD | ロビー中の一時切断も再接続で同じ席に戻れる。猶予を超えて席放棄したときは枠が空き、リーダー継承の対象になる |
| C8 | MUST | 試合中に「やめる」ができる。**全プレイヤー**（観戦を除く）の画面に出せる。ホーム（卓作成／参加）に戻る |
| C9 | MUST | 「やめる」後も席は保持。参加コードで同じ端末が戻ると席に復帰する。自分の手番の途中で抜けた場合、その手番は CPU 代行を見るだけ（次の自分の手番から操作可） |
| C10 | MUST | 人間が複数いる卓で、最後の一人以外が「やめる」したときは席を保持し、**制限時間を待たず** CPU が代行する（接続断は §C5 どおり制限時間待ち） |
| C11 | MUST | 最後の人間が抜けたときはマッチを一時停止し、手番タイマーと CPU 進行を止める。誰かが復帰したら再開 |
| C12 | MUST | 開始済みの卓コードに、席を持たない端末が入ると **観戦**（操作不可・盤面は見える） |
| C13 | MAY | 長時間切断時のフォールバック（席の永続 CPU 化等）は後の版でよい |

### 5.5 手番制限時間と CPU 代行

人間が **2人以上**いるマッチにだけ適用する。人間1人＋CPU の卓では、本節の制限はかけない（一人でゆっくり触ってよい）。

| ID | 優先 | 要件 |
|---|---|---|
| P1 | MUST | マッチ開始時点（および以降）の人間席が2以上のとき、各人間の **手番**に制限時間を設ける |
| P2 | MUST | 制限時間内に植える／パス／呪いなど、その席の合法な進行操作がなければ、サーバが **現行と同じ CPU 戦略**でその席の手を1回代行する |
| P3 | MUST | 代行しても席の種類は人間のまま（席トークンで戻れる）。永続的に CPU 席へは変えない |
| P4 | MUST | 代行後は次の手番・次の待ちに進み、必要ならまた制限時間を振り直す |
| P5 | SHOULD | 収穫結果の［次へ］など、卓が人間の確認を待っている状態にも同様の制限をかけ、切れたらサーバが進める（人間が複数いるときだけ） |
| P6 | SHOULD | 残り時間が全員（または当人）に見える |
| P7 | MAY | 制限時間の秒数はロビーでリーダーが選べる。選ばれなければサーバ既定値 |

### 5.6 セキュリティと公正

| ID | 優先 | 要件 |
|---|---|---|
| S1 | MUST | クライアントは自分の席の合法手だけ送れる。サーバが検証する（現行エンジンと同じ） |
| S2 | MUST | 参加コードを知らない第三者が卓一覧から勝手に入れない（公開ロビーなし） |
| S3 | MUST | 設定変更・開始はサーバ側でリーダーかどうか検証する |
| S4 | SHOULD | 席トークンは URL に長く載せない（保存はストレージ、通信はヘッダ／初回のみ） |
| S5 | MAY | レート制限・悪用対策は友人試験の規模では最小でよい |

### 5.7 運用・ホスト環境

| ID | 優先 | 要件 |
|---|---|---|
| H1 | MUST | 長時間の双方向通信（WebSocket 等）が使えるホストを選ぶ。静的ホスト単体だけでは足りない |
| H2 | MUST | 同時に複数卓を持てる（少なくとも友人試験で数卓） |
| H3 | SHOULD | 無料枠のスリープや同時接続上限を README か本仕様の付記に書いておく |
| H4 | SHOULD | Phase 1 では卓状態はメモリ上でよい。プロセス再起動で進行中卓が消えてよい旨を参加者に伝える |
| H5 | MUST | 開始から **24時間**経過した進行中卓は GC で破棄する |
| H6 | MAY | 永続化（再起動耐性）は Phase 2 以降 |

### 5.8 既存実装との境界

| ID | 優先 | 要件 |
|---|---|---|
| B1 | MUST | ゲームルール・収入・合法手はエンジンを変えず流用する |
| B2 | MUST | ローカル `serve`（LAN・1プロセス1卓）は残してよい。オンラインは別エントリでも同じエンジンでもよい |
| B3 | SHOULD | UI は現行グラフィカル UI をベースにし、オンライン用のロビー／設定／再接続表示だけ足す |
| B4 | MUST | 開始時に「人間席＋不足分 CPU」でエンジンの `Table`／`createGame` を組む（いまの CLI の humans/cpus 固定とは別の組み方） |
| B5 | MUST | 手番代行はホスト層が CPU 選択 API を呼ぶ。ルールエンジンの合法手の外では動かない |

---

## 6. 未決・後で変えてよいこと

本版では決めない／仮置きでよい項目。実装や試験のあと改訂する。

- 入場窓の長さ（いまは 10 分）
- 入場窓終了時に自動開始するか（既定はしない）
- 手番制限時間の既定秒数（および［次へ］用が別か）→ 実装既定 **60 秒**、ロビーで変更可
- ロビー／試合中の切断猶予の秒数（手番制限との前後関係の細かい調整）→ ロビー切断猶予 **60 秒**
- 表示名の文字種・長さ → 実装は最大 16 文字
- 参加コードの形式（桁数、記号）→ 実装は 6 文字（紛らわしい字を除く英数）
- リーダーのキック権限の有無
- 無料ホストの具体サービス選定（Render / Fly / Railway 等）→ Phase 1 試験は **Mac + ngrok** を推奨
- ネイティブ化の方式（PWA / Capacitor / 別クライアント）

---

## 7. 受け入れの目安（Phase 1）

1. A が卓を作り、B にコードを送る。10 分以内に B が参加し、同じマッチを遊べる。
2. ロビーでリーダー（先に入った A）が設定人数を 5・基本モードに変え、B にもそれが見える。A が開始すると、人間が A・B の 2 人なら CPU が 3 人入る。
3. A がロビーから抜け、B だけ残ると B がリーダーになり設定を変えられる。
4. 入場から 10 分超えたあと、C は新規参加できない。A・B は（開始後なら）試合を続けられる。
5. 人間2人の卓で、A の手番に制限時間内に操作しないと、CPU が A の席で1手代行し、試合が進む。A はあとから戻っても人間席のまま。
6. 人間1人＋CPU の卓では、手番制限がかからず、切れない。
7. プレイ中に B がタブを閉じ、別タブで開き直すと、同じ席に戻り操作できる。
8. 表示名を一度入れた端末では、次回以降その入力を繰り返さなくてよい。
9. パスワードやメール登録なしで上記が完了する。

---

## 8. 実装メモ（Phase 1）

### 8.1 エントリとファイル

| 役割 | 場所 |
|---|---|
| オンラインサーバ | `npm run serve:online` → `src/cli/serve-online.ts` |
| 卓／ロビー | `src/session/room.ts`（`Room` / `RoomHub`） |
| LAN 単卓（従来） | `npm run serve` → `src/cli/serve.ts`（オンラインホストでは `/lan`） |
| オンライン UI | `src/web/online.html` + `online.js`（盤面は `app.js` を再利用） |
| 端末・席の保存 | `localStorage`：`hatasaku-device-id` / `hatasaku-display-name` / `hatasaku-online-seat` |

### 8.2 WebSocket メッセージ（概要）

クライアント → サーバ：`hello`（再接続時は `code` + `seatToken`）、`create`、`join`（開始後は復帰または観戦）、`lobby-settings`、`start`、`cancel`、`leave`、`quit`、`action`、`next`  
サーバ → クライアント：`welcome`、`hello-ok`（`reason`: `quit` / `room-expired` 可）、`created` / `joined` / `rejoined` / `observing`、`lobby`、`view`（`role`: `player`|`observer`、`paused`、`turnDeadline`）、`error`

席の所有は WS 接続ではなく `deviceId` + `seatToken`。切断・「やめる」しても試合中の席は解放しない。観戦は `observers`。  
ロビー設定の編集中は、入場カウントの1秒更新でフォームを丸ごと描き直さない（下書き保持）。

### 8.3 手番制限

- 人間席が **2 以上**かつマッチが一時停止中でないときだけアームする（`Room.turnTimerApplies`）。
- 切れたらホストが現行 CPU 戦略（`irr`）で1手代行、または hold 中なら未 ACK をまとめて進める。
- 席 `kind` は human のまま。接続断席はタイマー待ち。明示の「やめる」（`forceProxy`）は制限時間を待たず即時代行。
- シーズン末 recap（`season` / `mix` / `trail` / `honor`）では手番タイマーを振らず、クライアントも制限表示の再描画をしない。
- 人間が0人になったら `paused`。タイマーと CPU 進行を止める。

### 8.4 試験用ホスト（Mac + ngrok）

友人試験の最短経路：

1. 常時起動できる Mac（または同等）で `npm run serve:online -- --port=8080`
2. 別ターミナルで `ngrok http 8080`
3. ngrok の `https://…` を共有。参加者はブラウザで開き、コードまたは `?code=` で参加
4. 同じプロセス上で複数卓可。**再起動で卓は消える**（H4）。開始から24時間で GC（H5）

向かない例：

- Vercel 等の短命サーバレス単体（長時間 WS に不向き）
- ConoHa Wing の PHP 共用（Node + 常駐 WS を想定しない）

後で Render / Fly / Railway 等の常駐 Node に載せ替えてよい。プロトコルは本節のまま流用する。

### 8.5 既定値（実装）

| 項目 | 値 |
|---|---|
| 入場窓 | 10 分 |
| ロビー切断猶予 | 60 秒 |
| 手番制限の既定 | 60 秒（ロビーで変更可、下限 10 秒） |
| マッチ寿命（GC） | 開始から 24 時間 |
| CPU ステップ（演出） | 200 ms（LAN と同じ） |
| 参加コード | 6 文字 |

---

## 9. 変更履歴

- 2026-09-24：v0.5。やめる／復帰／観戦、最終人間で一時停止、24時間GC、ロビー設定下書き保持。やめるは全プレイヤー・即時CPU代行。シーズン末recapの連続再描画を抑止。
- 2026-09-23：v0.4。Phase 1 実装メモ（ファイル配置、WS 概要、Mac+ngrok、既定値）。要件本文は v0.3 継承。
- 2026-09-23：v0.3。人間2人以上のとき手番制限時間。切れたら CPU が代行（席は人間のまま）。人間1人卓は制限なし。
- 2026-09-23：v0.2。ロビー中の設定変更、ロビーリーダー（参加順・退出で継承）、開始時に不足席を CPU で埋める。
- 2026-09-23：v0.1。友人向け Web 公開の要件草案。端末トークン、入場 10 分・試合無制限、再接続、既存 rules／software_spec との境界。
