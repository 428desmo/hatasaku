# 畑作 Web IA v0.2（スマホ主・カード物体）

対象ルール：`hatasaku_rules_v0.6.md`  
v0.1からの差分：向き、カード物体。露出の三層（L0/L1/L2）と収穫マトリクスは維持。  
例示は **F1**（基本4人 R5、自分=P3、8G、手番）。

参照スケッチ（ユーザー添付）は「これが正解」ではなく、**カードっぽさ**のイメージ。取るものと取らないものを§1.1に書く。

---

## 1. 向き：横がいい気はするが、スマホ横は高さがない

添付スケッチは横長に見えるが、中身は **プレイヤー4段＋場札** で高さも要る。タブレット／PCの卓ビューに近い。

スマホを横にすると幅は約844、**高さは約390**。4人×5農地のカードテーブルは載らない。

| 向き | 向いていること | 向かないこと |
|---|---|---|
| **縦 390×844** | 片手。帯を積める。マトリクスの行が読める | カードが横に並ぶとチップ化しやすい（v0.1の失敗） |
| **横 844×390** | カードゲームの3帯（イベント／盤／手札）が自然。場札が「手札」になる | 高さ不足。全員の農地カードは置けない |
| **タブ横（後）** | 添付に近い卓 | 今の主対象（スマホ）ではない |

**この版の結論（仮）**

- 手番の主レイアウトは **横の3帯** にする。カードゲームらしさはこっちが出る。
- 縦は同じ物体を積み直した **代替**。持ち替え用。情報階層は変えない。
- 添付のように「全員の農地カードを常時並べる」は、スマホではどちら向きでもやらない。走査はマトリクスのまま。

理由：横にしただけでN（収穫中の枚数）は数えやすくならない。添付は各カードに `11G(4G)` を書いて収入は読めるが、「トマトは今何枚収か」は依然として全カード走査。そこがRULE §7の中心課題なので、幅が増えてもマトリクスは残す。

---

## 1.1 添付から取る／取らない

**取る**

- 物体はカード。帯やチップでイベントを「表さない」
- 画面は3帯：イベント列（上）／盤（中）／手札＋パス（下）
- 空スロットは破線のカード形（カードが無い、が見える）
- 場札は手札として下に置く

**取らない**

- 全員の農地を常時フルスペックで並べる
- L0で W/H/CD のピップを全農地に出す（A2寄り。植え替えのL1で出す）
- イベントを横長の短いチップにする（添付のEvents帯はまだチップ。縦長のカード面にする）

---

## 2. カード物体（美麗さは後。形だけ）

ワイヤーでもカードだと分かる約束。

1. **縦長**（だいたい 3:4）。横長の枠はカードではない。
2. **面**：上にゾーン／名前、中央に大きい数値、下に補足。
3. **束**：山札は2枚ずらして重ねる。1枚の枠だけならカードの山ではない。
4. **空**：破線の同じ大きさ。スロット（受け皿）でありカードではない。
5. **裏**：山だけ。斜線。中身は出さない。

### イベントカード（作物イベント）

L0で4枚を **カード面** として並べる。有効（継続・今）は実線・大きい数値。予告（次・次々）は小さく、面は出すが「まだ効かない」。

```
┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐  ┌─┐
│継続 │  │ 今  │  │ 次  │  │次々 │  │／│ ← 山（裏の束）
│ネギ │  │トマト│  │ラディ│  │ジャガ│  └─┘
│ −2  │  │ ＋2 │  │ −4  │  │ ＋4 │
└─────┘  └─────┘  └─────┘  └─────┘
  有効              予告
```

L1でコンベア全体：捨て（表の束）・継続・今・次・次々・山（裏の束）。除外6は出さない。

### 場札（作物カード）

手札。L0は名前・栽培コスト・待W収H・今の1農地収入。L1で1枚だけ起こす（最低保証・C・タイプ・補正）。行き先はスロット側。

### 農地

カードが入っているスロット。L0の自分：頭字＋状態略号。空は破線。未取得は「次 1G」。相手はマトリクス経由のL1だけ、同じカード形で5枚。

---

## 3. 画面の骨格

### 横（主・スマホ 844×390）

高さ390ではイベント／盤／手札を**縦に3段**すると必ず潰れる（v0.2初稿で場札がマトリクスに重なった）。幅を使う。

```
┌ S1 R5 手番  P3* 8G                         手番|記録 ┐
│ イベントカード ×4 と山の束 │ 収穫マトリクス（盤）     │
│ 場札カード（手札）   自分の農地カード        [パス]   │
└─────────────────────────────────────────────────────┘
```

上段は左がイベント、右が盤。下段が手札。全員の農地は置かない。

### 縦（代替）

同じ物体を上から積む。イベントはチップに戻さず、縦長カードを4枚横に並べる（390幅に載る。v0.1が2×2にしたのは横長枠だったため）。

タブは2つ（手番|記録）。L0/L1/L2は v0.1 のまま。

---

## 4. 略号・露出・場札の開き方

v0.1 §3–6 を継承。変更点だけ：

- イベントのL0はカード面。チップ禁止。
- 自分スロットのL0はカード形（空は破線）。箱ではない。
- マトリクスはカードではない（走査表）。盤。

---

## 5. 画面一覧

| ID | 画面 | 目的 |
|---|---|---|
| W1 | 手番・既定 | 3帯＋カード物体がL0に載るか |
| W2 | 場札を起こす | 1枚だけ面を増やす。マトリクスは残す |
| W3 | スロット | 空カードを選んで植える |
| W4 | 相手の農地 | カード5枚を必要な人だけ |
| W5 | イベント全景 | カードのコンベア＋山の束＋捨て |
| W6 | 待ち | 手札の操作を消す |
| W7 | 収入 | カードではなく結果表（ホールド） |
| W8 | 記録 | L2 |

ワイヤー：`wireframes/mobile/index.html`（縦／横切り替え）

---

## 6. まだ決めていないこと

- 横をロックするか、端末の回転に追従するか（プレイ中の持ち替え）
- 5人でマトリクス列が増えたとき、横でも文字が持つか
- 収入画面（W7）までカード面で出すか。今は表のまま（判断ではなく結果だから）
