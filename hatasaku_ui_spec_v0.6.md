# 畑作ボードゲーム v0.6 — UI/UX設計仕様書（情報アーキテクチャ＋物理UI）

対象ルール：畑作ボードゲーム ルール仕様書 v0.6（ゲーム挙動の唯一の正）
用途：この文書を Cursor に渡し、Figma 上に初期プロトタイプを構築する。
方針：ビジュアルスタイル（画風）は決めない。**情報 → 意思決定 → 状態 → 空間 → コンポーネント → Figma** の順で設計する。

---

## 0. 読み方（タグ凡例・ID体系）

各記述は、次のタグで分類する。タグのない文は、説明文（分析・前提の整理）である。

| タグ | 意味 |
|---|---|
| **RULE** | ルール仕様書v0.6で決まっている事項。UI側で変更しない |
| **REQUIREMENT** | ルールから導かれる、UI上の必須条件 |
| **RECOMMENDATION** | 今回の分析から推奨するUI（UIのみ。ルールは変えない） |
| **ALTERNATIVE** | 有力な別案（UIのみ） |
| **ASSUMPTION** | 設計を進めるための暫定仮定 |
| **OPEN QUESTION** | 人間の設計者が決める未確定事項 |
| **GAME RULE CHANGE CANDIDATE** | ルール変更を伴う案（今回は実施しない。§25にまとめる） |

ID体系：推奨は `REC-xx`（§19に登録簿）、未確定事項は `OQ-xx`（§23）、人間の判断事項は `D-xx`（§24）、ルール変更候補は `GRC-xx`（§25）。

**用語**

- **農地スロット**：プレイヤーボード上の農地の置き場（最大5）。1スロットに作物カードが1枚置かれる。
- **待／収／休**：白＝待機、緑＝収穫、赤＝連作クールダウンのトークン状態を指す略号（UI上のラベル）。
- **空き**：手番の開始時に、白も緑もない農地（RULE §6.3）。
- **進行係**：ラウンドの共通処理（イベント更新、収穫の集計など）を担当する人（**OQ-03**。ルールにない役割）。
- **左／右**：特に断りがなければ、テーブル上の時間軸の方向。**過去が左、未来が右**（§16）。

---

# 1. Executive Summary

## 1.1 この設計の中心的な結論

1. **このゲームの中心的なUX課題は「全テーブルの収穫中スキャン」です。**
   **REQUIREMENT**：収入は `max(最低保証, 基本収入 − 同種の他の収穫中農地数 ＋ イベント補正)` で決まり、「他の農地」には全プレイヤーの農地と自分の別農地が含まれる（RULE §7）。プレイヤーは毎ラウンド「作物Xが全体で何枚収穫中か」を把握する必要がある。カード単体の美しさより、これを優先する。
2. **農地のトークンは、1つの農地に同時に1色しか存在しません。** RULE §6.2 から、白→緑→赤は必ず順番に置き換わり、共存しない。したがって農地の状態は「待機（白あり）／収穫（緑あり）／空き・休耕中（トークンは赤のみ）／空き・自由（トークンなし）」の4状態に一意に決まる。**トークンが唯一の真実**であり、物理的に余計なフラグを足さない（**REC-03**）。
3. **状態は色ではなく「位置（レーン）」で表す。** 農地ボード上に3本の縦レーン（待｜収｜休）を設け、収穫は常に**中央レーン**に置く（**REC-01**）。中央は180°回転しても中央のままなので、向かい側のプレイヤーからも同じ位置で「収穫中か」を読める。形状（○⬡◇）・色・アイコン・穴の形状ロックで4重に符号化する（**REC-02**）。
4. **イベントは「1枚ずつ左へ進むコンベア」として表現します。** 山札→次の次→次→今→継続中→捨て札、と全カードが毎ラウンド1つ左へ進む。有効なイベント（継続中＋今の最大2枚）と予告（次・次の次）を、ブラケットで明確に分ける（**REC-08、REC-09**）。
5. **他プレイヤーの農地は、文字ではなく輪郭で識別します。** 作物名の文字は向かい側から読めない。作物のシルエット（10種で輪郭が異なるグリフ）＋頭字＋タイプ帯（機能色）で識別し、四辺に冗長配置する（**REC-12**）。
6. **共通エリアに「作物パネル」を4枚置きます。** 今シーズンの4作物それぞれに、静的数値、収入はしご、収穫集計、イベント補正を集約する。Case C・Dの視線移動を大きく減らす（**REC-06**）。
7. **時間は1つの共通文法で表します。** 1セル＝1ラウンド。待機＝中抜き、収穫＝塗り、休耕＝斜線。作物カードの時間軸、ラウンド表示、イベントレールが同じ文法を共有する（**REC-13**）。
8. **公開情報の「所持コイン」は、手元の積み上げだけでは不十分です。** 手番順（コイン昇順）の判定に使うため、ラウンド末に総額を公開更新する台帳を推奨する（**REC-15、REC-16**）。

## 1.2 ルールから導かれる定量的な事実（設計判断の根拠）

| 事実 | 値 | 根拠 |
|---|---|---|
| 1回の植え付けサイクルでのトークンの出し入れ回数 | 4〜22回（平均14.4回） | 作物ごとに 2×(待＋収＋休)。RULE §12の数値から算出（§8.2） |
| 1プレイヤー・1シーズンあたりのトークン移動数（**推定**） | 基本で約84回、上級で約190回 | 平均14.4回 × 行動数/人（基本5.8、上級13.2。RULE §14.1）。作物の組み合わせで変動 |
| 農地スロット全体（5人）の最大数 | 25 | RULE §5 |
| 有効なイベントの最大数 | 2 | RULE §4 |
| イベント列の最大枚数 | 3 | RULE §9.1 |
| 上級での終了時所持コイン（平均） | 約180〜210 | RULE §2 |

## 1.3 主要な人間の判断事項（詳細は§24）

- トークン方式：形状ロック付きトークン（推奨）か、進行マーカー方式（ALTERNATIVE）か（**D-01**）。
- 共通「作物パネル」に、収穫集計のペグを付けるか（**D-02**）。
- 所持コインの公開方法（**D-03**）。
- デジタル版の自動計算・合法手表示を、既定でどこまで出すか（**D-09**）。デジタルの補助は、計算力の差（RULE §14.7）に影響しうる。

## 1.4 この設計が変更していないもの

ゲームの結果に影響するルールは、変更していない。UI上扱いにくいルールは、`OPEN QUESTION` または `GAME RULE CHANGE CANDIDATE` として指摘するにとどめる（§23、§25）。

---

# 2. Design Goals and Constraints

## 2.1 最重要制約：ゲームルールを変更しない

**REQUIREMENT**：次の事項は変更しない。作物の全パラメータ、農地取得コスト、待機・収穫・連作CD、基本収入、最低保証、イベント効果と有効期間、ラウンド数（基本10・上級18）、シーズン数、手番順、初期コイン、場札枚数、山札・捨て札の規定、公開／非公開情報、合法手、勝利条件。

本文書内の数値（作物の値など）は、RULE §12から転記したもので、UIのために変更していない。

## 2.2 設計目標（優先順）

| 優先 | 目標 | 説明 |
|---|---|---|
| G1 | 収穫中スキャン性 | 全テーブルで、作物Xの収穫中の農地が素早く数えられる |
| G2 | 状態の明確さ | 待機／収穫／空き・休耕中／空き・自由／未取得を混同しない。占有と連作制限を別軸で見せる |
| G3 | 経済の比較容易性 | 栽培コスト・農地取得コスト・所持コインを同じ視野で比較できる |
| G4 | 時間関係の明確さ | 待機・収穫・CD・残りラウンド・イベントの時間軸を、1つの文法で理解できる |
| G5 | 卓越しの可読性 | 他プレイヤーの農地・カードを、文字を読まずに識別できる |
| G6 | 操作量の抑制 | ルール上必要な操作を超える、UI起因の余計な操作を最小にする |
| G7 | アクセシビリティ | 色だけに依存しない。色覚多様性に配慮する |
| G8 | デジタル移植性 | 情報階層とゲーム状態モデルを、物理版とデジタル版で共通化する |
| G9 | 5人時の実用性 | 卓が過度に大きくならず、公開情報を確認できる |

## 2.3 やらないこと

- 画風（写実、ポップ、和風など）の決定。
- イラストの制作。
- ゲームバランスの変更。

**RECOMMENDATION**：色は、装飾（「トマトだから赤」）ではなく、機能（状態・タイプ・イベント極性・プレイヤー）に割り当てる（§4.4、§19.4）。

## 2.4 UXの評価指標

| 指標 | 定義 |
|---|---|
| 読取回数（reads） | 1つの判断のために、視線を向ける必要のある対象の数 |
| 視線移動 | 読取の間の視線の移動。対象が異なるエリアにある場合は、大きな移動として数える |
| 認知負荷 | Low（暗算なし）／Medium（単純な暗算）／High（複数の対象の集計と暗算）／Very High（多数の対象の走査、時間の重なりの暗算） |
| 追加操作 | ルール上必要な操作を超えて、UIのために発生する操作 |

## 2.5 ASSUMPTION（暫定仮定）

| ID | 仮定 |
|---|---|
| ASM-01 | 作物カードは標準的なポーカーサイズ **63×88 mm**（縦向き）を仮定する。カード寸法は確定事項ではない |
| ASM-02 | イベントカードは、作物カードより小さい **45×63 mm（ミニサイズ）** を仮定する。作物カードとの取り違えを防ぐため（**OQ-10**） |
| ASM-03 | トークンの直径は **14 mm**、緑のみ高さ10 mmの柱形を仮定する |
| ASM-04 | Figma上の換算は **1 mm = 4 px** とする |
| ASM-05 | 標準卓は **1200 × 950 mm** の長方形を仮定する（§12〜14） |
| ASM-06 | 得点表（RULE §11）は、公開情報として扱う（RULE §10に明記なし。**OQ-07**） |
| ASM-07 | 既定のゲームモードは「基本（10ラウンド）」とし、プレイヤーは3〜5人（CPU席を含む） |
| ASM-08 | UIの言語は日本語。英語表記の採用は要件ではない |
| ASM-09 | トークンの実色は、色覚多様性に配慮した値（§19.4）を仮に使う。RULE上の色名「白／緑／赤」は、ラベルとして保持する（**OQ-09**） |

---

# 3. Game-State and Information Inventory

## 3.1 時間階層

**RULE**：`Match → Season → Round → Player Turn → Income Phase` の階層がある（RULE §1、§4、§11）。

| 階層 | 単位 | この階層で変わる情報 | 変化の頻度 |
|---|---|---|---|
| Match | シーズンの列 | 得点表（各シーズンの終了時コイン、合計） | シーズンごとに1回 |
| Season | 10ラウンド（基本）／18ラウンド（上級） | 4作物、山札・場札・イベント山札、各プレイヤーの盤面・コイン、スタートプレイヤー | シーズン開始時に全初期化 |
| Round | イベント更新 → 手番 → 収入 | ラウンド番号、イベント列、手番順（2ラウンド目以降は所持コイン昇順） | 毎ラウンド |
| Player Turn | 1手番＝植える or パス | 場札、所持コイン、対象農地のカードとトークン | 手番ごと |
| Income Phase | 全員同時 | 所持コイン、全トークン、（次ラウンドの）手番順 | 毎ラウンド1回 |

**REQUIREMENT**：UIは、プレイヤーが「今どこにいるのか」（何シーズン目の何ラウンド目の、どのフェーズか）を常に把握できるようにする（§16）。

## 3.2 ゲーム状態モデル（物理版とデジタル版で共通）

**RECOMMENDATION**：情報階層とゲーム状態モデルを共通化する。物理版の配置は、この状態モデルの投影である。

```
Match
  seasons: Season[]                       # 基本はプレイヤー数と同数
  startPlayerOfSeason: playerId
  scoreSheet: { seasonIndex, playerId -> endCoins }, totals
Season
  mode: "basic"(10R) | "advanced"(18R)
  round: 1..R ,  phase: "eventUpdate" | "turns" | "income"
  crops: Crop[4]                          # 公開
  market: CropCard[3 or 4]                # 公開
  cropDeck: { count }                     # 枚数のみ公開
  cropDiscard: CropCard[]                 # 公開
  eventRow: EventCard[0..3]               # [0]=今, [1]=次, [2]=次の次
  activeArea: EventCard[0..1]             # 継続中
  eventDeck: { count }  eventDiscard: EventCard[]  excluded: 6 (basicのみ,非公開)
  turnOrder: playerId[]                   # R1=座席順(スタートから時計回り), R2+=コイン昇順・同数は前ラウンド順
  players: Player[3..5]
Player
  coins: int(公開)   slots: Slot[5]
Slot
  index: 1..5   acquired: bool
  card: CropCard | null                   # 前作カードを含む（植え替えまで残る）
  tokens: { white:0..4, green:0..4, red:0..4 }   # 同時に非0なのは高々1色
Derived（公開情報から導出。状態として保存しない）
  slotState:  unacquired | waiting | harvesting | idle_cooling | idle_free
  firstIncomeRound / lastIncomeRound / idleFromRound / sameCropPlantableFromRound
  harvestCountByCrop[crop]                # 今ラウンドの収入フェーズ開始時の収穫中の農地数
  eventModifierByCrop[crop]               # 有効な最大2枚の合計
  incomeNow[crop] = max(min, base − (harvestCount−1) + modifier)
```

**RULE**：`tokens` は、同時に1色しか非0にならない（§6.2の状態機械）。UIはこの不変条件に依存してよい。

## 3.3 情報インベントリ

**凡例**

- 参照距離：**手元**（持っているカード・コイン）／**自分**（自分のプレイヤーエリア）／**中央**（テーブル中央）／**他者**（他プレイヤーのエリア）／**全体**（テーブル全体）。
- 重要度：**Primary**（意思決定の中心）／**Secondary**（意思決定に使う）／**Tertiary**（時々参照）／**Reference only**（通常は不要）。
- 公開性は、RULE §10による。「導出」は、公開情報から導ける情報を指す。

### A. 進行・時間

| 情報 | 所有者 | 公開性 | 静的/動的 | 変化頻度 | 参照頻度 | 参照距離 | 重要度 | 推奨表示場所 |
|---|---|---|---|---|---|---|---|---|
| 現在シーズン／総シーズン数 | 共有 | 公開 | 動的 | シーズンごと | 低 | 全体 | Secondary | SeasonIndicator |
| モード（基本／上級）と総ラウンド数 | 共有 | 公開 | 静的（シーズン内） | シーズンごと | 低 | 全体 | Secondary | RoundIndicator（セル数） |
| 現在ラウンド／残りラウンド | 共有 | 公開 | 動的 | 毎ラウンド | 高（毎手番） | 全体 | **Primary** | RoundIndicator |
| 現在フェーズ（イベント更新／手番／収入） | 共有 | 公開 | 動的 | ラウンド内3回 | 中 | 中央 | Secondary | PhaseRail（RoundIndicatorに統合） |
| スタートプレイヤー | 共有 | 公開 | 動的 | シーズンごと | 低 | 全体 | Secondary | StartPlayerMarker |
| 手番順（R1＝座席順、R2〜＝コイン昇順・同数は前ラウンド順） | 共有 | 導出 | 動的 | 毎ラウンド | 中 | 中央 | Secondary | TurnOrderTrack |
| 現在の手番プレイヤー | 共有 | 公開 | 動的 | 手番ごと | 高 | 全体 | **Primary** | TurnOrderTrack ＋ 手番中の強調 |
| 得点表 | 共有 | 公開（ASM-06） | 動的 | シーズンごと | 低 | 中央（端） | Tertiary | ScoreBoard |
| 今シーズンの4作物 | 共有 | 公開 | 静的（シーズン内） | シーズンごと | 高 | 中央 | **Primary** | SeasonCropPanel ×4 |

### B. 共通エリア

| 情報 | 所有者 | 公開性 | 静的/動的 | 変化頻度 | 参照頻度 | 参照距離 | 重要度 | 推奨表示場所 |
|---|---|---|---|---|---|---|---|---|
| 作物の場札（3〜4枚） | 共有 | 公開 | 動的 | 毎手番 | 高 | 中央 | **Primary** | CropMarket |
| 作物山札の枚数 | 共有 | 枚数のみ | 動的 | 購入ごと | 低 | 中央（端） | Tertiary | CropDeck |
| 作物山札の中身・順番 | 共有 | 非公開 | — | — | — | — | Reference only | 伏せる |
| 作物捨て札（内容） | 共有 | 公開 | 動的 | 植え替えごと | 低 | 中央（端） | Tertiary | CropDiscard |
| イベント列（今／次／次の次） | 共有 | 公開 | 動的 | 毎ラウンド | 高 | 中央 | **Primary** | EventTimeline |
| 発動中置き場（継続中、最大1枚） | 共有 | 公開 | 動的 | 毎ラウンド | 高 | 中央 | **Primary** | ActiveEventArea |
| イベント山札の枚数 | 共有 | 枚数のみ | 動的 | 毎ラウンド | 低 | 中央（端） | Tertiary | EventDeck |
| イベント捨て札（内容） | 共有 | 公開 | 動的 | 毎ラウンド | 低 | 中央（端） | Tertiary | EventDiscard |
| 除外された6枚（基本モード） | 共有 | 非公開 | 静的 | シーズン開始のみ | なし | 隅 | Reference only | 除外ポケット |
| イベント列に出した累計／上限（残りイベント枚数） | 共有 | 導出 | 動的 | 毎ラウンド | 低 | 中央 | Tertiary | EventTimeline の脇（小） |

### C. プレイヤーごと

| 情報 | 所有者 | 公開性 | 静的/動的 | 変化頻度 | 参照頻度 | 参照距離 | 重要度 | 推奨表示場所 |
|---|---|---|---|---|---|---|---|---|
| 所持コイン | 各P | 公開 | 動的 | 毎ラウンド＋支払い時 | 高 | 自分／全体（比較） | **Primary** | CoinArea ＋ CoinLedger |
| 取得済み／未取得の農地スロット | 各P | 公開 | 動的 | 取得時 | 中 | 自分／他者 | Secondary | FarmSlot（SlotCover） |
| 次の農地取得コスト | 各P | 導出 | 動的 | 取得時 | 高（意思決定） | 自分 | **Primary** | PlayerBoard（印刷＋最左のSlotCover） |
| 各農地の作物（識別） | 各P | 公開 | 動的 | 植え付け時 | 高 | 全体 | **Primary** | FarmSlot ＞ CropCard |
| 各農地の状態（待機／収穫／空き・休耕中／空き・自由） | 各P | 導出（トークン） | 動的 | 毎ラウンド | 高 | 全体 | **Primary** | FarmSlot（レーン＋バッジ） |
| 白（待機）トークン数 | 各P | 公開 | 動的 | 毎ラウンド | 高 | 全体 | **Primary** | 待レーン |
| 緑（収穫）トークン数 | 各P | 公開 | 動的 | 毎ラウンド | 高 | 全体 | **Primary** | 収レーン |
| 赤（クールダウン）トークン数 | 各P | 公開 | 動的 | 毎ラウンド | 中 | 自分（詳細）／他者（有無） | Secondary（自分）／Tertiary（他者） | 休レーン |
| 前作カード（空き農地に残る） | 各P | 公開 | 動的 | 収穫終了〜植え替え | 高（同種判定） | 自分 | **Primary** | FarmSlot のカード領域（そのまま） |

### D. 静的カード情報

| 情報 | 所有者 | 公開性 | 静的/動的 | 変化頻度 | 参照頻度 | 参照距離 | 重要度 | 推奨表示場所 |
|---|---|---|---|---|---|---|---|---|
| 作物名・グリフ（識別） | 共有 | 公開 | 静的 | なし | 高 | 全体 | **Primary** | CropCard（Identity region、四辺に冗長） |
| 栽培コスト | 共有 | 公開 | 静的 | なし | 高（購入時） | 手元／中央 | **Primary** | CropCard（Economy region） |
| 待機 W | 共有 | 公開 | 静的 | なし | 高 | 手元／中央 | **Primary** | CropCard（Lifecycle strip） |
| 収穫 H | 共有 | 公開 | 静的 | なし | 高 | 手元／中央 | **Primary** | CropCard（Lifecycle strip） |
| 基本収入 | 共有 | 公開 | 静的 | なし | 高 | 手元／中央 | **Primary** | CropCard（Income region）／作物パネルの収入はしご |
| 最低保証 | 共有 | 公開 | 静的 | なし | 中 | 手元／中央 | Secondary | 同上 |
| 連作CD C | 共有 | 公開 | 静的 | なし | 中（植え替え時） | 手元 | Secondary | CropCard（Cooldown region） |
| 作物タイプ（即金／一括／長期継続／中間） | 共有 | 公開 | 静的 | なし | 低 | 全体 | Tertiary | タイプ帯（機能色）。文字は補助（RULE §12） |
| イベントの対象作物 | 共有 | 公開 | 静的 | なし | 高 | 中央 | **Primary** | EventCard（グリフ＋作物名） |
| イベントの数値（±2、±4） | 共有 | 公開 | 静的 | なし | 高 | 中央 | **Primary** | EventCard（符号付き大数字＋▲▼） |

### E. 導出情報（計算で得られる。**新しい情報ではなく、公開情報の再表現**）

| 情報 | 所有者 | 公開性 | 静的/動的 | 変化頻度 | 参照頻度 | 参照距離 | 重要度 | 推奨表示場所 |
|---|---|---|---|---|---|---|---|---|
| 作物別：今ラウンドの収穫中の農地数（全プレイヤー計） | 共有 | 導出 | 動的 | 毎ラウンド | 高 | 全体 | **Primary** | SeasonCropPanel ＞ HarvestTally（D-02）／デジタルは収穫マトリクス |
| 作物別：現在有効なイベント補正の合計 | 共有 | 導出 | 動的 | 毎ラウンド | 高 | 中央 | **Primary** | SeasonCropPanel ＞ ModifierWell |
| 作物別：今ラウンドの1農地あたりの収入 | 共有 | 導出 | 動的 | 毎ラウンド | 高 | 中央 | **Primary** | 収入はしご（読取）／デジタルは自動計算 |
| 農地別：初回収入のラウンド（＝現在＋白の数） | 各P | 導出 | 動的 | 毎ラウンド | 中 | 自分／他者 | Secondary | デジタルのみ（ツールチップ）。物理は読取規則（§6.3） |
| 農地別：最終収入のラウンド、空きになるラウンド | 各P | 導出 | 動的 | 毎ラウンド | 中 | 自分 | Secondary | 同上 |
| 農地別：同種を再び植えられるラウンド | 各P | 導出 | 動的 | 毎ラウンド | 中 | 自分 | Secondary | 同上（赤の数が「あと何ラウンド」） |
| 農地別：別作物／同種の合法性 | 各P | 導出 | 動的 | 毎ラウンド | 高 | 自分 | **Primary** | 物理は「赤のレーンが空か」で読む。デジタルは合法手表示 |
| 作物別：確定している将来の収穫中の農地数 | 共有 | 導出 | 動的 | 毎ラウンド | 中 | 全体 | Secondary | デジタルのみ（Case C）。他者の将来の植え付けは含まない |
| 対象作物が、残りラウンド内に収入をもたらす回数 | 手番プレイヤー | 導出 | 動的 | 毎ラウンド | 中 | 手元／中央 | Secondary | RoundIndicator と作物カードの時間軸の並置（§16） |


---

# 4. Information Hierarchy

## 4.1 情報の階層（視認性の要求レベル）

| 階層 | 要求 | 含まれる情報 |
|---|---|---|
| **T0：全員が常に一目で** | テーブルの反対側からでも読める | 現在ラウンド／フェーズ、現在の手番、有効なイベント（対象作物と符号）、各農地の状態（待／収／空き）、各農地の作物の識別、所持コインの大小 |
| **T1：意思決定時に自分の視野で** | 自分のエリアで、数値まで読める | 場札の栽培コスト・W・H・基本収入・最低保証・CD、農地取得コスト、前作カードと赤トークンの有無、作物別の収穫中の農地数と収入 |
| **T2：参照** | 探せば見つかる | 捨て札の内容、山札の枚数、得点表、作物タイプ、イベント列に出した累計 |
| **T3：Reference only** | 通常は不要 | 山札の中身（非公開）、除外された6枚（非公開） |

## 4.2 視認距離の仕様

**ASSUMPTION**：テーブルの大きさ（§12〜14）から、次の3つの距離を仮定する。

| レベル | 想定距離 | 読めるべきもの | 最小サイズ（ASSUMPTION） |
|---|---|---|---|
| **L0（テーブル全体）** | 約1.0〜1.5 m | 状態バッジの形、収穫レーンに緑があるか、イベントの極性（▲▼）、現在ラウンドの強調、手番の強調 | 形状の外接寸法 ≥ 12 mm、バッジ ≥ 10 mm |
| **L1（向かい側）** | 約0.7〜1.0 m | 作物のグリフ、頭字、大数字（イベント値、収入はしご、コイン総額） | グリフ ≥ 18 mm、大数字の高さ ≥ 10 mm |
| **L2（自分）** | 約0.3〜0.5 m | 栽培コスト・W・H・基本収入・最低保証・CDの数値、ラベル | 数字の高さ ≥ 4 mm（約12pt相当） |

## 4.3 情報 × 距離のマトリクス

| 情報 | L0 | L1 | L2 |
|---|---|---|---|
| 農地の状態（待／収／空き） | ✔（形とレーン位置） | ✔（トークン数） | — |
| 作物の識別 | — | ✔（グリフ・頭字・帯） | ✔（作物名の文字） |
| 有効なイベント | ✔（極性） | ✔（対象作物のグリフ） | ✔（数値） |
| 収入の数値 | — | ✔（作物パネルの読取値） | ✔ |
| 栽培コスト・W・H・基本収入 | — | — | ✔ |
| 農地取得コスト | — | — | ✔（自分のボード） |
| 現在ラウンド | ✔（強調セル） | ✔（数字） | — |
| 所持コイン | ✔（大小の比較は台帳で） | ✔（台帳の総額） | ✔（手元の詳細） |

## 4.4 符号化チャネルの割り当て（色・形・位置を機能に割り当てる）

**RECOMMENDATION**：色を装飾に使わず、機能に割り当てる。各機能は、**色だけに依存せず**、必ず形・位置・アイコンのいずれかで冗長化する。

| 機能 | 主符号 | 副符号 | 色の使い方 |
|---|---|---|---|
| 農地の状態 | **位置（レーン）** | 形状（○⬡◇）、アイコン（砂時計・穂・月） | 待＝白、収＝緑、休＝朱赤（**状態専用**） |
| 作物の識別 | **輪郭（グリフ）** | 頭字、名前の文字 | 色は使わない（識別の主符号にしない） |
| 作物のタイプ | 帯の**模様** | 帯の色 | タイプ帯の色（4色）。状態色と重ならない色を使う |
| イベントの極性 | **▲（増）／▼（減）と符号** | 数値の大きさ（シェブロン数） | 増＝暖色、減＝寒色。色は副次 |
| プレイヤー | **形＋番号** | 色 | プレイヤー色は、ボード枠・コインマーカー・スタートマーカー・台帳の行に限定 |
| 時間（待・収・休のセル） | セルの**塗りパターン**（中抜き／塗り／斜線） | アイコン | 状態色と揃える |

具体的な色・パレットは§19.4。

## 4.5 設計原則

| # | 原則 |
|---|---|
| P1 | **状態は位置で、識別は形で、数値は所有者の向きで表す** |
| P2 | 他のプレイヤーが読む必要のある情報（識別・状態）は、**回転に強い**（向きに依存しない）記号にする |
| P3 | 数値を読むのは主に所有者（自分向き）と、意思決定時の場札・共通パネル（両向き印刷） |
| P4 | 同じ概念は、物理版でもデジタル版でも同じ形・位置・語で表す |
| P5 | トークンが**唯一の真実**。冗長な物理フラグは足さず、冗長表示は派生表示（読み取り専用）に限る |
| P6 | 1つの農地の中で、情報がカードとトークンで重ならない（トークンがカードの数値を隠さない） |
| P7 | UI起因の操作を増やさない。増やす場合は、任意の補助モジュールとして分離する |
| P8 | 補助（自動計算・合法手表示）は、「表示補助」か「新しい情報の付与」かを区別する（§17） |

---

# 5. Player Decision / Visual Search Analysis

## 5.1 分析の前提

- 「推定視線移動」は、意思決定に必要な読取（reads）の数と、その間の大きな視線移動の数の目安である（実測ではない。**ASSUMPTION**）。
- 比較する3つの条件：**（B）補助なしの物理**（RULE準拠の素朴な配置）、**（P）推奨物理UI**（本文書の推奨）、**（D）デジタル（表示補助A1まで、§17）**。
- 3〜5人のテーブルでは、他プレイヤーの農地は最大20スロット（他の4人 × 5）である。

## 5.2 ケース別の分析

### ケースA：「この場札のジャガイモを買うべきか？」

| 項目 | 内容 |
|---|---|
| 1. 必要情報 | ジャガイモの栽培コスト2・待機2・収穫2・基本収入9・最低保証4・CD4（RULE §12）／自分の所持コイン／自分の空き農地の有無と、次の取得コスト／残りラウンド（初収入が間に合うか）／ジャガイモを収穫中・待機中の農地（全員）／ジャガイモ対象のイベント（継続中・今・次・次の次）／他の場札との比較 |
| 2. 情報の現在位置 | 場札（中央）／コイン・農地（自分）／ラウンド（共通ボード）／他者の農地（他者）／イベント（中央） |
| 3. 推定視線移動 | **B**：場札 → 数値を読む → コイン → 自分の農地の空き・取得コスト → ラウンド → 他者の農地を走査 → イベント列。約8〜10回。**P**：場札のカード時間軸（数値内蔵）→ 作物パネルの「ジャガイモ」行（収穫中の枚数、イベント補正）→ 自分のボード（コインと取得コストが同じ帯）→ ラウンド表示。約5〜6回。**D**：場札にオーバーレイ（収穫中の枚数、初収入ラウンド）。約3回 |
| 4. 記憶しておく必要のある情報 | 他者の待機中のジャガイモの残り（白の数）から見積もる収穫開始時期。前ラウンドまでの競合の推移 |
| 5. 認知負荷 | **B：High**。**P：Medium**。**D：Low〜Medium** |
| 6. UIで減らせる負荷 | 作物パネルに静的数値・収穫中の枚数・イベント補正を集約（**REC-06**）。作物カードの時間軸で、W・Hを長さとして把握（**REC-11**）。コインと取得コストを同じ帯に並べる（**REC-05**）。デジタルでは、場札に「初収入ラウンド（＝現在＋W）」を重ねる（公開情報の再表示） |

### ケースB：「この農地に同じ作物をもう一度植えられるか？」

| 項目 | 内容 |
|---|---|
| 1. 必要情報 | 対象農地に待機・収穫のトークンがない（空き）か／前作カードの作物（植えたい作物と同じか）／赤トークンの有無（同種は赤がなくなるまで不可。RULE §6.3）／赤の数（あと何ラウンドか） |
| 2. 情報の現在位置 | すべて対象の農地スロットの中（レーン＋前作カード） |
| 3. 推定視線移動 | **B**：カードとトークンの山を見て、状態を推論。約3〜4回。**P**：待・収レーンが空か → 休レーンに赤があるか → 前作グリフ ＝ 植えたい作物か。約2〜3回、**同一スロット内で完結**。**D**：合法手のハイライト（1回） |
| 4. 記憶しておく必要のある情報 | なし（前作カードが残り、赤が残りラウンドを表す） |
| 5. 認知負荷 | **B：Medium**（トークンが混在すると状態を誤認する）。**P：Low**。**D：Low** |
| 6. UIで減らせる負荷 | レーン分離で、「赤が空か」だけを判定できる（**REC-01**）。ボードに読取規則を印刷（§10.6）。デジタルは合法手の表示（**D-09**） |

### ケースC：「次ラウンドにトウモロコシを植えた場合、収穫期に競合しそうか？」

前提：次ラウンド（現在＋1）に植えると、トウモロコシ（W3、H2）の収入は、現在＋4と現在＋5のラウンド終了時になる（RULE §6.1）。

| 項目 | 内容 |
|---|---|
| 1. 必要情報 | 全プレイヤーのトウモロコシ農地の状態：待機中なら白の数、収穫中なら緑の数 → それぞれの収穫窓 [現在＋白, 現在＋白＋緑−1] が現在＋4〜＋5と重なるか／自分の他のトウモロコシ農地／イベント（トウモロコシ対象）。ただし、現在＋4以降のイベントは列の外（先読みは現在＋2まで）で**不確定** |
| 2. 情報の現在位置 | 他者の農地（全員分。最大20スロット）、自分の農地、イベント列 |
| 3. 推定視線移動 | **B**：全農地を順に走査し、トウモロコシを見つけるたびにレーンを読み、ラウンド番号に換算して重なりを判定。約10〜20回。**P**：走査は同じだが、作物のグリフとレーン位置で、トウモロコシと待／収の判別が速くなる。約8〜14回（**大きくは減らない**）。**D**：収穫予定の表示（確定分のみ）。約2〜3回 |
| 4. 記憶しておく必要のある情報 | 走査した農地ごとの収穫窓（暗算）。累積した各ラウンドの競合数 |
| 5. 認知負荷 | **B：Very High**。**P：High**。**D：Low〜Medium** |
| 6. UIで減らせる負荷 | 物理：作物パネルの任意モジュール「収穫予定ストリップ」（**ALT-05**、進行係が更新。操作が増えるため任意）。デジタル：確定している将来の収穫中の農地数を表示（表示補助。他者の**将来の植え付けは含まない**） |

**OPEN QUESTION（OQ-17）**：現在のルールでは、他プレイヤーの将来の収穫窓を把握するために、全農地を走査して暗算する必要がある。物理UIでは、これがゲーム中で最も認知負荷の高い判断になる可能性がある。ルールは変更しない。UI補助の採否は人間が決める（**D-02**）。

### ケースD：「このラウンド、自分のトマトはいくら収入を得るか？」

| 項目 | 内容 |
|---|---|
| 1. 必要情報 | 自分のトマト農地が収穫中（緑あり、白なし）か／全プレイヤーのトマト収穫中の農地数（自分の別農地を含む）／有効なイベントのうちトマト対象の数値の合計（最大2枚）／基本収入9・最低保証4。収入 ＝ max(4, 9 −（収穫中の他の農地数）＋補正) |
| 2. 情報の現在位置 | 自分の農地／全員の農地／イベント列と発動中置き場／カード |
| 3. 推定視線移動 | **B**：自分の緑を確認 → 全員の農地を走査してトマトの緑を数える → 発動中置き場とイベント列を確認 → 暗算。約8〜15回。**P**：作物パネルのトマト行を1回読む（収穫中の枚数、イベント補正、収入はしごの値）。約2〜3回。**D**：自動計算（1回） |
| 4. 記憶しておく必要のある情報 | 数えたトマトの枚数、途中の計算値 |
| 5. 認知負荷 | **B：High**。**P：Low〜Medium**。**D：Low** |
| 6. UIで減らせる負荷 | 作物パネル（**REC-06**）：HarvestTally、ModifierWell、収入はしごを1か所に。収入フェーズの手順を「計数 → 受取 → 更新」の順に印刷（**REC-07**）。**収入フェーズの開始時の状態で数える**（緑を置いたばかりの農地を含めない。RULE §7） |

### ケースE：「新しい農地を買うか、既存農地を使うか？」

| 項目 | 内容 |
|---|---|
| 1. 必要情報 | 所持コイン／次の農地取得コスト（0/1/2/3/4のいずれか）／植えたい作物の栽培コスト／既存の空き農地の有無、前作、赤（同種を植えたい場合の制限）／残りラウンド／農地が5枚の上限に達していないか |
| 2. 情報の現在位置 | 自分のボード（取得コスト、スロット）／コイン／場札／ラウンド表示 |
| 3. 推定視線移動 | **B**：場札 → コスト → コイン → ボードで空きを走査 → 取得コストの表を確認 → 比較。約6〜8回。**P**：「所持コイン｜次の取得コスト｜栽培コスト」を、同じ帯（EconomyStrip）で読む → 空きスロットを走査（レーン）。約4〜5回。**D**：支払い合計のプレビュー（表示補助）。約2回 |
| 4. 記憶しておく必要のある情報 | 場札の目的の作物。各スロットが空く予定のラウンド |
| 5. 認知負荷 | **B：Medium**。**P：Low〜Medium**。**D：Low** |
| 6. UIで減らせる負荷 | 取得コストを各スロットの下に常時印刷し、未取得スロットはSlotCoverで覆い、**最左のカバーが「次」**になる（**REC-04、REC-05**）。コインと取得コストを近接配置（PlayerBoardの帯）。デジタルは、選択中の場札に対する支払い合計を表示 |

### ケースF：「残りラウンド数を考えると、この作物を今植えて収入が発生するか？」

| 項目 | 内容 |
|---|---|
| 1. 必要情報 | 現在ラウンド r／総ラウンド R／作物のW・H。初収入は r＋W のラウンド終了時（RULE §6.1）。収入の回数 ＝ min(H, R −（r＋W）＋1)（0以下なら収入なし） |
| 2. 情報の現在位置 | RoundIndicator（中央または端）／作物カード（場札または手元） |
| 3. 推定視線移動 | **B**：ラウンド表示を見る → 作物カードのW・Hを見る → 暗算。約3回。**P**：RoundIndicatorの「残りセル」と作物カードの時間軸（W＋Hのセル数）を、同じ文法（1セル＝1ラウンド）で並置して比較。約2回。**D**：「最終的な収入の回数」を表示（表示補助） |
| 4. 記憶しておく必要のある情報 | 現在のラウンド番号（RoundIndicatorが常時表示されれば不要） |
| 5. 認知負荷 | **B：Medium**。**P：Low**。**D：Low** |
| 6. UIで減らせる負荷 | 時間の共通文法（**REC-13**）。RoundIndicatorの残りセルを強調表示（**REC-14**）。カードの時間軸には、「ここまでに植えれば間に合う」といった結論を印刷しない（RULE §6.1の時間の長さを示すのみで、判断はプレイヤーが行う） |

## 5.3 ケースの総括

| ケース | 主な負荷の原因 | B | P | D |
|---|---|---|---|---|
| A 購入の判断 | 情報が5か所に散在 | High | Medium | Low〜Medium |
| B 同種の再植 | トークン混在による状態の誤認 | Medium | **Low** | Low |
| C 将来の競合 | 全農地の走査と時間の重なりの暗算 | Very High | **High** | Low〜Medium |
| D 今の収入 | 全農地の走査（収穫中の集計） | High | **Low〜Medium** | Low |
| E 農地の購入 | コスト情報の分散 | Medium | Low〜Medium | Low |
| F 残りラウンド | 暗算 | Medium | Low | Low |

**発見**：

1. 推奨物理UIで大きく減るのは、B・D・E・F。**Cは物理では高いまま**であり、これはルール構造（将来の収穫窓の暗算）に由来する。
2. 走査コスト（ケースC、D）は、「作物パネルの集計（HarvestTally）」が唯一の大きな削減手段である。ただし、これは進行係の操作を追加する（**D-02**）。
3. デジタル版では、C・Dを大きく下げられるが、それは**計算力の差**（RULE §14.7で問題とした「イベントを織り込める人の優位」）に影響しうる。既定の補助レベルは、人間が決める（**D-09**）。

---

# 6. Farm Lifecycle and State Representation

## 6.1 ルールの再掲（UIの前提）

**RULE**（§6.1〜6.3）：

- 植えるとき、白を W 個置く。収入フェーズごとに、白があれば1個取り除く（収入なし）。白がなくなったら緑を H 個置く（**そのラウンドは収入なし**）。
- 白がなく緑があれば、収入を得て緑を1個取り除く。緑がなくなったら赤を C 個置く（C=0なら置かない。**そのラウンドは赤を取り除かない**）。
- 緑が0で赤があれば、赤を1個取り除く。
- **空き**は、手番の開始時に白も緑もない農地。赤や前作カードが残っていても空き。
- **別の作物**は、空いた直後から植えられる。**同じ作物**は、赤がなくなるまで植えられない。
- 前作カードは、新しい作物を植えるまで残る。植えるとき、前作カードは作物の捨て札へ、赤は除去する。

## 6.2 構造上の重要な発見

1. **同時に非0になるトークンの色は、高々1色**である（白 → 緑 → 赤の順に置き換わる）。したがって、トークンは「3つのモードを持つ1つのカウンタ」とみなせる。
2. 農地の状態は、次の**5状態**に一意に決まる。

| 状態 | 白 | 緑 | 赤 | 前作カード／カード |
|---|---|---|---|---|
| `unacquired`（未取得） | — | — | — | なし |
| `waiting`（待機中） | ＞0 | 0 | 0 | あり（現在の作物） |
| `harvesting`（収穫中） | 0 | ＞0 | 0 | あり（現在の作物） |
| `idle_cooling`（空き・休耕中） | 0 | 0 | ＞0 | あり（前作） |
| `idle_free`（空き・自由） | 0 | 0 | 0 | あり（前作） |

3. **占有の軸と連作制限の軸は別**である。
   - **占有の軸**：未取得／待機中／収穫中／空き。
   - **連作制限の軸**：赤があれば、同種だけ植えられない。この軸が意味を持つのは、空きのときだけ（赤は植え付け時に取り除かれ、収穫中・待機中には存在しない）。
   - つまり、`idle_cooling` は「別の作物は植えられるが、同種は植えられない」状態で、`idle_free` は「どちらも植えられる」状態。

```
              植える              白がなくなる           緑がなくなる         赤がなくなる／植え替え
未取得 ──────▶ waiting ─────────▶ harvesting ─────────▶ idle_cooling ─────────▶ idle_free
(取得+植付)   (白 W 個)            (緑 H 個)              (赤 C 個)              (トークンなし)
                 ▲                                          │  C=0 のとき          │
                 └───────── 別作物を植える（前作と赤を除去）◀─┴───(直接 idle_free)───┘
                 └───────── 同種は idle_free になってから ─────────────────────────┘
```

4. `idle_free` と `idle_cooling` は、どちらも「空き」で、別作物を植えられる。UIは、**空きの判定**（白・緑がない）と、**同種の判定**（赤がない）を、別々に読めるようにする。

## 6.3 読取規則（手番フェーズ中の、トークンの読み方）

**REQUIREMENT**：手番中の状態から、次のように読める必要がある。現在のラウンドを **t** とする。

| 状態 | 読み方 | 例（RULE §6.4、§22.4のF1） |
|---|---|---|
| 待機中（白 w） | 初収入は「t＋w」のラウンド終了時 | 白2、t=5 → R7の終了時が初収入 |
| 収穫中（緑 g） | 今ラウンドの終了時を含めて g 回の収入（t〜t＋g−1）。空きになるのは t＋g | 緑3、t=5 → R5・R6・R7の終了時に収入、R8から空き |
| 空き・休耕中（赤 c） | 同種を再び植えられるのは「t＋c」のラウンドから。別作物は今すぐ | 赤4、t=5 → R9から同種OK |
| 空き・自由 | 別作物も同種も、今すぐ | — |

**RECOMMENDATION**：この読取規則を、プレイヤーボード上に短く印刷する（§10.6）。デジタルは、ツールチップで表示する。

## 6.4 「トークンの状態」に関する設計上の問い（回答）

| 問い | 回答 |
|---|---|
| カードとトークンをどこに置くか／トークンはカード上か農地ボード上か | **農地ボード上**（カードの外）。**RECOMMENDATION（REC-01）**。理由：（1）カードの数値を隠さない（REQ：最大4個を置いても主要情報が隠れない）。（2）カードは植え替えで入れ替わるが、トークンは農地スロットの状態。（3）レーンを固定できる |
| 白／緑／赤を同じ場所に置くべきか | **別のレーン**（左から 待｜収｜休）。同時に1色しか存在しないが、**位置で状態を符号化**するため。**ALTERNATIVE（ALT-02）**：1レーンに、形状違いのトークンを置く（占有面積は小さいが、位置の冗長性がなくなる） |
| 色だけで区別してよいか | **不可**（REQUIREMENT）。形状（○⬡◇）、アイコン、レーン位置、穴の形状ロックで冗長化（**REC-02**） |
| 数量を「残りラウンド数」として直感的に理解できるか | 「残り」として読めるが、**意味がモードごとに微妙に異なる**：白＝初収入までのラウンド数（t＋w）、緑＝残りの収入回数、赤＝同種が再び可能になるまでのラウンド数。§6.3の読取規則で明示する |
| 前作カードが残っているが農地は空いている、をどう見せるか | **物理では、追加のフラグを足さない**（**REC-03**）。待・収レーンが空であることが「空き」であり、トークンが唯一の真実。前作カードは、そのまま残す。**ALTERNATIVE（ALT-03）**：カードを手前へ1段ずらす。**デジタル**では、前作カードを減光し「前作」タグを付ける（**REC-21**） |
| 他プレイヤーから「現在収穫中か」を瞬時に判定できるか | **中央レーンの緑（背の高い六角柱）の有無**で判定する（180°回転しても中央のまま）。集計は作物パネル（**REC-06**）。デジタルは収穫マトリクス（**REC-21**） |
| 同じ作物を再度植えられるかを簡単に判定できるか | **休レーンが空か**だけを見る（**REC-01**）。空なら同種もOK |

**却下した案（自己レビューで修正）**：初稿では、収穫を終えた前作カードを「90°横向きにする」慣習を検討したが、横向きのカード（88mm）がスロット幅（66mm）を超えて隣のスロットに重なるため、**却下**した。180°回転は、向かい側から見ると正立して見え、かえって誤読を生むため、これも却下した。

## 6.5 端点・例外

| 場合 | UIでの扱い |
|---|---|
| 新規取得と同時の植え付け | 農地取得（SlotCoverを取り除く）→ 支払い → カードを置く → 白を W 個（RULE §5の処理順）。前作がないため、同種も植えられる（RULE §5） |
| C=0の作物（ラディッシュ、ネギ、サツマイモ） | 赤は置かない。緑がなくなった時点で `idle_free` |
| 緑を置いたばかりの農地 | そのラウンドの収穫中には**含めない**（RULE §7）。UIは、集計（凍結）を更新より先に行う順序を強制する（§6.6） |
| 赤を置いたばかりの農地 | そのラウンドは赤を取り除かない（RULE §6.2） |
| 最終ラウンド後に緑や白が残る | シーズン終了で全片付け（RULE §3.2）。得点はない（RULE §11） |
| 植え替え | 処理順：支払い → 前作カードを作物の捨て札へ・赤を除去 → カードを置く → 白を W 個 → 場札を補充（RULE §5） |

## 6.6 収入フェーズの手順（トークン更新の順序）

**REQUIREMENT**：収入は、**収入フェーズの開始時の状態**で判定する（RULE §7）。トークンを先に更新すると、緑を置いたばかりの農地を収穫中として数えてしまう。UIは、手順の順序を守らせる。

**RECOMMENDATION（REC-07）**：収入フェーズの手順を、共通ボード（PhaseRail）に次の順で印刷し、デジタルは自動で進める。

| 順 | 手順 | 内容 |
|---|---|---|
| 1 | **計数（凍結）** | 「緑あり・白なし」の農地を、作物別に数える。ここでは、トークンに触らない |
| 2 | **受取** | 各プレイヤーが、自分の各農地の収入 ＝ max(最低保証, 基本収入 −（同種の他の収穫中農地数）＋ イベント補正）を受け取る |
| 3 | **更新** | 各プレイヤーが、農地ごとにトークンを更新する（§6.1）。左のスロットから順に |
| 4 | **台帳** | 所持コインの総額を台帳に反映し、次のラウンドの手番順を更新する（REC-15、REC-16） |

---

# 7. Crop Card Information Architecture

## 7.1 情報の参照頻度による階層

意思決定（第5章）から見た、作物カードの情報の重要度：

| 順位 | 情報 | 主に使う場面 |
|---|---|---|
| 1 | 作物の識別（グリフ、名前、タイプ） | すべて。**他プレイヤーの農地でも必要** |
| 2 | 栽培コスト | 購入（A、E） |
| 3 | 時間情報（待機W、収穫H） | 購入（A）、将来の競合（C）、残りラウンド（F） |
| 4 | 収入（基本収入、最低保証） | 購入（A）、収入の読取（D） |
| 5 | 連作CD | 同種の再植（B）。購入時は補助 |

## 7.2 読み手のロール

| ロール | 状況 | 求められる情報 |
|---|---|---|
| R1 手元 | 場札から取ったカードを持つ | 全情報（近距離） |
| R2 自分のボード | 自分の農地のカード | 識別、CD（前作）、時間 |
| R3 向かい側 | 他プレイヤーの農地のカード | **識別**（輪郭・頭字）、タイプ、（状態はレーンで） |
| R4 横側 | 他プレイヤーの農地（90°回転して見える） | 識別（回転に強い記号） |
| R5 場札 | 全員が中央のカードを見る | 識別、栽培コスト、時間、収入（**両向き**） |

## 7.3 共通の領域（すべての案）

| 領域 | 内容 |
|---|---|
| Identity region | グリフ、作物名、頭字、タイプ帯 |
| Economy region | 栽培コスト |
| Lifecycle region | 待機 W、収穫 H |
| Income region | 基本収入、最低保証 |
| Cooldown region | 連作CD C |
| Token interaction region | **カード上には設けない**（トークンは農地ボードのレーン）。ALT-04のみ例外 |

## 7.4 3つの案

### 案A：数値中心（Numbers-first）

- **情報階層**：識別 → 栽培コスト（大） → W・H・基本収入・最低保証・CD（数字の表）。
- **レイアウト構造**：上段にグリフと名前、その下にタイプ帯。中段に栽培コストの大数字。下段に、ラベル付きの数値の行（待機／収穫／収入／最低／連作）。
- **長所**：数値の正確な読み取り。印刷が単純。
- **短所**：時間の長さが「数字」でしか伝わらず、比較に暗算が要る。他プレイヤーからは、数値が読めない。
- **手元での可読性**：高。**他プレイヤーから**：低（識別はグリフのみ）。**トークンを載せた際**：ALT-04でなければ影響なし。**印刷適性**：高。**デジタルへの転用性**：中（表をそのまま出すだけ）。

```
+-----------------------+
| [グリフ]  ラディッシュ   |   Identity
| ▓▓▓▓ タイプ帯 ▓▓▓▓    |
|   ① 栽培コスト          |   Economy
|  待機 1   収穫 1       |   Lifecycle（数字）
|  収入 7   最低 3       |   Income
|  連作  なし            |   Cooldown
+-----------------------+
```

### 案B：時間軸中心（Timeline-first）

- **情報階層**：識別 → 栽培コスト → **時間軸（待・収・休のセル列）** → 収入（収穫セルの上）。
- **レイアウト構造**：上段にグリフ・名前・栽培コスト。中段に、1セル＝1ラウンドの**帯状の時間軸**（待＝中抜き、収＝塗り、休＝斜線）。収入（基本収入と最低保証）は、収穫セルの上に表示する。C=0のときは、斜線セルの代わりに「連作可」のアイコンを表示する。
- **長所**：W・H・Cの**長さが視覚的に比較できる**（ケースA、F）。時間軸の形が、作物ごとに異なる（「時間の指紋」）ため、識別の補助にもなる。トークンのレーンの語彙（○／塗り／斜線）と一致。
- **短所**：時間軸のセルが細かく、数字の可読性が案Aより低い。設計の労力が大きい。
- **手元での可読性**：高。**他プレイヤーから**：中（グリフ＋時間軸の輪郭）。**トークンを載せた際**：影響なし（トークンはカード外）。**印刷適性**：中（細いセル）。**デジタルへの転用性**：**高**（同じ帯を、進行状況のアニメーションに使える）。

```
+-----------------------+
|①  [グリフ]  トマト      |   Economy（コイン型）＋ Identity
| ▓▓▓▓ タイプ帯 ▓▓▓▓    |
|        収入 9（最低 4）  |   Income（収穫セルの上）
|  ○○○  ●●●●  ▒▒▒▒     |   Lifecycle strip（待3・収4・休4）
|  待     収     休       |
+-----------------------+
```

### 案C：アイコン中心（Icon-first）

- **情報階層**：作物の絵（グリフ大） → ピクトグラムの反復（砂時計×W、穂×H、月×C） → コインの数。
- **レイアウト構造**：中央に大きなグリフ。下段に、待・収・休のアイコンを、それぞれ W・H・C 個並べる。栽培コストと収入は、コイン型アイコンと数字。
- **長所**：他プレイヤーからも、識別と大まかな長さがつかめる。言語に依存しない。
- **短所**：アイコンの個数を数える必要がある（Cが4のとき4個）。数値の正確な読み取りは案Aより遅い。基本収入と最低保証の2つの数字は、結局、数字で示す必要がある。
- **手元での可読性**：中。**他プレイヤーから**：**高**。**トークンを載せた際**：影響なし。**印刷適性**：高。**デジタルへの転用性**：高。

```
+-----------------------+
|        [グリフ 大]       |   Identity
|  ▓▓▓▓ タイプ帯 ▓▓▓▓    |
|  ⌛⌛  ☀☀  ☾☾          |   Lifecycle（個数）
|  ① 栽培   ▲7（3）       |   Economy / Income
+-----------------------+
```

### 案D（例外）：カード上のトークン領域（ALT-04）

カード上にトークンのマスを印刷する案。**却下**：トークンが数値を隠す（REQUIREMENT違反）。比較のために記載するのみ。

## 7.5 比較

| 評価軸 | 案A 数値 | 案B 時間軸 | 案C アイコン |
|---|---|---|---|
| 手元での可読性 | 5 | 4 | 3 |
| 他プレイヤーからの可読性 | 1 | 3 | 4 |
| 時間情報の比較のしやすさ（ケースA、F） | 2 | **5** | 3 |
| 収入・最低保証の正確な読み取り | 5 | 4 | 3 |
| トークンとの併用（カード外にレーンがある前提） | 5 | 5 | 5 |
| 印刷適性 | 5 | 3 | 4 |
| デジタルへの転用性 | 3 | **5** | 4 |
| 作物ごとの「形」の違い（識別の補助） | 1 | **5** | 3 |

（1＝低い、5＝高い。**ASSUMPTION**：設計者の判断による相対評価）

**RECOMMENDATION（REC-11）**：**案Bを基本**とし、栽培コストと収入の数値は案Aの可読性（大きさ・コントラスト）を取り入れる（B＋）。他プレイヤーの識別は、案Cのグリフの大きさ（≥18mm）を借りる。Figmaでは、`layout=A|B|C` をバリアントとして3案とも作り、比較できるようにする。

## 7.6 全作物の対応表（RULE §12の値を転記）

| 作物 | 栽培コスト | 待 W | 収 H | 休 C | 基本収入 | 最低保証 | タイプ | 時間軸（セル） | 総セル数 |
|---|---|---|---|---|---|---|---|---|---|
| ラディッシュ | 1 | 1 | 1 | 0 | 7 | 3 | 即金型 | ○ ● 〔連作可〕 | 2 |
| コマツナ | 1 | 1 | 2 | 2 | 6 | 3 | 即金型 | ○ ●● ▒▒ | 5 |
| ジャガイモ | 2 | 2 | 2 | 4 | 9 | 4 | 中間型 | ○○ ●● ▒▒▒▒ | 8 |
| トウモロコシ | 2 | 3 | 2 | 2 | 10 | 4 | 中間型 | ○○○ ●● ▒▒ | 7 |
| ネギ | 2 | 2 | 4 | 0 | 8 | 3 | 長期継続型 | ○○ ●●●● 〔連作可〕 | 6 |
| エダマメ | 2 | 2 | 1 | 4 | 12 | 5 | 一括型 | ○○ ● ▒▒▒▒ | 7 |
| サツマイモ | 3 | 4 | 1 | 0 | 19 | 8 | 一括型 | ○○○○ ● 〔連作可〕 | 5 |
| カボチャ | 3 | 4 | 3 | 4 | 11 | 4 | 長期継続型 | ○○○○ ●●● ▒▒▒▒ | 11 |
| トマト | 3 | 3 | 4 | 4 | 9 | 4 | 長期継続型 | ○○○ ●●●● ▒▒▒▒ | 11 |
| スイカ | 4 | 4 | 2 | 4 | 13 | 5 | 一括型 | ○○○○ ●● ▒▒▒▒ | 10 |

- 「時間軸（セル）」の〔連作可〕は、C=0を表すアイコン（斜線セルなし）。
- 総セル数の最大は11（カボチャ、トマト）。カードの有効幅（約57 mm）に収めるには、セル幅を **5 mm 以下**にする（**ASSUMPTION**：11×5＝55 mm）。

## 7.7 作物の識別（グリフ、頭字、タイプ帯）

**RECOMMENDATION（REC-12）**：作物の識別は、次の3つで行う。**色を主符号にしない**。

| 符号 | 内容 |
|---|---|
| グリフ（主） | 10種で**輪郭が互いに異なる**シルエット（≥18 mm）。仕上げの絵柄は決めない。下記の特徴を最低限の輪郭の違いとする |
| 頭字（副） | 名前の頭の1〜2文字を、円内に大きく表示する。トウモロコシとトマトはどちらも「ト」で始まるため、この2つは**トウ／トマ**の2文字とする（一覧は下表） |
| タイプ帯（機能色） | 即金型・一括型・長期継続型・中間型の4種を、帯の**模様と色**で表す（RULE §12のタイプ）。作物の色（「トマトは赤」）とは無関係 |

| 作物 | グリフの輪郭の特徴（識別のための最低限の違い） | 頭字 |
|---|---|---|
| ラディッシュ | 先の細い根と、上に広がる葉 | ラ |
| コマツナ | 放射状の葉のロゼット（根は目立たない） | コ |
| ジャガイモ | ゴツゴツした丸い塊 | ジャ |
| トウモロコシ | 縦長の穂と、左右に開く皮 | トウ |
| ネギ | 細い縦長の茎の束 | ネ |
| エダマメ | 湾曲したさやの房 | エ |
| サツマイモ | 両端が細い紡錘形の塊 | サ |
| カボチャ | 縦の溝がある扁平な球と、上のヘタ | カ |
| トマト | 球と、上の星形のがく | トマ |
| スイカ | 大きな楕円と、縞模様 | ス |

**REQUIREMENT**：グリフは、**四辺（上下左右）のいずれからでも識別できる**よう、カードの上下に180°回転した複製を置く（横側の視点は、回転して見える輪郭に頼る）。

## 7.8 カードの裏面

**RECOMMENDATION**：作物カードの裏面と、イベントカードの裏面は、**形（サイズ）と模様と縁の色の3つで区別する**。山札・捨て札を誤って混ぜないため（REQUIREMENT：作物とイベントの捨て札を混同しない）。

| カード | 裏面の識別 |
|---|---|
| 作物カード | 63×88 mm、縁の模様＝「畝」、中央のアイコン＝種 |
| イベントカード | 45×63 mm（**ASM-02**）、縁の模様＝「雲」、中央のアイコン＝天候（太陽と雲） |


---

# 8. Token System

## 8.1 ルールの事実

**RULE**：白（待機）・緑（収穫）・赤（連作クールダウン）の3種。1農地に同時に置くのは最大4個（RULE §2、§6.2）。総数は各約40個（プロトタイプ用の目安）。トークンの操作は、RULE §6.2の状態機械のとおり。

## 8.2 トークンの操作量（RULEの数値からの算出）

1回の植え付けサイクルでは、白を W 個置き W 個取り除き、緑を H 個置き H 個取り除き、赤を C 個置き C 個取り除く。トークンの移動回数は **2×(W＋H＋C)**。

| 作物 | W | H | C | 移動回数 | （参考）進行マーカー方式（ALT-01）の移動回数 |
|---|---|---|---|---|---|
| ラディッシュ | 1 | 1 | 0 | 4 | 3 |
| コマツナ | 1 | 2 | 2 | 10 | 6 |
| ジャガイモ | 2 | 2 | 4 | 16 | 9 |
| トウモロコシ | 3 | 2 | 2 | 14 | 8 |
| ネギ | 2 | 4 | 0 | 12 | 7 |
| エダマメ | 2 | 1 | 4 | 14 | 8 |
| サツマイモ | 4 | 1 | 0 | 10 | 6 |
| カボチャ | 4 | 3 | 4 | 22 | 12 |
| トマト | 3 | 4 | 4 | 22 | 12 |
| スイカ | 4 | 2 | 4 | 20 | 11 |
| **平均** | — | — | — | **14.4** | **8.2** |

- 進行マーカー方式は、1（置く）＋（W＋H＋C）（1ラウンド1マス）で算出。
- **推定**（ASSUMPTION）：1プレイヤー・1シーズンあたり、14.4 × 行動数/人（基本5.8、上級13.2。RULE §14.1）＝ **基本で約84回、上級で約190回**。5人卓の上級では、卓全体で約950回。実際は、作物の組み合わせで変動する。

**発見**：トークンの操作は、ルール上必要な操作の中で**最大のボリューム**である（§15）。

## 8.3 トークンの表現方式の比較

| 方式 | 内容 |
|---|---|
| **T1**（RULEの素朴な実装） | 白・緑・赤の3色の円盤を、農地の脇にまとめて置く |
| **T2**（**推奨**） | 形状（○⬡◇）・色・アイコン・レーン位置で符号化し、**穴の形状ロック**付きのレーン（待｜収｜休）に置く |
| **T3**（ALT-01） | 作物カードの時間軸（帯）に、進行マーカーを1個だけ置き、1ラウンドごとに1マス進める |
| **T4** | 農地ごとに、モード付きのダイヤル（回転式カウンタ）を1個置く |

| 評価軸 | T1 | T2 | T3 | T4 |
|---|---|---|---|---|
| 状態の遠距離判定（他プレイヤーから） | 低（色のみ） | **高**（位置＋形） | 中（マーカー位置が小さい） | 低（数字が小さい） |
| 操作量 | 14.4 | 14.4 | **8.2** | 約8 |
| 誤操作（レーン違い、置き忘れ） | 高 | **低**（形状ロック） | 中（進め忘れ） | 中 |
| 4個置いた時の占有面積 | 小 | 中（3レーン×4穴：60×64mm） | 小（カード上の1セル） | 小 |
| 色覚多様性 | 低 | **高** | 高 | 高 |
| カードの数値を隠さない | ○ | ○ | △（1セルを隠す） | ○ |
| 誤って動かすリスク | 高 | **低**（穴に嵌まる） | 中 | 低 |
| ルールとの整合（RULE §6.2） | ○ | **○**（同一） | △（状態機械は同値。**ルール本文の書き換えが必要**：GRC-04） | △（同上） |
| デジタルへの転用 | 中 | 高（同じ状態を投影） | **高**（帯がそのままUIになる） | 低 |

**RECOMMENDATION（REC-02）**：物理は **T2**。理由：ルール（白・緑・赤の3種）と整合し、状態を位置＋形で読める。デジタル版は、T2と同じ状態を投影する（トークンを「ピップ」として表示。§17）。
**ALTERNATIVE（ALT-01）**：T3は、操作量を約4割減らし、時間軸の語彙とも一致する有力案。ただし、状態機械の記述（RULE §6.2、§6.3）を書き換える必要があり、`GAME RULE CHANGE CANDIDATE`（**GRC-04**）として別枠に置く（ゲームの結果は変わらない見込みだが、ルール本文の変更を伴う）。

## 8.4 推奨するトークンの仕様（T2）

| トークン | 形状 | 寸法（ASM-03） | 色（ASM-09） | アイコン | 置き場（レーン） | 穴の形 |
|---|---|---|---|---|---|---|
| 待機（白） | **円盤**（○） | Ø14 mm、厚さ2 mm | 白（縁は濃色） | **砂時計** | 待レーン（左） | 円形の浅い穴 |
| 収穫（緑） | **六角柱**（⬡） | Ø14 mm、高さ10 mm | 緑（青緑寄り） | **穂** | 収レーン（**中央**） | 六角形の穴 |
| クールダウン（赤） | **菱形**（◇） | 14 mm、厚さ2 mm | 朱赤＋斜線模様 | **月** | 休レーン（右） | 菱形の穴 |

**レーンの仕様**

| 項目 | 内容 |
|---|---|
| 構成 | 3本の縦レーンを、左から **待｜収｜休**（時間の順）に並べる。**収穫は常に中央** |
| 穴の数 | 1レーンあたり**4穴**（RULE：最大4個。W、H、C の最大がすべて4） |
| 寸法（ASSUMPTION） | 列間20 mm、縦ピッチ16 mm。穴の領域は 60（幅）× 64（高さ）mm（ヘッダー8 mmは別。レーンブロック全体で 60×72 mm） |
| 埋め方 | カードに近い穴から順に埋め、取り除くときは遠い穴から取る（残りの個数が連続して見える） |
| ヘッダー | 各レーンの上端に、ラベル（待／収／休）とアイコンを常設。レーンの背景は、待＝無地、収＝薄い塗り、休＝斜線 |
| 位置（プレイヤーボード上） | カードの**遠い側**（テーブル中央側）に置く（**REC-01**）。向かい側のプレイヤーから近く、所有者の手には、カード1枚分（約90 mm）遠い。**ALTERNATIVE（ALT-08）**：手前側に置く（所有者の操作は楽になるが、他者からは遠くなる） |

**中央レーン＝収穫の理由**：180°回転しても中央のままなので、向かい側から見ても、収穫の位置が同じ。待（左）と休（右）は入れ替わって見えるが、形（○と◇）、色、アイコンで区別できる。

## 8.5 誤操作の防止

| リスク | 対策 |
|---|---|
| 誤ったレーンに置く | 穴の形が、トークンの形にしか合わない（ポカヨケ） |
| トークンの更新の取り忘れ | 農地を**左から順に**更新する手順を印刷（§6.6）。不変条件（同時に非0なのは1レーン）の確認で、一部の誤りは検出できる |
| 緑を置いたばかりの農地の誤集計 | 「計数（凍結）→受取→更新」の順序を印刷（**REC-07**） |
| 卓上でトークンが動く | 穴に嵌める。デジタルではこのリスクはない |
| 白の個数の意味の誤解 | ボードに読取規則を印刷（§6.3、§10.6） |

## 8.6 アクセシビリティ

**REQUIREMENT**：状態は、色だけに依存しない。次の**冗長な符号**を、すべて持たせる：レーン位置、形状、アイコン、穴の形。
**RECOMMENDATION**：Figma上で、グレースケール表示、および1型・2型・3型の色覚シミュレーションを行い、3状態を区別できることを確認する（§22）。

## 8.7 トークンの数

**OPEN QUESTION（OQ-08）**：RULE §2は「各約40個（プロトタイプ用の目安）」としている。1プレイヤーが、1色を同時に置く最大数は20個（5スロット×4）。通常のプレイでは、1プレイヤーあたり1色で3〜4個程度と推定される（**ASSUMPTION**）ため、5人卓でも各約17個で足りる見込みだが、最悪の場合（全スロットが同じ状態）は100個近くになる。物理版では、この不足の可能性を、試作で確認する必要がある。ルールの変更ではない。

---

# 9. Event Card and Event Timeline

## 9.1 ルールの再掲

**RULE**（§4、§9）：

- イベント列は最大3枚。左端が今ラウンドのイベントで、右の2枚は次のラウンド、次の次のラウンド。
- 発動中置き場に、直前ラウンドの左端のカードが最大1枚。
- **有効なイベント**は、今の左端＋発動中置き場のカード（最大1枚）の、最大2枚。
- ラウンド開始時（2ラウンド目以降）の更新手順：（1）効果を2ラウンド終えたカードを、発動中置き場からイベント捨て札へ。（2）直前のラウンドの左端を発動中置き場へ移し、列を左に詰める。（3）上級で、山札が空で累計が上限未満なら、捨て札を混ぜて山札に戻す。（4）山札があり累計が上限未満なら、右端に1枚補充。
- 上限（基本10枚、上級18枚）に達した後は補充しない。終盤は列が短くなる（基本はR9〜10、上級はR17〜18）。
- イベントの捨て札は公開（表向き）。山札は枚数のみ公開。

## 9.2 構造上の発見：「コンベア」

更新の手順は、**全カードが毎ラウンド1つずつ左へ進む**と言い換えられる：

```
山札 ─▶ 次の次 ─▶ 次 ─▶ 今 ─▶ 継続中 ─▶ 捨て札
(右端に補充)  (予告)  (予告)  (有効)  (有効)   (2ラウンド終えたら)
```

**RECOMMENDATION（REC-08）**：イベント列・発動中置き場・イベント山札・イベント捨て札を、**時間軸に沿った1本のレール（EventRail）**に、左から右へ次の順で並べる。

```
 過去 ◀───────────────────────────────────────────────▶ 未来
 [捨て札] │ [継続中] [今] │ [次] [次の次] │ [山札]
 (公開)     └── 有効（最大2枚）──┘   └── 予告 ──┘   (枚数のみ)
```

| ゾーン | 容量 | 有効？ | 内容 | 更新時の動き |
|---|---|---|---|---|
| 捨て札 | 制限なし | — | 効果を終えたカード（公開） | 継続中のカードがここへ |
| 継続中（発動中置き場） | 最大1 | **有効** | 直前ラウンドの「今」だったカード | 「今」のカードがここへ |
| 今 | 1 | **有効** | 今ラウンドのイベント | 「次」のカードがここへ |
| 次 | 1 | 予告 | 次ラウンドのイベント | 「次の次」のカードがここへ |
| 次の次 | 1 | 予告 | 次の次のラウンドのイベント | 山札から補充 |
| 山札 | — | — | 枚数のみ公開 | 上限に達していれば補充しない |

**RECOMMENDATION（REC-09）**：**有効**（継続中＋今）と**予告**（次・次の次）を、ブラケットで分ける。有効の帯は太い実線の枠と「今ラウンドの収入に効く（最大2枚）」の見出し、予告の帯は破線の枠と「予告（まだ効かない）」の見出しで表す。**REQUIREMENT**：イベント列の左端だけが「今」だが、継続中のカードも有効であることを、位置（有効ブラケットの中）で示す。

**時間軸を表す語**（英語表記は要件ではない。**ASM-08**）：継続中／今ラウンド／次ラウンド／次の次。デジタルでは、各ゾーンにラウンド番号（R5、R6、R7…）を併記する。物理は、印刷を「前／今／＋1／＋2」とする。

## 9.3 更新の物理操作（1ラウンドの動き）

| 順 | 操作 | カード移動 |
|---|---|---|
| 1 | 継続中のカードを捨て札へ | 1枚 |
| 2 | 今のカードを継続中へ | 1枚 |
| 3 | 次のカードを今へ、次の次のカードを次へ（左へ1つずつ） | 2枚 |
| 4 | 山札の1枚を次の次へ（条件を満たすとき） | 1枚 |

- 最大4枚の移動。**進行係が行う**（**OQ-03**）。
- 上級のみ：山札が空で累計が上限未満のとき、捨て札を混ぜて山札に戻す（手順3）。**RECOMMENDATION**：EventDeckが0枚で、累計が上限未満のとき、**再シャッフルのリマインダー**を表示する。特定のラウンド番号（R15）をUIに固定しない（条件で決まるため）。

## 9.4 イベントカードの設計

**RECOMMENDATION（REC-10）**

| 領域 | 内容 |
|---|---|
| 対象作物 | グリフ（≥18 mm）と作物名。作物カードと**同じグリフ**を使う |
| 数値 | 符号付きの大数字（例：＋4、−2）。高さ ≥ 10 mm |
| 極性 | **▲（増）／▼（減）**の形。増は塗り、減は輪郭。色は副次（暖色／寒色） |
| 大きさ | シェブロンの数：±2＝1個、±4＝2個 |
| 持続の注記 | 「2R」を、2つのセル（今／翌）で表す。カードの位置（ゾーン）が、どちらのラウンドかを示す |
| 向き | 数値とグリフは、上下に180°回転した複製を置く（中央のカードは、向かい側からも読める。P2） |
| 寸法 | 45×63 mm（**ASM-02**）。作物カード（63×88）と、明確に別サイズにする（山札の取り違え防止。**OQ-10**） |

**ALTERNATIVE（ALT-12）**：イベントカードを作物カードと同サイズにする（製造の単純化）。ただし、山札の取り違えのリスクが上がる。

## 9.5 有効なイベントの、作物別の集約

**RECOMMENDATION**：作物パネル（§11.3）の**ModifierWell**に、その作物に対する、現在有効なイベント補正を集約する。

| 表示 | 内容 |
|---|---|
| バッジ（最大2個） | 「継続中」由来と「今」由来。対象でないものは空の枠 |
| 合計 | 例：＋2＋（−2）＝0 |

## 9.6 終盤の列の短縮

**REQUIREMENT**（RULE §4、§9.1）：終盤は列が短くなる。**RECOMMENDATION**：EventRailのゾーンは、常に同じ数（6つ）を保ち、補充されないゾーンは**斜線の「なし」表示**にする（列が消えたように見えて、混乱するのを避ける）。EventTimelineの脇に、**残りのイベント枚数**（累計／上限）を小さく表示する（T2）。

## 9.7 誤解のリスクと対策

| リスク | 対策 |
|---|---|
| 「継続中」を有効でないと思う | 有効ブラケットの中に置く（REC-09） |
| 「次」を今ラウンドに効くと思う | 予告は破線・淡色・「まだ効かない」の見出し |
| 更新（左へ1つ進める）を忘れる | PhaseRailの手順1に「イベント更新」を常設（§16）。デジタルは自動 |
| 作物とイベントの捨て札の混同 | 山札・捨て札のトレイを、形・色・アイコンで別にする（§11.5） |
| 継続中のカードを、期間終了後も残す | 継続中が「最大1枚」であることを、ゾーンの穴（1枚分）で示す |

---

# 10. Player Board

## 10.1 構成要素

| 要素 | 内容 |
|---|---|
| FarmSlot ×5 | 農地スロット。各スロットに、レーンブロック（待｜収｜休）、カード領域、フッター（スロット番号と取得コスト） |
| SlotCover ×5 | 未取得スロットを覆うタイル（取得コストを表示）。取得時に取り除く |
| CoinArea | 所持コインの置き場（額面別の穴） |
| PlayerIdentity | プレイヤーの形＋番号＋色＋名前 |
| 読取規則の印刷 | トークンの読み方（§10.6） |
| StartPlayerMarker の置き場 | スタートプレイヤーが置く小さな窪み |

## 10.2 FarmSlotの構造（1スロット）

**RECOMMENDATION（REC-01）**：所有者から見て、**奥（テーブル中央側）から手前**に向かって、次の順で並べる。

```
   （テーブル中央側・向かい側のプレイヤーに近い）
   ┌───────────────────────┐
   │  待   収   休          │  ← レーンヘッダー（ラベル＋アイコン）
   │  ○    ⬡    ◇           │  ← 4穴 × 3列（60×64 mm）
   │  ○    ⬡    ◇           │
   │  ○    ⬡    ◇           │
   │  ○    ⬡    ◇           │
   ├───────────────────────┤
   │                       │
   │   カード領域            │  ← 63×88 mm（縦向き）。前作カードはここに残る
   │   （SlotCoverで覆う）    │
   │                       │
   ├───────────────────────┤
   │ ③  取得コスト 2         │  ← フッター（常時印刷。自分向き）
   └───────────────────────┘
   （所有者側・手前）
```

| 項目 | 寸法（ASSUMPTION、mm） |
|---|---|
| スロットの幅 | 66（カード63＋余裕1.5×2） |
| レーンブロック | 60×72（ヘッダー8＋穴4×16） |
| カード領域 | 63×88 |
| フッター | 66×12 |
| スロットの高さ（余白込み） | 約180 |
| スロットのピッチ | 70（隙間4） |

**ALTERNATIVE（ALT-08）**：レーンブロックをカードの手前側に置く。所有者のトークン操作は楽になるが、向かい側のプレイヤーからは遠くなる（G1に反する）。

## 10.3 未取得スロットと「次の農地」

**RULE**（§5）：農地取得コストは、すでに持っている農地の枚数で決まる。1枚目→2枚目→…の順に、次の未取得枠が対象になる。コストは 0/1/2/3/4。

**RECOMMENDATION（REC-04、REC-05）**

| 項目 | 内容 |
|---|---|
| SlotCover | 未取得の各スロットに、カバータイルを置く。表面に、**取得コストの大数字**と「未取得」のラベル |
| 「次」の明示 | 取得は左のスロットから順なので、**最も左に残っているカバー**が「次に取得する農地」。取得時にそのカバーを取り除く |
| 取得後 | スロットのフッターに、取得コストが**常時印刷**されているため、参照できる（Reference） |
| 共通ボードに置くか | **置かない**。各プレイヤーボードに印刷する（判断時に自分のボードだけを見れば済む。**REC-05**） |
| 取得の制約 | カバーが、未取得スロットにカードを置く誤りを、物理的に防ぐ |

## 10.4 EconomyStrip（経済の比較）

**REQUIREMENT**：栽培コスト・農地取得コスト・所持コインを、容易に比較できる。

**RECOMMENDATION**：プレイヤーボードの**手前の帯**に、CoinAreaを置き、左端のSlotCover（次の取得コスト）と、視野に収まる位置関係にする。栽培コストは場札（中央）にあるので、「場札の栽培コスト」と「所持コイン」と「次の取得コスト」の3つを、**2回の視線移動**（場札 → 自分のボードの手前の帯）で比較できる。デジタルは、選択中の場札に対して、支払い合計を表示する（表示補助）。

## 10.5 CoinArea

**RULE**（§10）：所持コインは公開情報。

**REQUIREMENT**：公開情報なのに、物理的に読み取りにくい状態を避ける。

| 案 | 内容 | 評価 |
|---|---|---|
| C1（RULEの素朴な実装） | 手元にコインを積むだけ | 他プレイヤーの総額が読めない。手番順（コイン昇順）の判定に手間 |
| **C2（推奨）** | 手元のコイン＋**共通のCoinLedger**（各プレイヤーの総額を、ラウンド末に公開更新） | 大小比較と手番順の判定が容易。更新の操作が、プレイヤーごとに1回/ラウンド増える |
| C3（ALT-06） | コインを廃止し、カウンタ（台帳）のみ | 操作量は減るが、コインの実体がなくなる（**GRC-03**） |

**RECOMMENDATION（REC-15）**：C2。台帳の更新タイミングは、**ラウンド末**（収入フェーズの手順4。§6.6）。**OPEN QUESTION（OQ-02）**：手番中の支払い（植え付け）を、台帳にも即時反映するか。デジタルは、常に最新を表示する。
**額面**：RULE §2のとおり、額面1/5/10（上級は50も）。CoinAreaは、額面ごとの窪みに分ける。

## 10.5b プレイヤー識別

**RECOMMENDATION（REC-18）**：プレイヤーは、**形（●■▲◆★）＋番号（1〜5）＋色**で識別する。色は、ボード枠・コインマーカー・StartPlayerMarker・台帳の行に**限定**する（カードやトークンには使わない）。CPU席も、通常の席と同じ扱い（RULE §1）。

## 10.6 ボードに印刷する読取規則

**RECOMMENDATION**：プレイヤーボードの片隅に、次の内容を短く印刷する（デジタルはツールチップ）。

```
【状態の読み方】
  待：白が n 個 →  n ラウンド後の終了時に、初めての収入
  収：緑が n 個 →  今ラウンドの終了時を含めて n 回の収入
  休：赤が n 個 →  n ラウンド後から、同じ作物もOK（別の作物は今すぐOK）
  待・収が空 → 空き（別の作物を植えられる）／休が空 → 同じ作物もOK
【更新（左のスロットから）】
  白を1個取る（0になったら緑を H 個・収入なし） ／ 緑を1個取って収入（0になったら赤を C 個）／ 赤を1個取る
```

## 10.7 カード交換とトークン操作の操作性

| 操作 | 設計 |
|---|---|
| 植え付け（カード交換） | 前作カードを作物の捨て札へ → 赤を取り除く → カードを置く → 白を W 個 → 場札を補充（RULE §5）。カード領域には、指を入れる**切り欠き**を設ける |
| 毎ラウンドのトークン更新 | 農地ごとに1〜2個の移動。**左から順に**。遠側のレーンに、腕を伸ばす（最大約90 mm）。この距離は、**OPEN QUESTION（OQ-18）**として、試作で確認する |
| 緑・赤の一括配置（遷移） | 待レーンの最後の1個を取り除いたら、収レーンに H 個を置く（隣のレーンなので、手が動く距離は短い） |

## 10.8 他プレイヤーからの視認性

- 向かい側：レーンブロックが近く、**中央の収レーンの緑**が見える。カードのグリフが見える。
- 横側：レーンブロックの3列が90°回転して見える。**形（○⬡◇）と色**で識別する。中央（収）は、回転しても真ん中。
- カードの数値は、他プレイヤーからは読めない（**P3**）。必要なら、共通パネルを見る。

## 10.9 依頼された検討項目への対応表

| 項目 | 設計 |
|---|---|
| 5農地の配置 | 横一列、左から①〜⑤ |
| 未取得と取得済みの区別 | SlotCoverの有無 |
| 次に取得する農地 | 最も左のSlotCover |
| 1〜5枚目の取得コスト | 各スロットのフッターに常時印刷。カバーにも大数字 |
| 作物カードの置き方 | 縦向き、カード領域。前作カードはそのまま残す |
| トークンの置き方 | カードの外、遠側の3列レーン（待｜収｜休） |
| 前作カードの扱い | 残す（RULE §6.3）。物理の追加フラグはなし（REC-03） |
| 空き農地の視認性 | 待・収レーンが空 |
| 連作制限の視認性 | 休レーンの赤の有無 |
| 所持コインとの位置関係 | 手前の帯（EconomyStrip） |
| 他者からの視認性 | 遠側レーン、グリフ、台帳 |
| カード交換時の操作性 | 切り欠き、隣接した捨て札トレイ |
| トークン更新の操作性 | 左から順、穴の形状ロック、遠側レーンの届きやすさは**OQ-18** |
| 取得コストを共通ボードに置くか | **各プレイヤーボードに印刷**（共通ボードには置かない） |

---

# 11. Shared Table Area

## 11.1 共通エリアの構成要素

**RULE**（§2、§8）：共通エリアには、作物の場札、作物山札・捨て札、イベント列、発動中置き場、イベント山札・捨て札、（現在ラウンド情報）、得点表がある。

| 構成要素 | 触る頻度 | 見る頻度 | 触る人 | 備考 |
|---|---|---|---|---|
| CropMarket | 高（毎手番） | 高 | 手番プレイヤー | 全員が頻繁に見る |
| SeasonCropPanel ×4 | 低〜中（集計ペグを使う場合） | **高** | 進行係（ペグ）／全員（読取） | D-02 |
| EventRail（列＋発動中＋山札＋捨て札） | 中（毎ラウンドの更新） | 高 | 進行係 | REC-08 |
| RoundIndicator／PhaseRail／SeasonIndicator | 低（1回/ラウンド） | 中 | 進行係 | §16 |
| TurnOrderTrack | 低（1回/ラウンド） | 中 | 進行係 | REC-16 |
| CoinLedger | 中（1回/ラウンド/人） | 中 | 各プレイヤー | REC-15 |
| CropDeck | 中（購入のたびに補充） | 低 | 手番プレイヤー | 端に置ける |
| CropDiscard | 中（植え替えのたび） | 低 | 手番プレイヤー | 端に置ける |
| EventDeck／EventDiscard | 低（1回/ラウンド） | 低 | 進行係 | 端に置ける |
| ScoreBoard | 低（シーズンに1回） | 低 | 進行係 | 隅 |
| 除外ポケット（基本のみ） | 準備時のみ | なし | — | 隅 |
| Bank（コイン・トークンの共通置き場） | 高（収入・支払い） | 中 | 全員 | 全員が届く位置 |

## 11.2 配置の方針

| 問い | 回答 |
|---|---|
| どれを中央に置くべきか | **CropMarket**（全員が触る）と、**SeasonCropPanel**（全員が頻繁に見る）、**EventRail**（全員が見る） |
| どれを端に置けるか | 山札・捨て札（2組）、除外ポケット、得点表 |
| 全員が頻繁に見るものは何か | EventRail、SeasonCropPanel、RoundIndicator、CropMarket |
| 全員が触るものは何か | CropMarket、Bank、CropDeck・CropDiscard（手番中）、CoinLedger（自分の行） |
| 進行係だけが触ればよいものは何か | EventRail、RoundIndicator、TurnOrderTrack、HarvestTally（ペグ）、ScoreBoard |
| 山札と捨て札を混ぜないか | 形・色・アイコンで、トレイを分離（§11.5） |
| 作物とイベントの捨て札を混同しないか | サイズの違い（63×88 と 45×63）と、トレイの形の違い |

**OPEN QUESTION（OQ-03）**：「進行係」は、RULE上の役割ではない。ラウンドの共通処理の負担を、誰が持つか（固定、スタートプレイヤー、ローテーション）は、人間が決める。

## 11.3 SeasonCropPanel（作物パネル）

**RECOMMENDATION（REC-06）**：今シーズンの4作物のそれぞれに、**63×130 mm程度のパネル**を共通エリアに置く。束（作物カード10枚＋イベントカード4枚）に、パネル1枚を付属させる。

| 領域 | 内容 |
|---|---|
| Identity | グリフ、作物名、頭字、タイプ帯（大きく。L1で読める） |
| Static values | 栽培コスト、待機W、収穫H、基本収入、最低保証、連作CD（**カードと同じ値の再掲**。作物カードが他者の農地にあっても、共通パネルで確認できる） |
| **IncomeLadder（収入はしご）** | 収穫中の農地の総数 N（自分を含む）ごとの、1農地あたりの収入（イベント補正前）：N＝1〜8。値は `max(最低保証, 基本収入 −(N−1))`（RULE §7から算出）。例：ラディッシュ（7、3）：N＝1〜5＝7,6,5,4,3。下に「補正（±）を加える」の注記 |
| **HarvestTally（収穫集計）** | 「今ラウンドに収穫中の農地の数」を、ペグで示すトラック（0〜10）。**進行係が、収入フェーズの手順1（計数）で設定する**（D-02）。**非公式（補助）**であり、真実は各農地のトークン |
| **ModifierWell（イベント補正）** | 現在有効なイベントの、この作物に対する補正の合計（§9.5） |

**REQUIREMENT**：HarvestTallyとIncomeLadderは、**RULE §7の式を、そのまま表示するもの**であり、新しい判断（推奨や結論）を出さない。

**ALTERNATIVE**：（a）ペグなし（ラダーとModifierWellのみ。進行係の操作なし。D-02のB）。（b）パネルなし（各自が作物カードを見る。D-02のC）。

**ALT-05（任意モジュール）**：**収穫予定ストリップ**。パネル上に、現在〜現在＋5の6セルを並べ、確定している収穫中の農地数を進行係が記入する。ケースCの負荷を減らすが、操作が増えるため任意。

## 11.4 CropMarket

| 項目 | 内容 |
|---|---|
| 構成 | 3枚（5人は4枚）の場札の枠。4枚目の枠は、5人以外ではグレーアウトの印刷 |
| 補充 | 購入した枠に、山札から補充（RULE §8）。枠が固定なので、補充の位置が明確 |
| 向き | 数値（栽培コスト、基本収入など）を、上下に180°回転した複製にする（**REC-20**）。向かい側のプレイヤーも読める |
| 寸法 | 4枚で約264 mm（63×4＋隙間4×3） |

## 11.5 山札・捨て札のトレイ

**REQUIREMENT**：山札と捨て札を誤って混ぜない。作物とイベントの捨て札を混同しない。

**RECOMMENDATION（REC-17）**

| トレイ | 形 | 縁の色・模様 | アイコン | 公開性 |
|---|---|---|---|---|
| 作物山札 | 63×88の枠（角丸） | 畝の模様 | 種 | **枚数のみ**（**OQ-06**） |
| 作物捨て札 | 63×88の**浅い皿（ずらして重ねて中身が見える）** | 畝の模様（淡） | 種＋皿 | 公開 |
| イベント山札 | 45×63の枠 | 雲の模様 | 天候 | 枚数のみ |
| イベント捨て札 | 45×63の浅い皿 | 雲の模様（淡） | 天候＋皿 | 公開 |

- **OPEN QUESTION（OQ-06）**：「枚数のみ公開」を物理で実現する方法（山札を数える操作は、順序や中身を見ずにできる）。物理版では、トレイの側面に**枚数の目安の目盛り**を付けることを提案する（RECOMMENDATION）。デジタルは、正確な枚数を表示する。
- 作物とイベントは、サイズも異なるため、物理的に取り違えにくい（**ASM-02**）。

## 11.6 ScoreBoard

RULE §3、§11：シーズンごとの終了時コインと合計を記録する。

**RECOMMENDATION**：行＝プレイヤー（形＋番号＋色）、列＝シーズン＋合計の表。物理は、テーブルの隅に置く（**ASM-06**：公開情報として扱う）。デジタルは、常時ヘッダーに折りたたみ表示。

## 11.7 共通エリアの配置例（1組・約720×440 mm。ASSUMPTION）

```
   ┌────────────────────────────────────────────────────────────────────┐
   │ EventRail 320×100                    │ RoundIndicator 270×40           │
   │ [捨][継][今][次][次の次][山]          │ PhaseRail 360×60                │
   ├────────────────────────────────────────────────────────────────────┤
   │ SeasonCropPanel ×4（63×130、ピッチ70）  │ CoinLedger 200×130 │ TurnOrder 200×60 ＋ SeasonIndicator 200×40│
   ├────────────────────────────────────────────────────────────────────┤
   │ CropMarket 264×88                     │ Bank（コイン・トークン）300×88   │
   ├────────────────────────────────────────────────────────────────────┤
   │ 作物山札／捨て札  イベント山札／捨て札  除外ポケット │ ScoreBoard 240×80   │
   └────────────────────────────────────────────────────────────────────┘
```

- 中央の3段（EventRail＋Round、Panel＋Ledger、Market＋Bank）が、全員が頻繁に見る領域。
- 下段の山札・捨て札と得点表は、端（進行係の側）に置く。

---

# 12. 3-Player Table Layout

## 12.0 全レイアウト共通の前提

| 項目 | 内容（ASSUMPTION） |
|---|---|
| プレイヤーボード | 380×225 mm（5農地の横幅：5×66＋隙間＋余白＝380） |
| 共通エリア（1組） | 約720×440 mm（§11.7） |
| 標準卓 | **1200×950 mm**（ASM-05）。3〜5人で同じ卓を使う（テーブル面積の増減で、レイアウトを変える） |
| カードの向き | すべてのカードは、**所有者向き**。他プレイヤーからの可読性は、回転に強い記号（グリフ、形、位置）で確保する（**P2**） |
| 座席の順 | 時計回り（RULE §3.1）。スタートプレイヤーは、シーズンごとに1つずつ移る |

**課題**：「カードをすべて自分向きにすると、他プレイヤーから読めない」。
**対応（RECOMMENDATION）**：（1）識別は、グリフと頭字（回転に強い）。（2）状態は、レーン位置（中央＝収穫）と形。（3）数値は、共通の作物パネルと場札に**両向き**で印刷（REC-20）。（4）コインは台帳。（5）自分向きに揃った数値は、所有者だけが読めばよい（P3）。

## 12.1 レイアウト（トップビュー概念図。最終デザインではない）

```
                 北（N）
        ┌─────────────┬─────────────┐
        │ P2（北西）      │ P3（北東）       │   ← 各 380×225
        └──────┬──────┴──────┬──────┘
                │   共通エリア（720×440）   │
   W（空き）      │ EventRail / Panels /   │      E（空き）
   Bank・得点表 │ Market / Ledger         │      進行係の作業台
                └──────────┬──────────┘
                        ┌───────┐
                        │ P1（南）│  ← 380×225
                        └───────┘
                 南（S）
```

- P1（南）は、共通エリアに近い。P2・P3（北）は、2つの板を横に並べる（幅 380×2＋隙間40＝800 mm）。
- E・Wの端は、**Bank、山札・捨て札、得点表、進行係の作業台**に使う。共通エリアを、720×440より小さくできる。

## 12.2 分析

| 項目 | 3人 |
|---|---|
| 共通エリアの大きさ | 720×440。E・W端に山札などを逃がせるので、実質、中央は約540×440で足りる |
| 各プレイヤーエリア | 380×225。P2・P3は、隣の板と40 mm離す（肘の余裕） |
| 最大5農地の横幅 | 380 mm（板の幅）。標準卓の幅1200 mmに対して、余裕がある |
| 他プレイヤー農地の視認性 | P1→P2・P3：北側の2枚が、正面（奥）に並ぶ。レーンは遠側（中央側）にあるため、近い。カードは逆さに見える → グリフで識別。P2⇄P3：横に隣接。同じ向きなので、カードもそのまま読める（距離は約400 mm） |
| イベント列の視認性 | 中央にあり、全員から約350〜450 mm。P2・P3からは、上下が逆に見える → 数値の両向き印刷（REC-20） |
| 場札への到達距離 | P1：約250〜300 mm。P2・P3：約250〜300 mm（対称的） |
| 山札への到達距離 | 下段（進行係の側）に置くと、進行係以外は約500〜600 mm。手番プレイヤーが補充するとき、身を乗り出す必要がある → **OQ-19** |
| コイン置き場 | 各ボードの手前の帯。Bankは、E・W端 |
| 得点表 | W端（または隅） |
| テーブル面積 | 1200×950（標準卓）。**代替**：約1000×850 に縮小可（3人のとき） |
| 混雑 | 低い |
| カードの向き | 所有者向き。P2・P3の共通エリアの見え方は、上下逆 |

---

# 13. 4-Player Table Layout

## 13.1 レイアウト

```
                 北（N）
                ┌───────┐
                │  P3    │  380×225
                └───┬───┘
   ┌───────┐   ┌────┴─────────────────┐   ┌───────┐
   │  P2     │   │  共通エリア（720×440）   │   │  P4     │
   │（西）    │   │ EventRail / Panels /     │   │（東）    │
   │ 90°回転   │   │ Market / Ledger          │   │ 90°回転   │
   └───────┘   └────┬─────────────────┘   └───────┘
                ┌───┴───┐
                │  P1    │  380×225
                └───────┘
                 南（S）
```

- 4辺に1人ずつ。西・東の板は、90°回転して置く（板の長辺380 mmが、卓の奥行き方向）。
- 標準卓 1200×950：共通エリアの奥行き（950−225×2＝500）に、720×440が収まる。共通エリアの幅は、1200−225×2＝750 mm（720に対して、余裕30 mm）。

## 13.2 分析

| 項目 | 4人 |
|---|---|
| 共通エリアの大きさ | 720×440（750×500の余白内に収まる。余裕：幅30、奥行き60） |
| 各プレイヤーエリア | 380×225。西・東は、90°回転（板の長辺が奥行き方向） |
| 最大5農地の横幅 | 380 mm。西・東の板は、長辺380 mmが、卓の奥行き950 mmの内側に収まる |
| 他プレイヤー農地の視認性 | **向かい側**（P1⇄P3、P2⇄P4）：レーンが近く、L1で読める。カードは逆さ → グリフ。**横側**（P1→P2・P4）：板が90°回転して見える。レーンの3列は横向きに見える。**中央レーン＝収穫**は、回転しても中央 |
| イベント列の視認性 | 中央にあり、全員から約350〜450 mm。P2・P4は、90°回転して見える → 数値の両向き（上下）印刷は、P2・P4には効かない（**OQ-15**）。グリフと符号（▲▼）は回転に強い |
| 場札への到達距離 | 全員、約250〜320 mm（対称） |
| 山札への到達距離 | 下段（進行係の側）。他の3人は、約500〜650 mm。**OQ-19** |
| コイン置き場 | 各ボードの手前。Bankは、共通エリアの右端 |
| 得点表 | 共通エリアの隅 |
| テーブル面積 | 1200×950 |
| 混雑 | 中。P1とP2・P4の板の角の間隔は約60 mm |
| カードの向き | 所有者向き。西・東のプレイヤーは、南・北の板を、90°回転した状態で見る |

---

# 14. 5-Player Table Layout

## 14.1 レイアウト

```
                 北（N）
   ┌──────────┬──────────┐
   │ P3（北西）    │ P4（北東）    │   ← 各 380×225
   └────┬─────┴─────┬────┘
   Bank・   │ 共通エリア（720×440）  │   ┌───────┐
   得点表     │ EventRail / Panels /   │   │  P5     │
   （西端）   │ Market（4枚）/ Ledger   │   │（東）    │
              └────┬─────┬────┘   │ 90°回転   │
                    ┌──┴───┴──┐        └───────┘
                    │ P1（南西） │ P2（南東）│
                    └─────────┘
                 南（S）
```

- 南に2人、北に2人、東に1人。西端は、**Bank、山札・捨て札、得点表、進行係の作業台**を置く。
- 南・北の2枚の板：幅 380×2＋隙間40＝800 mm（標準卓の幅1200 mmに収まる）。
- 東の板：P5。南・北の板のy範囲（0〜225、725〜950）と重ならない（東の板は、y 285〜665）。角の間隔は約60 mm。

## 14.2 分析

| 項目 | 5人 |
|---|---|
| 共通エリアの大きさ | 720×440。**場札が4枚になる**（RULE §3.2）ため、Marketは4枠（264 mm）。山札などを西端に逃がすので、中央は圧迫しない |
| 各プレイヤーエリア | 380×225。5人分で、周長の使用は約1,900 mm |
| 最大5農地の横幅 | 380 mm。南（P1・P2）は隣と40 mm離す |
| 他プレイヤー農地の視認性 | **最も課題が大きい**。他の4人の板の農地：20スロット。同じ辺の隣（P1⇄P2）は同じ向きだが、距離が約420 mm。北のP3・P4は逆さに見える。東のP5は90°回転。**HarvestTally（作物パネル）**の価値が最大（REC-06、D-02） |
| イベント列の視認性 | 中央だが、5人では距離が増える（約400〜500 mm）。イベントの符号（▲▼）とグリフは、L0で読める |
| 場札への到達距離 | 約250〜350 mm。P5（東）は、板が短辺側にあり、共通エリアまで約300 mm |
| 山札への到達距離 | 西端。**東のP5からは約1.0 m**。**OQ-19**：手番プレイヤーが山札を補充する操作は、遠いプレイヤーには負担 |
| コイン置き場 | 各ボードの手前。Bankは、西端と共通エリアの両方に |
| 得点表 | 西端 |
| テーブル面積 | 1200×950（4人と同じ）。**5人時の混雑を、卓の追加面積なしで解消するには、西端に共通の備品を逃がす** |
| 混雑 | **高い**（5板＋共通エリア）。ただし、卓は4人と同じ大きさで足りる |
| カードの向き | 所有者向き。北のP3・P4は、南のP1・P2の板を、逆さに見る |

## 14.3 3・4・5人の比較

| 項目 | 3人 | 4人 | 5人 |
|---|---|---|---|
| 卓 | 1200×950（縮小可：約1000×850） | 1200×950 | 1200×950 |
| 板の配置 | 北2・南1 | 4辺各1 | 南2・北2・東1 |
| 共通エリア | 720×440（余裕あり） | 720×440（余裕：幅30、奥行き60） | 720×440＋西端の備品置き場 |
| 場札 | 3枚 | 3枚 | **4枚** |
| 他者の農地 | 10スロット | 15スロット | **20スロット** |
| 収穫の集計の必要度 | 中 | 高 | **最高** |
| 混雑 | 低 | 中 | 高 |
| 主な課題 | 山札の到達 | 東西のプレイヤーからの、イベント列の見え方 | 他者農地の走査量、山札の到達 |

---

# 15. Physical Ergonomics

## 15.1 毎ラウンド発生する操作の評価

| # | 操作 | 頻度 | 1回の操作回数 | 誤操作可能性 | 面倒さ | UI改善可能性 | 種別 |
|---|---|---|---|---|---|---|---|
| 1 | トークンを減らす（待・収・休） | 稼働中の農地ごとに1回/ラウンド（1人3〜5回） | トークン1個 | 中（取り忘れ） | 中 | 形状ロック、左から順に（REC-02） | **ルール必要** |
| 2 | 白→緑への移行 | 植え付けごとに1回 | 白を取る＋緑を H 個置く（1〜4個） | 中（個数の間違い） | 中 | 隣接レーン（REC-01） | **ルール必要** |
| 3 | 緑→赤への移行 | 植え付けごとに1回 | 緑を取る＋赤を C 個置く（0〜4個） | 中 | 中 | 同上 | **ルール必要** |
| 4 | 赤を減らす | 休耕中の農地ごとに1回/ラウンド | 赤1個 | 低 | 低 | 同上 | **ルール必要** |
| 5 | 作物カードを入れ替える（植える） | 手番ごと（行動時） | 前作を捨てる、赤を取る、カードを置く、白を置く（3〜7動作） | 低 | 中 | 切り欠き、捨て札を隣接（REC-01） | **ルール必要** |
| 6 | 場札を補充する | 購入ごとに1回 | カード1枚 | 低 | 低 | 固定の枠（§11.4） | **ルール必要** |
| 7 | イベントカードを移動する | 1回/ラウンド | カード最大4枚 | 中（順序の間違い） | 中 | レールの窪み（REC-08） | **ルール必要** |
| 8 | イベントを補充する | 1回/ラウンド | 1枚 | 低 | 低 | 同上 | **ルール必要** |
| 9 | コインを受け取る／支払う | 収入は農地ごと＋購入時 | 数動作（額面の組み合わせ） | 低〜中（数え間違い） | 中 | Bankの近接、額面の窪み | **ルール必要** |
| 10 | 収穫の集計（作物別の収穫中の農地数） | 1回/ラウンド | 全農地の走査 | 中 | **高** | HarvestTally（REC-06、D-02） | ルール上の必須の**読取**。集計ペグは**UI起因** |
| 11 | CoinLedgerの更新 | 1回/ラウンド/人 | 3桁の設定 | 中（設定ミス） | 低 | 更新を手順に組み込む（REC-07） | **UI起因**（REC-15） |
| 12 | TurnOrderTrackの更新 | 1回/ラウンド | 最大5個のマーカー | 中（同数の処理） | 中 | 同数は前ラウンド順を維持（RULE §4）。ALT-13で台帳と統合 | **UI起因**（REC-16） |
| 13 | RoundIndicatorを進める | 1回/ラウンド | 1個 | 低 | 低 | — | **UI起因** |
| 14 | SlotCoverを取り除く | 農地の取得ごと（最大5回/シーズン/人） | 1枚 | 低 | 低 | — | **UI起因**（REC-04） |
| 15 | HarvestTallyのペグを設定する | 1回/ラウンド | 4個 | 中（数え間違い） | 中 | D-02のBでは不要 | **UI起因**（任意） |
| 16 | StartPlayerMarkerを移す／得点表に記録 | 1回/シーズン | — | 低 | 低 | — | **UI起因** |

## 15.2 UI起因の操作の総量

**5人・1ラウンドあたり**（推定）：

| 種別 | 内容 | 操作量 |
|---|---|---|
| ルール必要（トークン更新） | 稼働中の農地×約4 × 5人 | 約20個 |
| ルール必要（イベント更新） | 最大4枚＋補充 | 約5枚 |
| **UI起因** | CoinLedger（5）、TurnOrderTrack（5）、RoundIndicator（1）、HarvestTally（4、任意） | **11〜15回** |

**発見**：UI起因の操作は、ルール必要な操作の約3〜4割に達する。**RECOMMENDATION**：UIの操作を**Core**（必須）と**Assist**（任意）に分ける。

| モジュール | 区分 | 理由 |
|---|---|---|
| RoundIndicator、PhaseRail | Core | 時間の位置の把握（REQUIREMENT） |
| CoinLedger、TurnOrderTrack | Core（**D-03、D-04**で判断） | 手番順の判定（RULE §4）に必要 |
| SlotCover | Core | 取得コストの明示、誤置きの防止 |
| SeasonCropPanel（ラダー、ModifierWell） | Core | 進行係の操作なし |
| **HarvestTally（ペグ）** | **Assist（D-02）** | 進行係の操作が増える |
| 収穫予定ストリップ（ALT-05） | Assist | 同上 |

## 15.3 ルール必要な操作とUI起因の余計な操作の区別

**REQUIREMENT**：UIの設計は、次の余計な操作を避ける。

- トークンを、レーンの間で二度動かす（例：待レーンから別の場所を経由する）。→ 穴の形状ロックで、1回で正しい場所へ。
- カードの向きを変える、ずらす、といった追加のフラグ操作。→ 採用しない（REC-03。ALT-03は任意）。
- 台帳の更新を、手番中に毎回行う。→ ラウンド末に1回（OQ-02）。

---

# 16. Round / Season / Time Representation

## 16.1 時間の要素

| 要素 | 範囲 | 出典 |
|---|---|---|
| シーズン | 1〜プレイヤー数（基本） | RULE §1、§3 |
| ラウンド | 1〜10（基本）／1〜18（上級） | RULE §1 |
| フェーズ | イベント更新 → 手番 → 収入 | RULE §4 |
| 待機・収穫・クールダウン | 作物ごとのW・H・Cラウンド | RULE §6 |
| イベントの時間軸 | 継続中・今・次・次の次 | RULE §4、§9 |

## 16.2 共通の視覚文法（時間の文法）

**RECOMMENDATION（REC-13）**：**1セル＝1ラウンド**。方向は**左が過去、右が未来**。フェーズを、次のパターンで表す。

| 意味 | セルのパターン | アイコン | 使う場所 |
|---|---|---|---|
| 待機（W） | **中抜き** | 砂時計 | 作物カードの時間軸、待レーン、（デジタルの）ピップ |
| 収穫（H） | **塗り** | 穂 | 作物カードの時間軸、収レーン、ピップ |
| クールダウン（C） | **斜線** | 月 | 作物カードの時間軸、休レーン、ピップ |
| 現在 | **太い枠** | — | RoundIndicator、EventRail（「今」） |
| 過去（終了） | 塗りつぶしの灰色 | — | RoundIndicator |
| 未来（未到達） | 淡い輪郭 | — | RoundIndicator、EventRailの予告 |

この文法を、**すべての時間表示**で共有する。「作物カード、農地のレーン、ラウンド表示、イベントレール」が、同じ形の語彙を使う。

## 16.3 RoundIndicator

| 項目 | 内容 |
|---|---|
| 構成 | R個のセル（基本10、上級18）を横一列に並べる。各セルに、ラウンド番号 |
| 現在 | 太い枠＋マーカー |
| 残り | 未到達のセルを、淡い輪郭で表示。**残りセル数**が、一目で分かる |
| ケースFへの対応 | 作物カードの時間軸（W＋Hのセル）と、RoundIndicatorの残りセルを、**同じ文法（1セル＝1ラウンド）で並置**して比較できる。**セル幅は揃えなくてよい**（文法を揃える） |
| 寸法 | 18セル×15 mm＝270 mm。共通エリア内（§11.7） |
| 特殊表示 | 特定のラウンド番号に、ルール上のイベントを固定表示しない（RULE §4の再シャッフルは、山札の枚数で決まるため。§9.3） |

## 16.4 PhaseRail

3つのフェーズを、横一列のボックスで表し、現在のフェーズにポインタを置く。

```
 ① イベント更新  ▶  ② 手番（手番順に）  ▶  ③ 収入
                                             ├ 1 計数（凍結）
                                             ├ 2 受取
                                             ├ 3 更新（左のスロットから）
                                             └ 4 台帳
```

- 1ラウンド目は、①をスキップする（RULE §4）。PhaseRailは、1ラウンド目に①を淡色で表示する。
- ③の4つのサブステップは、§6.6の順序を印刷する（REC-07）。

## 16.5 SeasonIndicator と StartPlayerMarker

| 要素 | 内容 |
|---|---|
| SeasonIndicator | シーズン数のピップ（●○○○）。現在のシーズンを塗る。マッチの全体の進行が分かる |
| StartPlayerMarker | そのシーズンのスタートプレイヤーのボード（の窪み）に置く。**シーズンごとに時計回りに1人ずつ移る**（RULE §4）。1ラウンド目の手番順は、このマーカーから時計回り |
| TurnOrderTrack | R1は座席順（スタートから時計回り）、R2以降は所持コイン昇順（同数は前ラウンド順を維持） |

## 16.6 背景設定（季節）

**OPEN QUESTION（OQ-14）**：RULEの付録Cの背景設定（基本：5月初旬〜9月中旬、上級：3月下旬〜。ルール外・非公開の設定）を、物理版・デジタル版のUIに出すか。**推奨**：物理版では、ルール本文に関わる表示をしない。デジタルでは、演出（背景の季節変化）にとどめ、日付・月の表示は、方針が決まるまで**実装しない**。

## 16.7 ルールを変更しない補助トラック

**RECOMMENDATION**：RoundIndicator、PhaseRail、SeasonIndicatorは、いずれも、ルールの規定を変えない補助の表示。ルール上のタイムトラック方式は導入しない。

---

# 17. Physical-to-Digital Mapping

## 17.1 原則

**RECOMMENDATION**：物理版をそのまま画面にコピーせず、**情報階層とゲーム状態モデル（§3.2）を共通化**する。物理版の配置（レーンの位置、カードのずらし方など）は、状態の**投影**であり、状態そのものではない。デジタル版は、状態から直接描画する。

対象の展開順：（1）物理ボードゲーム、（2）現在のテキストベースの模擬プレイのGUI化、（3）ブラウザ／スマートフォンでのオンライン対戦。

## 17.2 補助の区別（表示補助か、新しい情報か）

**REQUIREMENT**：デジタルの補助は、次の3段階に分けて扱う。

| 段階 | 判定基準 | 例 | 既定（D-09） |
|---|---|---|---|
| **A0：物理相当** | 物理版と同じ情報のみ。計算は、プレイヤーが行う | 物理と同じ配置の再現 | — |
| **A1：表示補助** | **画面上の公開情報を読み取るだけ**で得られる情報の再表示（推論・時間の投影を含まない） | 作物別の収穫中の農地数（HarvestTally）、イベント補正の合計、収入はしごの強調、現在の収入の自動計算、フェーズのポインタ、レーンの状態のツールチップ、合法手のハイライト（ルールの適用のみ）、状態のアニメーション | **有効（案）** |
| **A2：導出情報** | 複数の情報源と**時間の投影**が必要で、プレイヤーの計算力の差を埋める（**計算力の差**に影響しうる：RULE §14.7） | 確定している将来の収穫中の農地数、初収入ラウンド／最終収入ラウンドの表示、植えた場合の収入予測 | **無効（案）**。学習モードでオプション |
| **A3：新情報** | 非公開情報の利用、または最適化の助言 | 山札の中身の推定、AIによる推奨手 | **提供しない** |

**注意**：A1の「合法手のハイライト」は、ルールの適用（RULE §5、付録A）のみで、戦略的な助言ではない。ただし、計算力の差に影響しうるため、設定で切り替えられるようにする（**D-09**）。**マッチ内の全プレイヤーに、同じ補助レベルを適用する**（**OQ-11**）。

## 17.3 対応表

| Physical | Digital equivalent | Shared information | Digital-only enhancement（段階） |
|---|---|---|---|
| CropCard | CropCard（同じレイアウト、`layout=B`） | 作物名、グリフ、タイプ、栽培コスト、W・H・C、基本収入、最低保証 | ホバー／タップで詳細（A1）。場札に「初収入ラウンド」（**A2**） |
| EventCard | EventCard | 対象作物、符号付き数値、極性 | 対象作物のハイライト（A1） |
| FarmSlot | FarmSlot（同じ5状態） | カード、白・緑・赤の個数、取得の有無 | 状態のアニメーション（A1）。前作カードの減光＋「前作」タグ（A1）。合法手のハイライト（A1） |
| TokenWaiting／TokenHarvest／TokenCooldown | **ピップ**（○／⬡／◇の形、レーンに整列） | 個数 | 遷移のアニメーション（A1） |
| PlayerBoard | PlayerBoard | 5スロット、コイン、プレイヤー識別 | 折りたたみ（他者のボード）、自分のボードの拡大 |
| SlotCover | 未取得スロットの表示 | 取得コスト、「次」 | 支払い後の残りコインのプレビュー（A1） |
| CropMarket | CropMarket | 場札3〜4枚 | 選択した場札の支払い合計のプレビュー（A1）。初収入ラウンド（**A2**） |
| EventTimeline／ActiveEventArea | EventRail（同じ6ゾーン） | 継続中・今・次・次の次、捨て札の内容、山札の枚数 | ラウンド番号の併記（A1） |
| SeasonCropPanel | SeasonCropPanel（同じ領域） | 静的数値、IncomeLadder | **HarvestTallyの自動更新**（A1）、収入の自動計算（A1）、収穫予定（**A2**） |
| HarvestTally | 自動集計 | 作物別の収穫中の農地数 | 収穫マトリクス（下記。A1） |
| CoinArea／CoinLedger | 数値表示（常時最新） | 所持コイン（公開） | 増減のアニメーション（A1） |
| TurnOrderTrack | 手番順のリスト | 手番順 | 現在の手番の強調（A1） |
| RoundIndicator／PhaseRail／SeasonIndicator | 同じ形（ヘッダーに折りたたみ） | ラウンド、フェーズ、シーズン | 残りラウンド数の表示（A1）、収入フェーズの自動進行（A1） |
| CropDeck／EventDeck | 山札（枚数のみ） | 枚数 | 正確な枚数の表示（OQ-06）。再シャッフルの通知（A1） |
| CropDiscard／EventDiscard | 捨て札の一覧（公開） | 内容 | 並べ替え・絞り込み（A1） |
| ScoreBoard | 得点表 | シーズンごとのコイン、合計 | 常時ヘッダー |
| StartPlayerMarker | スタートプレイヤーの表示 | スタートプレイヤー | — |
| Bank | 表示なし（自動） | — | 自動の受取・支払い |

## 17.4 デジタル版のレイアウト概念

**デスクトップ**（ASSUMPTION）：3領域。

```
┌──────────────────────────────────────────────────────────────┐
│ ヘッダー：SeasonIndicator │ RoundIndicator │ PhaseRail │ 手番順                │
├──────────────────────┬───────────────────────────────────────┤
│ 左：SeasonCropPanel ×4      │ 中央：EventRail ／ CropMarket                       │
│    （収穫集計・はしご）        │                                                     │
│ 右：CoinLedger ／ ScoreBoard │ 下：自分のPlayerBoard（大） ＋ 他者のPlayerBoard（小） │
└──────────────────────┴───────────────────────────────────────┘
```

**モバイル（縦）**：ヘッダー（固定）＋4タブ＋下部のアクションバー。

| タブ | 内容 |
|---|---|
| 卓 | EventRail（コンパクト）、SeasonCropPanel（横スクロール）、CropMarket |
| 自分 | 自分のPlayerBoard、コイン、EconomyStrip |
| 相手 | **収穫マトリクス**（下記）、他プレイヤーのPlayerBoard（折りたたみ） |
| 記録 | ScoreBoard、ログ |

**収穫マトリクス（REC-21）**：行＝今シーズンの4作物、列＝プレイヤー。各セルに、その作物の農地の状態（`収3`＝収穫中・緑3、`待2`＝待機・白2、`休4`＝休耕・赤4）を、形状アイコン＋個数で表示する。これは、物理の「全テーブルの走査」を、1つの表に置き換えるもの。**公開情報の再配置（A1）**であり、時間の投影は含まない。

## 17.5 状態モデルの移植性

**REQUIREMENT**：デジタル版は、§3.2の状態モデルだけを入力とし、物理の偶然の配置（カードの置き方、トークンの微妙な位置）に依存しない。表示の状態（ホバー、選択）は、ゲーム状態に含めない。

---

# 18. Design Alternatives

## 18.1 検討した案の一覧

| ID | 案 | 長所 | 短所 | 判定 |
|---|---|---|---|---|
| ALT-01 | 進行マーカー方式（T3）：作物カードの時間軸に、マーカーを1個置き、1ラウンドごとに進める | 操作量が約4割減。遷移の操作が不要。時間の文法と一致 | 状態機械（RULE §6.2）の書き換えが必要。マーカーが1セルを隠す。他者からの遠距離判定が弱い | **ALTERNATIVE**。**D-01**、GRC-04 |
| ALT-02 | 1レーン方式：待・収・休を、形状違いのトークンで、1レーンに置く | 占有面積が小さい | 位置の冗長性がない。遠距離判定が弱い | ALTERNATIVE |
| ALT-03 | 前作カードを、収穫終了時に手前へ1段ずらす（空きの積極表示） | 空きが視認しやすい | 追加のフラグ操作。トークンとの二重の真実（不整合のリスク）。カード領域が広がる（＋約20 mm） | ALTERNATIVE（採用しない。**OQ-01**） |
| ALT-04 | カード上のトークン領域 | 状態がカードと一体 | トークンが数値を隠す（REQUIREMENT違反） | **却下** |
| ALT-05 | 収穫予定ストリップ（作物パネル上の任意モジュール） | ケースCの負荷を減らす | 進行係の操作が増える | 任意（Assist） |
| ALT-06 | コインを廃止し、カウンタ（台帳）のみ（C3） | 操作量が減る。公開情報が常に読める | コインの実体がなくなる（**GRC-03**） | GRC-03として別枠 |
| ALT-07 | 作物固有の色を、識別の主符号にする | 直感的（「トマトは赤」） | 状態色と衝突。色覚多様性に弱い。10色の識別は困難 | 採用しない（**D-08**） |
| ALT-08 | レーンをカードの手前側に置く | 所有者の操作が楽 | 他者から遠く、G1に反する | ALTERNATIVE |
| ALT-09 | EventRailを4ゾーン（継続中・今・次・次の次）だけにして、山札・捨て札を別置きにする | 共通エリアが小さくなる | コンベアの連続性が弱まる。捨て札・山札の位置が離れる | ALTERNATIVE（**D-05**） |
| ALT-10 | 座席別のリファレンス：各プレイヤーに、今シーズンの4作物の小型の参照タイルを置く | 数値を全員が自分向きで読める | 部品が増える（4枚×プレイヤー数）。準備の手間 | ALTERNATIVE（**D-07**） |
| ALT-11 | 作物パネルなし（各自が作物カードを見る） | 部品が減る | ケースC・Dの負荷が下がらない | ALTERNATIVE（D-02のC） |
| ALT-12 | イベントカードを作物カードと同サイズにする | 製造の単純化 | 山札の取り違えのリスク | ALTERNATIVE（D-06） |
| ALT-13 | PlayerPawn（コインダイヤル付き）：CoinLedgerとTurnOrderTrackを1つの部品に統合 | 更新の操作が減る（N回分） | ダイヤル付きの部品が必要 | ALTERNATIVE（**D-04**） |

## 18.2 作物カードの3案（再掲）

案A（数値中心）／**案B（時間軸中心。推奨）**／案C（アイコン中心）。詳細は§7.4〜7.5。Figmaでは、3案とも `layout=A|B|C` のバリアントとして作る。

## 18.3 その他の代替（判断はD-xxへ）

- **卓のサイズ**：標準卓 1200×950（推奨）／3人時の縮小（約1000×850）。
- **トークンのレーンの並び**：横3列（推奨）／縦3行（時間の左右方向の文法と衝突するため非推奨）。
- **デジタルのアシストレベル**：A1を既定（案）／A0を既定（**D-09**）。

---

# 19. Recommended Design Specification

## 19.1 推奨の登録簿

| ID | 推奨（すべて **RECOMMENDATION（UIのみ）**） | 主な根拠 | 関連コンポーネント |
|---|---|---|---|
| REC-01 | FarmSlotに、3本の縦レーン（待｜収｜休）を、カードの**遠側**に置く。収穫は常に**中央** | G1、G2、REQ：カードを隠さない | FarmSlot |
| REC-02 | トークンは、形状（○⬡◇）＋色＋アイコン＋穴の形状ロックで符号化 | G7、誤操作防止 | TokenWaiting／Harvest／Cooldown |
| REC-03 | 物理の追加フラグを設けない。**トークンが唯一の真実**。空き＝待・収レーンが空 | P5（二重の真実を避ける） | FarmSlot |
| REC-04 | 未取得スロットは、**SlotCover**で覆う。最左のカバーが「次」 | REQ：経済の比較、誤置きの防止 | SlotCover |
| REC-05 | 農地取得コストは、各プレイヤーボードに印刷する（共通ボードには置かない）。EconomyStripでコインと近接 | ケースE | PlayerBoard |
| REC-06 | 共通の**SeasonCropPanel**を4枚置く（静的値、IncomeLadder、HarvestTally、ModifierWell） | REQUIREMENT：収穫中スキャン性（§1.1）、ケースA・D | SeasonCropPanel |
| REC-07 | 収入フェーズの手順を、**計数（凍結）→受取→更新→台帳**の順に印刷 | RULE §7の「開始時の状態」 | PhaseRail |
| REC-08 | **EventRail**：捨て札｜継続中｜今｜次｜次の次｜山札の6ゾーン。全カードが毎ラウンド左へ1つ進む | REQUIREMENT：有効なイベント（最大2枚）と予告の区別、RULE §4の手順 | EventTimeline／ActiveEventArea |
| REC-09 | 有効（継続中＋今）と予告（次・次の次）を、ブラケットで分ける | REQUIREMENT：有効なイベントと予告の区別 | EventTimeline |
| REC-10 | EventCard：グリフ＋符号付き大数字＋▲▼＋シェブロン、ミニサイズ、両向き | P2、REQ | EventCard |
| REC-11 | CropCardは案B（時間軸中心）を基本とし、数値の可読性は案Aを取り入れる | ケースA・F、G4 | CropCard |
| REC-12 | 作物の識別は、グリフ（輪郭）＋頭字＋タイプ帯（機能色）。四辺から識別 | G5、REQUIREMENT：他者の農地での識別 | CropCard（Identity） |
| REC-13 | 時間の共通文法：1セル＝1ラウンド、中抜き＝待、塗り＝収、斜線＝休 | G4 | 全体 |
| REC-14 | RoundIndicator＋PhaseRail＋SeasonIndicator＋StartPlayerMarker | REQUIREMENT：現在位置の把握（§3.1） | 各種 |
| REC-15 | CoinArea＋CoinLedger（ラウンド末に総額を公開更新） | REQUIREMENT：公開情報の可読性、RULE §10 | CoinArea |
| REC-16 | TurnOrderTrack：R1は座席順、R2〜はコイン昇順（同数は前ラウンド順） | RULE §4 | TurnOrderTrack |
| REC-17 | 山札・捨て札のトレイを、形・縁の模様・アイコンで、作物とイベントに分離 | REQ：捨て札の混同防止 | CropDeck 他 |
| REC-18 | プレイヤーの識別は、形＋番号＋色（色は限定的な範囲に） | G7 | PlayerIdentity |
| REC-19 | 標準卓 1200×950。3人（北2・南1）、4人（4辺）、5人（南2・北2・東1、西端に備品） | G9 | Table layouts |
| REC-20 | 共通エリアの数値は、**両向き**（上下180°）で印刷する | P2、P3 | Panel、CropMarket、EventCard |
| REC-21 | デジタル：収穫マトリクス、前作の減光、状態のアニメーション、アシストレベルの設定（A0〜A2） | G8、§17.2 | デジタル全般 |

## 19.2 情報と符号化の対応（まとめ）

| 情報 | 符号化 |
|---|---|
| 農地の状態 | **レーン位置（中央＝収穫）**＋形状＋アイコン＋穴の形状ロック（色は副） |
| 占有と連作制限の別軸 | 待・収レーンの有無（占有）と、休レーンの有無（連作制限） |
| 作物の識別 | グリフ（輪郭）＋頭字（＋作物名の文字） |
| 作物のタイプ | 帯の模様＋色 |
| イベントの極性・大きさ | ▲▼の形＋符号＋シェブロン数（色は副） |
| プレイヤー | 形＋番号（＋限定された範囲の色） |
| 時間 | セルの文法（中抜き／塗り／斜線）＋左→右の方向 |

## 19.3 コンポーネントの寸法一覧（ASSUMPTION：1 mm = 4 px）

| コンポーネント | mm | px |
|---|---|---|
| CropCard | 63×88 | 252×352 |
| EventCard | 45×63 | 180×252 |
| Token（円盤・菱形） | Ø14 | 56 |
| Token（六角柱、上面図） | Ø14 | 56 |
| LaneBlock | 60×72 | 240×288 |
| FarmSlot | 66×180 | 264×720 |
| PlayerBoard | 380×225 | 1520×900 |
| SeasonCropPanel | 63×130 | 252×520 |
| EventRail | 320×100 | 1280×400 |
| CropMarket（4枠） | 264×88 | 1056×352 |
| RoundIndicator（18セル） | 270×40 | 1080×160 |
| TurnOrderTrack | 200×60 | 800×240 |
| CoinLedger | 200×130 | 800×520 |
| ScoreBoard | 240×80 | 960×320 |
| 共通エリア（1組） | 720×440 | 2880×1760 |
| 標準卓 | 1200×950 | 4800×3800 |

## 19.4 色・文字・余白（ASSUMPTION。初期値。**画風は決めない**）

**色**（色覚多様性に配慮したパレット。**OQ-09**：RULEの色名「白／緑／赤」はラベルとして保持）

| スタイル名 | 値 | 用途 |
|---|---|---|
| `state/waiting` | 塗り #FFFFFF、線 #2B2B2B | 待機（○、砂時計） |
| `state/harvest` | #009E73（線 #005C42） | 収穫（⬡、穂） |
| `state/cooldown` | #D55E00（線 #7A3500）＋斜線 | クールダウン（◇、月） |
| `state/idle` | #E6E6E6 | 空き（デジタルの表示） |
| `state/unacquired` | 破線 #9A9A9A | 未取得 |
| `type/immediate`（即金型） | #56B4E9、模様：ドット | タイプ帯 |
| `type/lump`（一括型） | #F0E442、模様：横線 | タイプ帯 |
| `type/longrun`（長期継続型） | #CC79A7、模様：波線 | タイプ帯 |
| `type/balanced`（中間型） | #E0E0E0、模様：無地 | タイプ帯 |
| `event/up` | #E69F00（▲、塗り） | イベント：増 |
| `event/down` | #332288（▼、輪郭） | イベント：減 |
| `player/1〜5` | #CC6677、#DDCC77、#88CCEE、#AA4499、#44AA99 ＋ 形 ●■▲◆★ | ボード枠、コインマーカー、StartPlayerMarker、台帳の行に**限定** |
| `text/primary`、`surface/base` | #1A1A1A、#FFFFFF／#F2F2F2 | 文字、面 |

**文字**（Figmaのテキストスタイル。essentialな情報は、16 px（約4 mm）以上）

| スタイル | サイズ | 用途 |
|---|---|---|
| `numeral/xl` | 48 px、太字 | イベント値、コイン総額、収入 |
| `numeral/l` | 40 px、太字 | 栽培コスト、基本収入 |
| `numeral/m` | 24 px | 最低保証、W・H・C |
| `label/m` | 16 px | ラベル、作物名 |
| `label/s` | 12 px | **参考情報のみ**（必須情報には使わない） |

**余白**：4 pxの倍数（4、8、12、16、24）。

---

# 20. Figma Component Specification

## 20.0 共通の規約

| 項目 | 内容 |
|---|---|
| 単位 | px（1 mm = 4 px。**ASM-04**）。寸法は、mmとpxを併記 |
| 命名 | コンポーネント名はPascalCase。バリアントのプロパティ名はcamelCase。値は英小文字 |
| レイアウト | Auto Layout（方向・余白・間隔を明記）を基本とする。物理の固定寸法のものは、固定サイズ＋絶対配置 |
| 作物の列挙値 | `crop=radish|komatsuna|potato|corn|negi|edamame|sweetpotato|pumpkin|tomato|watermelon` |
| 状態の列挙値 | `state=unacquired|waiting|harvesting|idleCooling|idleFree` |
| データの入れ方 | 作物・イベントの数値は、**§7.6の表とRULE §12の値をそのまま**使う（値を変えない。追加・推測しない） |
| 色・文字 | §19.4のスタイル名を使う（値のハードコード禁止） |
| 注釈 | 各コンポーネントに、`RULE`／`REQUIREMENT`／`RECOMMENDATION`のタグを、Figmaの説明欄に転記する |

## 20.1 コンポーネントの階層

```
Table
 ├ PlayerBoard ×3..5
 │   ├ PlayerIdentity ／ StartPlayerMarker(slot)
 │   ├ FarmSlot ×5
 │   │   ├ LaneBlock ── TokenLane(待) ── TokenWaiting ×0..4
 │   │   │            ├ TokenLane(収) ── TokenHarvest ×0..4
 │   │   │            └ TokenLane(休) ── TokenCooldown ×0..4
 │   │   ├ CardBed ── CropCard（前作カードを含む） ／ SlotCover
 │   │   └ SlotFooter（スロット番号・取得コスト）
 │   ├ CoinArea
 │   └ ReadingLegend（読取規則）
 └ SharedArea
     ├ EventRail ── EventDiscard ─ ActiveEventArea ─ EventTimeline(今・次・次の次) ─ EventDeck
     ├ SeasonCropPanel ×4 ── CropGlyph ／ TypeBand ／ IncomeLadder ／ HarvestTally ／ ModifierWell
     ├ CropMarket ── CropCard ×3..4
     ├ CropDeck ／ CropDiscard
     ├ RoundIndicator ／ PhaseRail ／ SeasonIndicator
     ├ TurnOrderTrack ／ CoinLedger ／ ScoreBoard
     └ Bank ／ ExcludedPocket
```

---

## Component: CropCard

### Purpose
作物の識別・購入判断・植え付け後の参照。他プレイヤーの農地に置かれていても、識別できる。

### Required regions（`layout=B`。寸法は px、カード252×352、内側の余白12）
1. **Identity region**：y 12〜116。左に `CropGlyph`（88×88）、右に作物名（`label/m`、16 px以上）と頭字の円（Ø40）。**タイプ帯**（y 120〜136、幅228）を直下に置く。
2. **Economy region**：y 144〜192。左端にコイン型の**栽培コスト**（Ø48、`numeral/l`）。
3. **Income region**：y 196〜244。**収穫セルの開始位置に揃えて**、基本収入（`numeral/l`）と最低保証（`numeral/m`、「最低」ラベル）を配置。C=0の作物も同じ。
4. **Lifecycle region**：y 252〜312。**時間軸の帯**（セル幅20 px、高さ32 px、最大11セル＝220 px）。左から、待（中抜き、砂時計）→ 収（塗り、穂）→ 休（斜線、月）。帯の下に、`待W 収H 休C` の数値ラベル（`label/m`）。
5. **Cooldown region**：帯の休セル（斜線）と、Cの数値。**C=0のとき**は、斜線セルを描かず、「連作可」のアイコン（ループ＋チェック）を、収の右に表示する。
6. **Token interaction region**：**設けない**（トークンはFarmSlotのレーン）。
7. **Mirror region**：y 316〜340。グリフの小（24 px）と栽培コスト（Ø24）を、**180°回転した複製**として置く（`dualOrientation=true`のとき）。

### Variants
- `crop` = 10種（上の列挙値）
- `layout` = `A` | `B` | `C`（§7.4の3案。Bを基本）
- `view` = `front` | `back`（`back`は畝の模様、種のアイコン）
- `scale` = `full`（252×352）| `mini`（126×176、作物パネル・ログ用）
- `dualOrientation` = `true` | `false`
- `dimmed` = `true` | `false`（デジタルの前作カード。不透明度 60%）

### Required visible properties
`cropName`、`cropType`、`cultivationCost`、`waitingRounds`、`harvestRounds`、`baseIncome`、`minimumIncome`、`cooldownRounds`

### Information hierarchy
識別（グリフ、頭字、タイプ帯）＞ 栽培コスト ＞ 時間軸（W・H・C）＞ 収入（基本収入、最低保証）

### Physical dimensions / provisional dimensions
63×88 mm（252×352 px。**ASM-01**）。角丸12 px。

### Layout constraints
- 時間軸の帯は、`waitingRounds＋harvestRounds＋cooldownRounds` のセル数（最大11）が、228 px内に収まる。
- 数値は、`numeral/l`（40 px）以上または`numeral/m`（24 px）以上。12 px以下の文字を必須情報に使わない。
- グリフは、周囲に12 px以上の余白。

### Visibility requirements
- 作物の識別は、テーブル越しに（グリフの輪郭と頭字で）判別できる。
- **トークンがカードの数値を隠さない**（トークンはカード外）。
- 現在の状態は、カードではなくFarmSlotのレーンで判別できる（小さな文字を読ませない）。

### Interaction notes
場札からの選択（タップ／クリック）。デジタルは、ホバーで詳細（A1）。

### Digital mapping
同じレイアウト。`scale=mini`をログやマトリクスで使う。前作カードは `dimmed=true`。

### Figma implementation notes
10作物×3レイアウトのバリアント。`CropGlyph` と `TypeBand` は、instance swap。時間軸の帯は、**セルのコンポーネント**（`cell=wait|harvest|cooldown`）を並べるAuto Layoutで作り、作物ごとに個数を変える。C=0の作物用に、`CooldownNone` のコンポーネントを用意する。

---

## Component: CropGlyph

- **Purpose**：作物の輪郭による識別（REC-12）。
- **Required information**：作物の輪郭（§7.7の特徴を満たす、10種で互いに異なるシルエット）。
- **Information hierarchy**：輪郭のみ。単色。
- **States / Variants**：`crop`（10種）、`size`（88、48、24）、`rotation`（0、180）。
- **Child components**：なし。
- **Physical dimensions**：22×22 mm（88 px）、12×12 mm（48）、6×6 mm（24）。
- **Layout constraints**：正方形。余白なしのシルエット。最小 18 mm（L1）を、Identityに使う。
- **Visibility requirements**：回転（0、90、180、270°）しても識別できる輪郭。
- **Interaction notes**：なし。
- **Digital mapping**：同じ。ホバーで作物名。
- **Figma implementation notes**：ベクターで作る。**仕上げの絵柄（画風）は決めない**。仮のシルエットで足りる。

## Component: TypeBand

- **Purpose**：作物タイプ（即金型／一括型／長期継続型／中間型）の機能的な色・模様。
- **Required information**：タイプ名（補助）、帯の模様と色。
- **Information hierarchy**：模様 ＞ 色 ＞ 文字。
- **States / Variants**：`type=immediate|lump|longrun|balanced`。
- **Child components**：なし。
- **Physical dimensions**：228×16 px（カード内）。パネルでは 228×24。
- **Layout constraints**：全幅。模様は、状態の斜線（休）と紛れない（ドット、横線、波線、無地）。
- **Visibility requirements**：L1で、帯の色と模様が識別できる。
- **Interaction notes**：なし。**Digital mapping**：同じ。
- **Figma implementation notes**：色は `type/*` スタイル。模様はパターン塗り。

---

## Component: FarmSlot

### Purpose
1つの農地スロットの状態を、レーン・カード・フッターで表す。**占有（待・収）と連作制限（休）を別軸**で見せる。

### Required regions（264×720 px）
1. **LaneBlock**：y 0〜288。ヘッダー（y 0〜32）＋3列×4穴（列間80、縦ピッチ64）。**遠側（上）**。
2. **CardBed**：y 300〜652。カードの領域（252×352に、両側6 pxの余裕）。前作カードは、ここに残る。
3. **SlotFooter**：y 652〜700。スロット番号（①〜⑤）と取得コスト（`numeral/m`）。**常時表示**。
4. **Cover overlay**：未取得のとき、`SlotCover` をCardBedの上に重ねる。
5. **StateBadge**（デジタル／注釈用。`showBadge=true`のとき）：状態の名前とアイコン。

### Variants
- `state` = `unacquired` | `waiting` | `harvesting` | `idleCooling` | `idleFree`
- `slotIndex` = 1〜5（取得コスト＝0/1/2/3/4を対応させる）
- `whiteCount`、`greenCount`、`redCount` = 0〜4（**同時に非0なのは高々1つ**。`state`から決まる）
- `crop` = `none` | 10種（CardBedのCropCardをinstance swap）
- `viewer` = `owner` | `opponent`（対向の視点で見た向き。`opponent`は180°回転して表示）
- `showBadge` = `true` | `false`

### 状態ごとの見え方

| `state` | 待レーン | 収レーン | 休レーン | CardBed | StateBadge |
|---|---|---|---|---|---|
| `unacquired` | 空（穴のみ） | 空 | 空 | `SlotCover`（取得コスト） | 未取得（破線） |
| `waiting` | ○ ×1〜4 | 空 | 空 | 現在の作物カード | 待機中（砂時計） |
| `harvesting` | 空 | ⬡ ×1〜4 | 空 | 現在の作物カード | 収穫中（穂） |
| `idleCooling` | 空 | 空 | ◇ ×1〜4 | 前作カード（`dimmed`） | 空き・休耕中（月）※「別の作物は植えられる」 |
| `idleFree` | 空 | 空 | 空 | 前作カード（`dimmed`） | 空き（空の四角） |

### Required visible properties
`slotIndex`、`acquisitionCost`、`cropId`（前作を含む）、`whiteCount`、`greenCount`、`redCount`

### Information hierarchy
状態（レーン）＞ 作物の識別（グリフ）＞ スロット番号・取得コスト ＞ カードの数値

### Physical dimensions / provisional dimensions
66×180 mm（264×720 px。**ASSUMPTION**）。ピッチ70 mm（隙間4 mm）。

### Layout constraints
- レーンは**常時表示**（トークンがなくても、穴とヘッダーが見える）。
- **収レーンは常に中央**。左から待｜収｜休。
- トークンをカードの上に置かない。カードの数値を隠さない。
- 未取得のスロットに、カードを置けない（SlotCoverが覆う）。

### Visibility requirements
- 向かい側から、収レーンの緑（六角柱）の有無が見える。
- 「空き」は、待・収レーンが空であることで読める（追加のフラグなし。**REC-03**）。
- 同種を再び植えられるかは、休レーンが空かどうかで読める。

### Interaction notes
- 植え付け（デジタル）：合法なスロットをハイライト（A1）。
- 物理：トークンの更新は左のスロットから順。

### Digital mapping
`Slot`（状態モデル）から、`state`を導出して描画する。トークンは**ピップ**（○／⬡／◇、24 px）でレーンに並べる。

### Figma implementation notes
`LaneBlock` と `CardBed` を分離してAuto Layout（縦）でつなぐ。`state` は、レーンの個数の組み合わせで表現する（`TokenLane` の `count` プロパティを、`state` から切り替える）。バリアントの総数を抑えるため、`whiteCount`、`greenCount`、`redCount` は、`TokenLane` の子のバリアントで持つ。

---

## Component: TokenLane / LaneBlock

- **Purpose**：待・収・休の各レーンに、最大4個のトークンを置く穴を持つ。
- **Required information**：ヘッダー（ラベル、アイコン）、穴、個数。
- **Information hierarchy**：位置（レーン）＞ 形 ＞ 個数 ＞ ラベル。
- **States / Variants**：`TokenLane`：`type=waiting|harvest|cooldown`、`count=0..4`。`LaneBlock`：3レーンの束。
- **Child components**：`TokenWaiting`／`TokenHarvest`／`TokenCooldown`（各×0〜4）。
- **Physical dimensions**：LaneBlock 60×72 mm（240×288 px。ヘッダー8 mm＋穴4×16 mm）。1レーン 20×72 mm。
- **Layout constraints**：穴の形は、トークンの形に一致（円／六角形／菱形）。埋める順序は、カードに近い穴から。**収は中央**。
- **Visibility requirements**：ヘッダーは常時表示。レーンの背景は、待＝無地、収＝薄い塗り、休＝斜線。
- **Interaction notes**：物理はトークンを穴に嵌める。デジタルは、遷移をアニメーション。
- **Digital mapping**：ピップの列。
- **Figma implementation notes**：`count` を、穴の塗り（埋まり）の表示に対応させる。

---

## Component: TokenWaiting

- **Purpose**：待機（白）の1単位。「初収入まで」のカウント。
- **Required information**：形（○）、アイコン（砂時計）。
- **Information hierarchy**：形 ＞ アイコン ＞ 色。
- **States / Variants**：`context=lane|supply|pip`。
- **Child components**：なし。
- **Physical dimensions**：Ø14 mm、厚さ2 mm（56 px）。ピップは 24 px。
- **Layout constraints**：円形。待レーンの円形の穴に嵌まる。
- **Visibility requirements**：白（縁は濃色）。中抜きのセル（時間の文法）と揃える。
- **Interaction notes**：物理：白を取る／置く。**Digital mapping**：ピップ。
- **Figma implementation notes**：`state/waiting` のスタイル。中央に砂時計のアイコン。

## Component: TokenHarvest

- **Purpose**：収穫（緑）の1単位。「残りの収入回数」のカウント。
- **Required information**：形（六角柱の上面）、アイコン（穂）。
- **Information hierarchy**：形 ＞ アイコン ＞ 色。
- **States / Variants**：`context=lane|supply|pip`。
- **Child components**：なし。
- **Physical dimensions**：Ø14 mm、高さ10 mm（56 px、上面図）。ピップは 24 px。
- **Layout constraints**：六角形。収レーンの六角形の穴に嵌まる。**他のトークンより背が高い**（物理）。
- **Visibility requirements**：L0で判別できる（背の高さ、六角形の輪郭、青緑）。塗りのセルと揃える。
- **Interaction notes**：物理：緑を取り、収入を得る。**Digital mapping**：ピップ。
- **Figma implementation notes**：`state/harvest`。上面図に内側のリングで、高さを暗示（画風は決めない）。

## Component: TokenCooldown

- **Purpose**：連作クールダウン（赤）の1単位。「同種が再び可能になるまで」のカウント。
- **Required information**：形（菱形）、アイコン（月）、斜線の模様。
- **Information hierarchy**：形 ＞ 模様 ＞ アイコン ＞ 色。
- **States / Variants**：`context=lane|supply|pip`。
- **Child components**：なし。
- **Physical dimensions**：14 mm、厚さ2 mm（56 px）。ピップは 24 px。
- **Layout constraints**：菱形。休レーンの菱形の穴に嵌まる。
- **Visibility requirements**：朱赤＋斜線（色に依存しない）。斜線のセルと揃える。
- **Interaction notes**：物理：赤を取る。**Digital mapping**：ピップ。
- **Figma implementation notes**：`state/cooldown`＋斜線パターン。

---

## Component: SlotCover

- **Purpose**：未取得スロットを覆い、取得コストを示す。最左のカバーが「次に取得する農地」（REC-04）。
- **Required information**：取得コスト（0/1/2/3/4）、「未取得」のラベル。
- **Information hierarchy**：取得コスト（大数字）＞ ラベル。
- **States / Variants**：`cost=0|1|2|3|4`、`isNext=true|false`（デジタルのみ。最左のカバー）。
- **Child components**：なし。
- **Physical dimensions**：63×88 mm（252×352 px）＋縁。
- **Layout constraints**：CardBedと同じ大きさ。取得順に、左から。
- **Visibility requirements**：`state/unacquired`（破線）。数字は、L2で読める（`numeral/xl`）。
- **Interaction notes**：物理：取得時に取り除く。デジタル：取得時に消える（アニメーション）。
- **Digital mapping**：未取得スロットの表示。`isNext` を強調（A1）。
- **Figma implementation notes**：FarmSlotの `state=unacquired` で、CardBedの上に重ねる。

---

## Component: PlayerBoard

### Purpose
1人分の農地（5スロット）、コイン、プレイヤー識別、読取規則をまとめる。

### Required regions（380×225 mm＝1520×900 px）
1. Header：y 0〜40（`PlayerIdentity`、`StartPlayerMarker` の窪み）
2. FarmSlot ×5：y 40〜760。ピッチ280 px、左右の余白68 px
3. Bottom strip：y 760〜900（`CoinArea`、`ReadingLegend`）

### Variants
`playerIndex=1..5`、`orientation=0|90|180|270`（卓のレイアウト用）、`viewer=owner|opponent`、`mode=physical|digital`

### Required visible properties
`playerId`、`coins`、`slots[5]`（各スロットの `state`、`crop`、トークン数）、`isStartPlayer`

### Information hierarchy
FarmSlotの状態 ＞ コイン ＞ プレイヤー識別 ＞ 読取規則

### Physical dimensions / provisional dimensions
380×225 mm（1520×900 px。**ASSUMPTION**）

### Layout constraints
5スロットが横一列に収まる。CoinAreaは手前の帯。SlotCoverの最左が、「次」として見える。

### Visibility requirements
遠側のレーンが、中央側（他プレイヤーの側）に向く。プレイヤー識別は、L0で読める（形＋番号）。

### Interaction notes
デジタル：自分のボードは大きく、他者のボードは折りたたむ。

### Digital mapping
同じ構造。`mode=digital` で、StateBadgeとツールチップが有効。

### Figma implementation notes
FarmSlotの5つのインスタンス＋Auto Layout（横）。`orientation` は、回転した複製（フレームの回転）で用意し、§12〜14の卓の配置で使う。

---

## Component: PlayerIdentity / StartPlayerMarker

- **Purpose**：プレイヤーの識別（REC-18）。スタートプレイヤーの表示。
- **Required information**：形（●■▲◆★）、番号（1〜5）、名前、色。
- **Information hierarchy**：形＋番号 ＞ 名前 ＞ 色。
- **States / Variants**：`playerIndex=1..5`、`isCpu=true|false`、`isStart=true|false`。
- **Child components**：形のアイコン。
- **Physical dimensions**：Identity 24×12 mm（96×48 px）。マーカー Ø14 mm。
- **Layout constraints**：色は、ボード枠・コインマーカー・StartPlayerMarker・台帳の行に限定。
- **Visibility requirements**：形＋番号は、L0で読める。
- **Interaction notes**：なし。**Digital mapping**：同じ（CPU席は、ラベルを付ける。OQ-16）。
- **Figma implementation notes**：色は `player/*` スタイル。

---

## Component: SeasonCropPanel（作物パネル）

### Purpose
今シーズンの各作物について、静的な数値、収入はしご、収穫集計、イベント補正を1か所に集約する（REC-06）。

### Required regions（252×520 px）
1. Identity：y 12〜120（`CropGlyph`、名前、頭字、`TypeBand`）
2. Static values：y 124〜232（栽培コスト、W、H、C、基本収入、最低保証。カードと同じ値）
3. **IncomeLadder**：y 236〜396（N＝1〜8の行）
4. **HarvestTally**：y 400〜456
5. **ModifierWell**：y 460〜508
6. Mirror：下端に、Identityと主要数値の**180°回転の複製**（`dualOrientation`）

### Variants
`crop`（10種）、`tallyMode=pegs|none`（**D-02**）、`dualOrientation=true|false`

### Required visible properties
`cropName`、`cultivationCost`、`waitingRounds`、`harvestRounds`、`baseIncome`、`minimumIncome`、`cooldownRounds`、`harvestingCount`、`eventModifier`

### Information hierarchy
収入（はしご、読取値）＞ 収穫中の枚数 ＞ イベント補正 ＞ 静的値 ＞ 識別

### Physical dimensions / provisional dimensions
63×130 mm（252×520 px）

### Layout constraints
IncomeLadderは、`max(最低保証, 基本収入−(N−1))`（N＝1〜8）の値を表示。**N は、自分を含む収穫中の農地の総数**。注記：「イベント補正（±）を加える」。値の表は、下の「IncomeLadderのデータ」を使う。

### Visibility requirements
L1で読める（IncomeLadderの値と、HarvestTallyのペグの位置）。両向き。

### Interaction notes
物理：進行係が、収入フェーズの手順1で、ペグを設定する。デジタル：自動。**HarvestTallyは、RULE §7の式の入力を示す補助であり、権威はトークン**。

### Digital mapping
`harvestCountByCrop`と`eventModifierByCrop`から描画。現在のNに対応する行を強調（A1）。

### Figma implementation notes
子コンポーネント：`IncomeLadder`（`crop` で行の値を切替、`highlightN=0..8`）、`HarvestTally`（`count=0..10`）、`ModifierWell`（`badges=0..2`、`sum=-8..+8`）。

### IncomeLadderのデータ（RULE §12の値から算出。`max(最低保証, 基本収入−(N−1))`）

| 作物 | N=1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| ラディッシュ（7、3） | 7 | 6 | 5 | 4 | 3 | 3 | 3 | 3 |
| コマツナ（6、3） | 6 | 5 | 4 | 3 | 3 | 3 | 3 | 3 |
| ジャガイモ（9、4） | 9 | 8 | 7 | 6 | 5 | 4 | 4 | 4 |
| トウモロコシ（10、4） | 10 | 9 | 8 | 7 | 6 | 5 | 4 | 4 |
| ネギ（8、3） | 8 | 7 | 6 | 5 | 4 | 3 | 3 | 3 |
| エダマメ（12、5） | 12 | 11 | 10 | 9 | 8 | 7 | 6 | 5 |
| サツマイモ（19、8） | 19 | 18 | 17 | 16 | 15 | 14 | 13 | 12 |
| カボチャ（11、4） | 11 | 10 | 9 | 8 | 7 | 6 | 5 | 4 |
| トマト（9、4） | 9 | 8 | 7 | 6 | 5 | 4 | 4 | 4 |
| スイカ（13、5） | 13 | 12 | 11 | 10 | 9 | 8 | 7 | 6 |

（かっこ内は、基本収入、最低保証。**イベント補正は、この表に含めない**）

---

## Component: EventCard

- **Purpose**：作物1種の収入への加減算（RULE §9.2）を表す。
- **Required information**：対象作物（グリフ、名前）、符号付き数値（＋4／＋2／−2／−4）、極性（▲／▼）、大きさ（シェブロン）、持続の注記（2R）。
- **Information hierarchy**：対象作物のグリフ ＞ 符号付き数値 ＞ 極性 ＞ 持続の注記。
- **States / Variants**：`crop`（10種）、`delta=+4|+2|-2|-4`（**計40枚**）、`view=front|back`、`dualOrientation=true|false`。
- **Child components**：`CropGlyph`、極性のアイコン。
- **Physical dimensions**：45×63 mm（180×252 px。**ASM-02**）。
- **Layout constraints**：数値の高さ ≥ 10 mm（`numeral/xl`）。上下180°の複製（`dualOrientation`）。
- **Visibility requirements**：L0で極性（▲塗り／▼輪郭）、L1で対象作物のグリフ。色（`event/up`、`event/down`）は副次。
- **Interaction notes**：EventRailのゾーン間を、左へ1つずつ動く。
- **Digital mapping**：同じ。対象作物のハイライト（A1）。
- **Figma implementation notes**：符号付き数値は、テキストプロパティ。裏面は雲の模様。

## Component: EventTimeline（今・次・次の次）

- **Purpose**：先読み可能なイベント列（RULE §9.1）。
- **Required information**：3ゾーンのEventCard（今／次／次の次）、ゾーンの見出し、ブラケット（今＝有効、次・次の次＝予告）。
- **Information hierarchy**：今 ＞ 次 ＞ 次の次。
- **States / Variants**：`rowLength=0|1|2|3`（終盤は短くなる）、`labelMode=physical|digital`（ラウンド番号併記）。
- **Child components**：`EventCard` ×0〜3、ゾーンの枠（空のときは「なし」）。
- **Physical dimensions**：3ゾーン、各200×280 px（`EventCard`に余白）。合計 600×280 px。
- **Layout constraints**：左から、今｜次｜次の次。**今**は有効ブラケットの内側（継続中の隣）。予告は破線。
- **Visibility requirements**：有効と予告が、枠（実線／破線）と見出しで区別される（REC-09）。
- **Interaction notes**：物理：進行係が、毎ラウンド左へ詰める。
- **Digital mapping**：同じ。ラウンド番号（R5、R6、R7）を併記。
- **Figma implementation notes**：空のゾーンは、斜線の「なし」表示（列の長さは常に6ゾーン）。

## Component: ActiveEventArea（継続中）

- **Purpose**：直前ラウンドの「今」だったカードが、もう1ラウンド有効であることを示す（RULE §4、最大1枚）。
- **Required information**：EventCard（0〜1枚）、見出し「継続中」、有効ブラケット。
- **Information hierarchy**：カード ＞ 見出し。
- **States / Variants**：`occupied=true|false`。
- **Child components**：`EventCard` ×0〜1。
- **Physical dimensions**：200×280 px（1ゾーン）。
- **Layout constraints**：**EventTimelineの「今」の左隣**。捨て札とEventTimelineの間。
- **Visibility requirements**：有効ブラケット（太い実線）の内側。「今ラウンドの収入に効く（最大2枚）」の見出しを、「今」と共有。
- **Interaction notes**：更新時に、継続中→捨て札、今→継続中。
- **Digital mapping**：同じ。
- **Figma implementation notes**：`EventRail` の中で、`EventTimeline` と隣接して、1つのブラケットで囲む。

## Component: EventRail（複合）

- **Purpose**：イベントのコンベア全体（REC-08）。
- **Required information**：6ゾーン（捨て札｜継続中｜今｜次｜次の次｜山札）、ブラケット2つ、残りのイベント枚数。
- **Information hierarchy**：有効ブラケット（継続中・今）＞ 予告 ＞ 山札・捨て札。
- **States / Variants**：`mode=basic|advanced`、`phase=eventUpdate|other`（更新中の強調）。
- **Child components**：`EventDiscard`、`ActiveEventArea`、`EventTimeline`、`EventDeck`。
- **Physical dimensions**：320×100 mm（1280×400 px）。6ゾーン×200 px＋余白。
- **Layout constraints**：時間の方向は、過去が左・未来が右。全カードが、毎ラウンド左へ1つ進む。
- **Visibility requirements**：L0で、ブラケットの違い（実線／破線）が判別できる。
- **Interaction notes**：物理：進行係が1ラウンドに最大4枚を動かす（§9.3）。
- **Digital mapping**：同じ。更新アニメーション（A1）。
- **Figma implementation notes**：矢印（←）で、左への流れを注釈する。

## Component: EventDeck / EventDiscard

- **Purpose**：イベントの山札（枚数のみ公開）と捨て札（公開）。
- **Required information**：EventDeck：枚数（`numeral/m`）、再シャッフルのリマインダー。EventDiscard：カードの一覧（公開）。
- **Information hierarchy**：枚数（Deck）／先頭のカードと枚数（Discard）。
- **States / Variants**：EventDeck：`count=0..16`、`reshuffleReminder=true|false`（山札が0枚で累計が上限未満のとき。上級）。EventDiscard：`count=0..18`。
- **Child components**：`EventCard`（背面／前面）。
- **Physical dimensions**：45×63 mm（180×252 px）＋トレイ（雲の模様）。
- **Layout constraints**：EventRailの右端（山札）と左端（捨て札）。作物のトレイと、形・サイズが異なる（REC-17）。
- **Visibility requirements**：山札の中身は非公開（背面）。捨て札は表向き。
- **Interaction notes**：物理：進行係が補充・廃棄。
- **Digital mapping**：山札は枚数、捨て札は一覧（絞り込み）。
- **Figma implementation notes**：トレイの縁に、`cloud` のパターン。

## Component: CropDeck / CropDiscard

- **Purpose**：作物の山札（枚数のみ公開）と捨て札（公開）。
- **Required information**：CropDeck：枚数（`numeral/m`）。CropDiscard：カードの一覧。
- **Information hierarchy**：枚数／先頭のカード。
- **States / Variants**：CropDeck：`count=0..40`。CropDiscard：`count=0..40`。`empty=true`（RULE §8：山札も捨て札も空のとき、補充できない）。
- **Child components**：`CropCard`（背面／前面）。
- **Physical dimensions**：63×88 mm（252×352 px）＋トレイ（畝の模様）。
- **Layout constraints**：共通エリアの端。イベントのトレイと分離（REC-17）。
- **Visibility requirements**：山札の中身は非公開。
- **Interaction notes**：物理：購入のたびに補充。植え替えのたびに、前作カードを捨て札へ。
- **Digital mapping**：同じ。正確な枚数を表示（**OQ-06**）。
- **Figma implementation notes**：トレイの縁に `furrow` のパターン。

## Component: CropMarket

- **Purpose**：作物の場札（3〜4人：3枚、5人：4枚。RULE §3.2）。
- **Required information**：場札のCropCard、空の枠（補充できないとき）。
- **Information hierarchy**：カードの識別 ＞ 栽培コスト ＞ 時間軸 ＞ 収入。
- **States / Variants**：`playerCount=3|4|5`（3・4は3枠。5は4枠）、`slotEmpty=0..4`。
- **Child components**：`CropCard`（`layout=B`、`dualOrientation=true`）×3〜4。
- **Physical dimensions**：4枠で 264×88 mm（1056×352 px）。枠の隙間 16 px。
- **Layout constraints**：枠は固定。補充は、購入した枠に。
- **Visibility requirements**：全員がカードの識別と栽培コストを読める（両向き）。
- **Interaction notes**：手番プレイヤーが選ぶ。
- **Digital mapping**：同じ。選択で支払い合計をプレビュー（A1）。
- **Figma implementation notes**：使用しない枠（3・4人の4枠目）は、グレーアウト。

---

## Component: CoinArea

- **Purpose**：プレイヤーの所持コインの置き場（RULE §10：公開情報）。
- **Required information**：コイン（額面1／5／10／50）、（デジタル）総額。
- **Information hierarchy**：総額 ＞ 額面別の内訳。
- **States / Variants**：`mode=basic|advanced`（50は上級のみ）、`view=physical|digital`。
- **Child components**：額面別の窪み、`PlayerIdentity`（小）。
- **Physical dimensions**：約380×35 mm（1520×140 px、PlayerBoardの手前の帯）。
- **Layout constraints**：手前の帯。左端のSlotCover（次の取得コスト）と、視野に収まる。
- **Visibility requirements**：所有者に読める。他者には、CoinLedgerで総額が読める。
- **Interaction notes**：受取・支払い。**Digital mapping**：総額の常時表示。
- **Figma implementation notes**：デジタルでは、総額を `numeral/xl`。

## Component: CoinLedger

- **Purpose**：各プレイヤーの所持コインの総額を、公開して比較する台帳（REC-15）。
- **Required information**：プレイヤー識別、総額（3桁）。
- **Information hierarchy**：総額 ＞ プレイヤー識別。
- **States / Variants**：`playerCount=3|4|5`、`updateTiming=roundEnd|live`（**OQ-02**）。
- **Child components**：`PlayerIdentity` ×N、3桁の数値（ダイヤル）。
- **Physical dimensions**：200×130 mm（800×520 px）。
- **Layout constraints**：行＝プレイヤー。順序は座席順（手番順は`TurnOrderTrack`）。
- **Visibility requirements**：L1で総額が読める（`numeral/xl`）。
- **Interaction notes**：物理：各プレイヤーが、ラウンド末に自分の行を更新（収入フェーズの手順4）。
- **Digital mapping**：常に最新の数値。
- **Figma implementation notes**：ALT-13（PlayerPawn）では、`TurnOrderTrack` に統合。

## Component: TurnOrderTrack

- **Purpose**：手番順の表示（RULE §4：R1＝座席順、R2〜＝所持コイン昇順、同数は前ラウンドの順）。
- **Required information**：順位1〜N、各順位のプレイヤー、現在の手番。
- **Information hierarchy**：現在の手番 ＞ 順序 ＞ プレイヤー識別。
- **States / Variants**：`playerCount=3|4|5`、`round=first|later`、`currentIndex=1..N`。
- **Child components**：`PlayerIdentity`（マーカー）×N。
- **Physical dimensions**：200×60 mm（800×240 px）。
- **Layout constraints**：左から、手番の順。
- **Visibility requirements**：現在の手番が、L0で判別できる（太い枠）。
- **Interaction notes**：物理：進行係が、収入フェーズ後にマーカーを並べ替える（同数は前の順序を維持）。
- **Digital mapping**：自動で並べ替え。
- **Figma implementation notes**：`round=first` の場合は、座席順（スタートプレイヤーから時計回り）。

---

## Component: RoundIndicator

- **Purpose**：現在ラウンド、残りラウンドの表示（REC-14）。
- **Required information**：R個のセル（番号付き）、現在のセル、残りのセル。
- **Information hierarchy**：現在 ＞ 残りセル数 ＞ 過去。
- **States / Variants**：`mode=basic|advanced`（10または18セル）、`current=1..R`。
- **Child components**：セル（`cell=past|current|future`）。
- **Physical dimensions**：最大 270×40 mm（1080×160 px。18セル×60 px）。
- **Layout constraints**：横一列。左が過去、右が未来。**時間の文法**（§16.2）。
- **Visibility requirements**：現在のセルは、太い枠＋マーカーで、L0で判別できる。
- **Interaction notes**：物理：進行係がマーカーを進める。
- **Digital mapping**：自動。残りラウンド数を併記（A1）。
- **Figma implementation notes**：特定のラウンド番号に、ルール上のイベントを固定表示しない（§16.3）。

## Component: PhaseRail

- **Purpose**：フェーズ（イベント更新／手番／収入）と、収入フェーズの手順（REC-07）の表示。
- **Required information**：3フェーズ、収入の4ステップ（計数→受取→更新→台帳）、現在の位置。
- **Information hierarchy**：現在のフェーズ ＞ 収入の現在のステップ。
- **States / Variants**：`phase=eventUpdate|turns|income`、`incomeStep=1..4`、`isRoundOne=true|false`（1ラウンド目は①を淡色）。
- **Child components**：フェーズのボックス、ポインタ。
- **Physical dimensions**：360×60 mm（1440×240 px）。
- **Layout constraints**：横一列。収入は、下にサブステップを縦に展開。
- **Visibility requirements**：ポインタは、L0で判別できる。
- **Interaction notes**：物理：進行係がポインタを移す。
- **Digital mapping**：自動進行（A1）。
- **Figma implementation notes**：RoundIndicatorと隣接。

## Component: SeasonIndicator

- **Purpose**：マッチ全体の進行（シーズン数と現在）の表示。
- **Required information**：シーズン数のピップ、現在のシーズン、スタートプレイヤー。
- **Information hierarchy**：現在のシーズン ＞ 総シーズン数。
- **States / Variants**：`seasons=1..5`、`current=1..seasons`。
- **Child components**：ピップ、`StartPlayerMarker`（参照）。
- **Physical dimensions**：200×40 mm（800×160 px）。
- **Layout constraints**：TurnOrderTrackの下（§22.3の共通エリア座標）。
- **Visibility requirements**：現在のシーズンが塗り。
- **Interaction notes**：物理：シーズンごとに更新。**Digital mapping**：ヘッダー。
- **Figma implementation notes**：ピップは、時間の文法（塗り／中抜き）。

## Component: ScoreBoard

- **Purpose**：シーズンごとの終了時コインと合計の記録（RULE §11）。
- **Required information**：行＝プレイヤー、列＝シーズン＋合計。
- **Information hierarchy**：合計 ＞ シーズンごとの値。
- **States / Variants**：`playerCount=3|4|5`、`seasons=1..5`、`filled=0..seasons`。
- **Child components**：`PlayerIdentity`、セル。
- **Physical dimensions**：240×80 mm（960×320 px）。
- **Layout constraints**：共通エリアの隅（または西端）。
- **Visibility requirements**：合計が、L1で読める。
- **Interaction notes**：物理：シーズン終了時に、進行係が記録。
- **Digital mapping**：折りたたみのヘッダー表示。
- **Figma implementation notes**：`ASM-06`（公開情報として扱う）を注釈。

## Component: ReadingLegend / Bank / ExcludedPocket（補助）

| コンポーネント | 内容 |
|---|---|
| ReadingLegend | §10.6の読取規則を印刷。PlayerBoardの片隅（380×35 mm内）。`view=physical` のみ |
| Bank | コイン・トークンの共通の置き場。共通エリアの端（西端）。物理のみ（デジタルは自動） |
| ExcludedPocket | 基本モードで除外する6枚の置き場（裏向き、非公開）。物理のみ。準備時のみ |

---

# 21. Figma File / Page Structure

**方針**：初期段階では、ファイルを細分化しない。**1ファイル・複数ページ**とする。

| ページ | 内容 |
|---|---|
| **00 README** | タグ凡例（§0）、ID体系、ASM一覧、更新履歴、注釈の記法（`RULE`／`REQ`／`REC`／`ALT`／`ASM`／`OQ` のラベルの付け方） |
| **01 Foundations** | 色スタイル・文字スタイル・余白（§19.4）、アイコン（砂時計・穂・月・▲▼・形状 ○⬡◇・●■▲◆★）、時間の文法のセル（中抜き／塗り／斜線）、タイプ帯の模様、作物グリフ10種、符号化チャネルの対応表（§4.4）、寸法表（§19.3） |
| **02 Crop Cards** | `CropCard`：10作物×`layout=A/B/C`、裏面、`mini`、`dualOrientation`。3案の比較ボード（同じ3作物を、A・B・Cで並置）。作物グリフ・タイプ帯 |
| **03 Event Cards** | `EventCard`（40種＋裏面）、`EventTimeline`、`ActiveEventArea`、`EventRail`、終盤の短縮状態（`rowLength=0〜3`） |
| **04 Player Area** | `TokenWaiting／Harvest／Cooldown`、`TokenLane`、`FarmSlot` の5状態、`SlotCover`、`PlayerBoard`、`ReadingLegend`。**ライフサイクルの図**（F3の時系列。§22.4） |
| **05 Shared Area** | `SeasonCropPanel`（10作物）、`CropMarket`、山札・捨て札、`RoundIndicator`、`PhaseRail`、`SeasonIndicator`、`TurnOrderTrack`、`CoinLedger`、`ScoreBoard`、共通エリアの組み立て（720×440 mm） |
| **06 Table Layout** | 3人・4人・5人のトップビュー（実寸のフレーム）。**視点別の見え方**（各席から見た回転フレーム） |
| **07 Scenarios** | フィクスチャ（F1〜F3）を、部品を組み合わせて描画したもの。走査テスト、遮蔽テスト、グレースケール・色覚シミュレーションのテスト用フレーム |
| **08 Digital Mapping** | デスクトップとモバイルの概念図、収穫マトリクス、補助レベル（A0〜A2）の表示差 |
| **09 Review** | セルフレビューのチェックリスト（§26）、OQ・Dの一覧（§23、§24）、Cursorの逸脱報告 |

**運用規約**

| 項目 | 内容 |
|---|---|
| フレーム名 | `Page番号/カテゴリ/名前`（例：`04/FarmSlot/state=harvesting`） |
| コンポーネント | コンポーネントセットにまとめ、プロパティ名は§20のとおり |
| スタイルと変数 | 色・文字は、スタイル（§19.4）を使う。Figma Variablesに、`assistLevel`（A0/A1/A2）、`viewer`（owner/opponent）、`playerCount`（3/4/5）、`mode`（basic/advanced）を持たせる |
| 注釈 | 仕様の根拠（RULE §番号、REQ、REC-xx）を、コンポーネントの説明欄に書く |

---

# 22. Cursor Implementation Instructions

## 22.1 原則

**DO**

1. この文書の**数値と状態の定義**を、そのまま使う（作物のパラメータは§7.6とRULE §12。IncomeLadderは§20の表）。
2. 各コンポーネントは、§20の仕様（Required regions、Variants、Required visible properties、Constraints）に従う。
3. **ASSUMPTION** の値を使ったら、該当フレームに注釈（`ASM-xx`）を付ける。
4. 仕様が決めていないことを、自分で決めたときは、**Review ページに「逸脱・補完の報告」として記録**する（大きな設計判断をしない）。
5. 色・文字は、スタイルを使う。

**DON'T**

1. ゲームのルール・数値（作物のパラメータ、コスト、イベントの数値など）を変更しない。
2. 画風（写実・和風など）を決めない。グリフは仮のシルエットでよい。
3. 状態を色だけで区別しない（形・アイコン・位置を必ず併用）。
4. FarmSlotの2つ以上のレーンに、同時にトークンを置かない（**不変条件**）。
5. トークンをカードの上に置かない。
6. 12 px以下の文字を、必須情報に使わない。
7. `OPEN QUESTION` や `D-xx` を、勝手に決めない。§22.7の既定を使い、報告する。

## 22.2 構築の順序と受け入れ条件

| 順 | 作業 | 受け入れ条件 |
|---|---|---|
| 1 | Foundations（スタイル、アイコン、時間の文法のセル、10種のグリフ、タイプ帯） | グレースケールで、待／収／休のアイコンと形が区別できる |
| 2 | Token 3種、`TokenLane`、`LaneBlock` | 各レーンの穴が4つ。形が、トークンと一致 |
| 3 | `CropCard`（`layout=B`、10作物）→ A・C | §7.6の表と、カードの値が**すべて一致**。時間軸のセル数が、W＋H＋C（C=0は「連作可」） |
| 4 | `SlotCover`、`FarmSlot`（5状態）、`PlayerBoard` | 5スロットが、380×225 mm（1520×900 px）に収まる。トークン最大（各レーン4個）でも、カードに重ならない |
| 5 | `EventCard`（40種）、`EventTimeline`、`ActiveEventArea`、`EventRail` | 6ゾーンの位置が§22.3のとおり。有効ブラケットと予告ブラケットが、実線／破線で区別できる |
| 6 | `SeasonCropPanel`（10作物）、`IncomeLadder`、`HarvestTally`、`ModifierWell` | IncomeLadderが§20の表と一致 |
| 7 | `CropMarket`、山札・捨て札、`RoundIndicator`、`PhaseRail`、`SeasonIndicator`、`TurnOrderTrack`、`CoinLedger`、`ScoreBoard`、`CoinArea` | 共通エリア（720×440 mm）に収まる |
| 8 | 卓のレイアウト（3・4・5人） | §22.3の座標に、実寸で配置。視点別の回転フレームを作る |
| 9 | フィクスチャ F1〜F3 の描画 | §22.4の期待値と、描画結果が一致 |
| 10 | Digital Mapping | 収穫マトリクス、補助レベルA0〜A2の差 |
| 11 | テスト（§22.5）の実施 | すべて合格 |
| 12 | Reviewページの記入 | 逸脱・補完の報告、未決事項の一覧 |

## 22.3 座標（px。1 mm = 4 px。ASSUMPTION）

**EventRail**（1280×400。ゾーン200幅、左余白40）：捨て札 x=40、継続中 x=240、今 x=440、次 x=640、次の次 x=840、山札 x=1040。有効ブラケット：継続中〜今（x=240〜640）を、実線で囲む。予告ブラケット：次〜次の次（x=640〜1040）を、破線で囲む。

**PlayerBoard の内部**：ヘッダー y=0〜40。FarmSlot ×5：x=68、348、628、908、1188（ピッチ280）、y=40。下の帯：y=760〜900。

**卓のフレーム**（4800×3800）

| 配置 | 4人 | 3人 | 5人 |
|---|---|---|---|
| 共通エリア（2880×1760） | x=960、y=1020 | 同左 | 同左 |
| 南 | 板1：x=1640、y=2900 | 板1：x=1640、y=2900 | 板1：x=800、y=2900／板2：x=2480、y=2900 |
| 北（180°回転） | 板3：x=1640、y=0 | 板2：x=800、y=0／板3：x=2480、y=0 | 板3：x=800、y=0／板4：x=2480、y=0 |
| 西（90°回転。回転後 900×1520） | 板2：x=0、y=1140 | — | 備品置き場（Bank、山札・捨て札、得点表） |
| 東（−90°回転。回転後 900×1520） | 板4：x=3900、y=1140 | — | 板5：x=3900、y=1140 |

（3人の西・東は、Bank・得点表などの置き場として使用してよい）

**共通エリア（2880×1760）の内部**（原点は共通エリアの左上。§11.7の配置に対応）

| コンポーネント | x, y | サイズ |
|---|---|---|
| EventRail | 0, 0 | 1280×400 |
| RoundIndicator | 1400, 0 | 1080×160 |
| PhaseRail | 1400, 160 | 1440×240 |
| SeasonCropPanel ×4 | x＝0、280、560、840、y＝440 | 各252×520 |
| CoinLedger | 1200, 440 | 800×520 |
| TurnOrderTrack | 2080, 440 | 800×240 |
| SeasonIndicator | 2080, 700 | 800×160 |
| CropMarket | 0, 1000 | 1056×352 |
| Bank | 1200, 1000 | 1200×352 |
| CropDeck／CropDiscard | 0, 1392／280, 1392 | 各252×352 |
| EventDeck／EventDiscard | 560, 1392／760, 1392 | 各180×252 |
| ExcludedPocket | 960, 1392 | 240×252 |
| ScoreBoard | 1800, 1392 | 960×320 |

（5人のときは、山札・捨て札・ExcludedPocket・ScoreBoard・Bankを、西端の備品置き場に移してよい。CropMarketは4枠のまま）

## 22.4 フィクスチャ（テスト用の状態。RULEの状態機械と整合するように作成済み）

### F1：基本モード・4人・第5ラウンド（手番フェーズの開始時）

- **今シーズンの4作物**：ラディッシュ、ジャガイモ、ネギ、トマト。
- **手番順**（所持コイン昇順）：P3（8）→ P4（11）→ P1（12）→ P2（15）。
- **場札**（3枚）：ジャガイモ、トマト、ネギ。作物の山札23枚、捨て札2枚（ラディッシュ×2）。
- **イベント**：継続中＝「ネギ −2」、今＝「トマト ＋2」、次＝「ラディッシュ −4」、次の次＝「ジャガイモ ＋4」。イベント山札3枚、捨て札3枚、除外6枚（非公開）。**有効なのは、ネギ −2とトマト ＋2**。

| プレイヤー | スロット | 作物 | 白 | 緑 | 赤 | `state` |
|---|---|---|---|---|---|---|
| P1 | 1 | トマト | 0 | 4 | 0 | harvesting |
| P1 | 2 | ネギ | 0 | 4 | 0 | harvesting |
| P1 | 3 | ラディッシュ | 0 | 1 | 0 | harvesting |
| P1 | 4〜5 | — | — | — | — | unacquired |
| P2 | 1 | ジャガイモ | 0 | 0 | 4 | **idleCooling**（同種は第9ラウンドから） |
| P2 | 2 | ネギ | 0 | 3 | 0 | harvesting |
| P2 | 3 | トマト | 2 | 0 | 0 | waiting（初収入は第7ラウンド終了時） |
| P2 | 4〜5 | — | — | — | — | unacquired |
| P3 | 1 | ラディッシュ | 0 | 0 | 0 | **idleFree** |
| P3 | 2 | トマト | 0 | 3 | 0 | harvesting |
| P3 | 3 | トマト | 1 | 0 | 0 | waiting（初収入は第6ラウンド終了時） |
| P3 | 4〜5 | — | — | — | — | unacquired |
| P4 | 1 | ジャガイモ | 0 | 2 | 0 | harvesting |
| P4 | 2 | ネギ | 0 | 2 | 0 | harvesting |
| P4 | 3〜5 | — | — | — | — | unacquired |

**期待値（描画と照合する）**：収穫中の農地数と、1農地あたりの収入（RULE §7：`max(最低保証, 基本収入 −(N−1) ＋ 補正)`）。

| 作物 | 収穫中の農地（N） | 補正 | 1農地あたりの収入 |
|---|---|---|---|
| トマト（9、4） | P1-1、P3-2（N＝2） | ＋2 | 9 −1 ＋2 ＝ **10** |
| ネギ（8、3） | P1-2、P2-2、P4-2（N＝3） | −2 | 8 −2 −2 ＝ **4** |
| ラディッシュ（7、3） | P1-3（N＝1） | 0 | **7** |
| ジャガイモ（9、4） | P4-1（N＝1） | 0 | **9** |

**この収入フェーズ後（第6ラウンドの開始時）の期待値**：P1：トマト緑3、ネギ緑3、ラディッシュ＝idleFree（C=0）。P2：ジャガイモ赤3、ネギ緑2、トマト白1。P3：ラディッシュidleFree（変化なし）、トマト緑2、もう一方のトマトは**白0・緑4**（緑を置くが、そのラウンドは収入なし）。P4：ジャガイモ緑1、ネギ緑1。

### F2：基本モード・第9ラウンド（イベント列の短縮）

継続中＝第8枚目、今＝第9枚目、次＝第10枚目、**次の次＝なし**（斜線の「なし」）。イベント山札0枚、捨て札7枚。第10ラウンドでは、次も「なし」になる。

### F3：ライフサイクルの時系列（1つのスロット。ジャガイモ：W2・H2・C4。第3ラウンドに植える）

| ラウンド開始時 | 状態 | 白 | 緑 | 赤 | 備考 |
|---|---|---|---|---|---|
| R3（植えた直後） | waiting | 2 | 0 | 0 | |
| R4 | waiting | 1 | 0 | 0 | |
| R5 | harvesting | 0 | 2 | 0 | R4終了時に緑を置いた。**R5終了時が初収入** |
| R6 | harvesting | 0 | 1 | 0 | |
| R7 | idleCooling | 0 | 0 | 4 | 別の作物はR7から可 |
| R8 | idleCooling | 0 | 0 | 3 | |
| R9 | idleCooling | 0 | 0 | 2 | |
| R10 | idleCooling | 0 | 0 | 1 | |
| R11 | idleFree | 0 | 0 | 0 | **同じ作物はR11から可**（RULE §6.4の例と一致） |

追加：ラディッシュ（W1・H1・C0）を第2ラウンドに植える場合：R2＝waiting（白1）、R3＝harvesting（緑1）、R4＝idleFree。

## 22.5 テスト（Figma上での確認）

| # | テスト | 合格基準 |
|---|---|---|
| T1 | 状態の遠距離判定 | 4人卓のフレームを25%に縮小して、各スロットの「緑あり」を判別できる |
| T2 | 180°回転 | 相手側の視点フレーム（180°回転）で、収レーンが中央のまま、緑の有無を判別できる |
| T3 | グレースケール | 待／収／休が、形とアイコンで区別できる |
| T4 | 色覚シミュレーション（1型・2型・3型） | 同上。状態・タイプ・極性が、色なしで区別できる |
| T5 | 遮蔽 | 各レーンに4個ずつ（計12個。実際は1レーンのみ使用）を置いても、カードの数値に重ならない |
| T6 | 数値の一致 | すべての作物・IncomeLadderの値が、§7.6と§20の表と一致 |
| T7 | 不変条件 | どのFarmSlotのバリアントも、2つ以上のレーンが非空になっていない |
| T8 | 文字の大きさ | 必須情報の文字が、16 px以上 |
| T9 | イベントの区別 | 有効（継続中・今）と予告（次・次の次）が、枠と見出しで区別できる。F2で、短縮が自然に見える |
| T10 | フィクスチャの照合 | F1の収入の期待値、F3の時系列が、描画と一致 |
| T11 | 走査 | F1の卓の視点別フレームで、「トマトを収穫中の農地は何枚か」を、レーンとグリフから数えられる |

## 22.6 Cursorからの報告（Reviewページ）

1. 作成したコンポーネントとバリアントの一覧。
2. 使用したASSUMPTIONの一覧。
3. 仕様にないため補完した点（その理由）。
4. §22.5のテストの結果。
5. 仕様の矛盾・不明点（発見したもの）。

## 22.7 未決事項の既定値（人間が決めるまで、これを使う）

| 決定 | 既定 |
|---|---|
| D-01 トークン方式 | T2（形状ロック付きレーン）。T3は、Alternativeとして別ページに作る |
| D-02 HarvestTally | 作物パネルにトラックを**印刷**（`tallyMode=pegs`）。使用は任意 |
| D-03 コイン | C2（手元のコイン＋CoinLedger） |
| D-04 手番順 | `TurnOrderTrack` と `CoinLedger` を別部品 |
| D-05 EventRail | 6ゾーン |
| D-06 イベントカード | ミニサイズ（45×63 mm） |
| D-07 向き | 両向き印刷 |
| D-08 作物の識別 | グリフ＋頭字＋タイプ帯 |
| D-09 補助レベル | A1 |
| D-10 進行係 | スタートプレイヤーが兼任（注釈として表示） |

---

# 23. Open Questions

形式：現在のルールでは○○となっており、物理UIでは△△という問題が生じる可能性がある。**ルールは変更しない**。暫定対応は、UIのみの扱い。

| ID | 現在のルール／状況 | 生じうる問題 | 暫定対応（UIのみ） |
|---|---|---|---|
| OQ-01 | RULE §6.3：前作カードは、新しい作物を植えるまで農地に「置いたまま」にする | 空きの状態を積極的に見せるために、カードをずらす・向きを変えると、「置いたまま」の解釈に触れる可能性がある。追加のフラグは、トークンとの二重の真実になる | 物理では、追加のフラグを設けない（REC-03）。ずらしはALT-03（任意）。デジタルは、減光で表示 |
| OQ-02 | RULE §10：所持コインは公開情報。手番順は、直前ラウンドの収入フェーズ後の値を使う（§4） | CoinLedgerを、ラウンド末のみ更新するか、支払いのたびに更新するかで、手元のコインと台帳の値が食い違う時間ができる | ラウンド末に更新（REC-15）。デジタルは常時最新 |
| OQ-03 | ルールに「進行係」の役割はない。イベント更新・収穫の集計・台帳の更新は、誰かが行う必要がある | 担当が決まらないと、更新の忘れが起きる。負担が偏る | スタートプレイヤーが兼任（D-10の案）。デジタルは自動 |
| OQ-04 | RULE §7：収入は「収入フェーズの開始時の状態」で判定する。トークンを更新する順序は、明記されていない | トークンを先に更新すると、緑を置いたばかりの農地を、収穫中と数えてしまう | UIの手順として、計数（凍結）→受取→更新→台帳の順を印刷（REC-07）。ルール本文への明記は、GRC-02 |
| OQ-05 | RULE §4：2ラウンド目以降の手番順は、所持コインの少ない順。同数は前ラウンドの順を維持 | 並べ替えの計算と、前ラウンドの順の記憶が必要。誰が並べ替えるか | TurnOrderTrack（REC-16）。前ラウンドの順は、トラックの状態として残る |
| OQ-06 | RULE §10：山札は枚数のみ公開 | 物理では、枚数を確認するために山札を数える操作が必要になり、順序や中身を見ない工夫が要る | トレイの側面に枚数の目盛り（目安）。正確な枚数は、デジタルで表示 |
| OQ-07 | 得点表の公開性は、RULE §10に明記されていない | 進行係だけが見る配置にするか、全員が見える配置にするかが決まらない | 公開として扱う（ASM-06） |
| OQ-08 | RULE §2：トークンは各約40個（プロトタイプ用の目安） | 5人卓の最悪ケースでは足りない可能性がある（§8.7） | 試作で確認。UIは、数を変えない |
| OQ-09 | RULE §2、§6：トークンの色は「白／緑／赤」 | 色覚多様性への配慮で、実際の色相を変える必要があるかもしれない | 色名はラベルとして保持。実色はCVD配慮の値（§19.4）。形・アイコンが主符号 |
| OQ-10 | カード寸法は、ルールで決まっていない | 作物カード63×88、イベントカード45×63は、仮定（ASM-01、ASM-02） | 人間が決める（D-06）。Figmaは、寸法をパラメータ化 |
| OQ-11 | デジタルの補助の範囲は、ルールで決まっていない。RULE §14.7は、イベントを織り込める人の優位を問題にしている | A1・A2の補助は、計算力の差に影響しうる。オンラインで、補助のレベルが人ごとに違うと、不公平になる | マッチ内の全員に、同じ補助レベルを適用（§17.2、D-09） |
| OQ-12 | RULE §8：イベントの捨て札を山札に戻した時点で、中身は非公開に戻る | 物理では、捨て札の内容を覚えている人が有利になる可能性がある（公開情報の暗記）。デジタルは、記録が残る | 物理・デジタルとも、捨て札は公開のまま。デジタルでは、再シャッフル後の山札の内容を、UIで示さない |
| OQ-13 | RULE §3.2：5人のとき、場札は4枚 | CropMarketの幅が増え、共通エリアが混雑する | 西端に備品を逃がす（REC-19）。Marketは4枠（264 mm） |
| OQ-14 | RULE 付録C：背景設定（季節との対応）は、非公開の設定 | UIに、月や日付を出すと、ルールの一部のように見える | 物理には出さない。デジタルは、演出にとどめる |
| OQ-15 | カードの数値は、印刷の向きが限られる | 東西に座るプレイヤーには、上下の両向き印刷が効かない（90°回転して見える） | グリフと符号（▲▼）は回転に強い。数値は、D-07の判断（座席別リファレンスの併用） |
| OQ-16 | RULE §1：CPU席も、通常の席として扱う | CPUの手番の表示（思考中の表示、直前の行動のログ）が、ルールにない | ログと現在の手番の強調。詳細はデジタル設計時に |
| OQ-17 | RULE §6、§7：将来の競合の判断は、全農地の走査と、時間の重なりの暗算が必要（ケースC） | 物理UIでは、推奨案を採用しても認知負荷が高いまま（§5.3） | 収穫予定ストリップ（ALT-05、任意）。デジタルはA2で対応（D-09） |
| OQ-18 | レーンを、カードの遠側に置く（REC-01） | 所有者が、遠側のレーンにトークンを置くために、腕を約90 mm伸ばす。毎ラウンドの操作の負担になる可能性 | 試作で確認。ALT-08（手前側）と比較 |
| OQ-19 | 山札・捨て札は、共通エリアの端（進行係の側）に置く | 手番プレイヤーが補充・廃棄する操作で、遠い席（5人時の東）からは約1.0 m離れる | 山札・捨て札を、手番プレイヤーが届く位置に置く工夫を、試作で確認。デジタルは影響なし |

---

# 24. Decisions Required from Human Designer

Figma制作の前に、人間が判断したほうがよい分岐のみを載せる。**ルール変更を要する事項は含めない**（§25）。

## D-01：トークンの方式

| 項目 | 内容 |
|---|---|
| Decision | 農地の状態を、どの物理部品で表すか |
| Option A | **T2**：形状ロック付きの3レーン（待｜収｜休）に、白・緑・赤のトークンを置く |
| Option B | **T3（ALT-01）**：作物カードの時間軸に、進行マーカーを1個置く |
| Option C | **T1**：3色の円盤を、農地の脇にまとめて置く |
| Trade-offs | A：ルールと同一で、位置による判定が強い。操作量は平均14.4回。B：操作量が約4割減るが、状態機械の記述を書き換える必要がある（GRC-04）。マーカーが1セルを隠す。C：単純だが、遠距離判定と誤操作に弱い |
| Your recommendation | **A** |
| Why | ルール本文と整合し、他プレイヤーからの判定（G1）と、誤操作防止に最も強い |
| Impact on Figma | Aの場合、§20のとおり。Bの場合、`CropCard` に `marker` プロパティ、`TokenLane` を廃止、`FarmSlot` の構造を変更 |

## D-02：HarvestTally（収穫集計）を、作物パネルに含めるか

| 項目 | 内容 |
|---|---|
| Decision | 作物パネルに、収穫集計のペグ（進行係が設定）を付けるか |
| Option A | パネルにペグのトラックを**印刷**し、使用は卓で選ぶ |
| Option B | パネルは、ラダーとModifierWellのみ（ペグなし） |
| Option C | パネルなし（ALT-11） |
| Trade-offs | A：収穫の走査（ケースC・D）を減らせるが、進行係の操作が約4回/ラウンド増える。B：操作は増えないが、走査は残る。C：部品は減るが、負荷は下がらない |
| Your recommendation | **A**（4〜5人では使用、3人では任意） |
| Why | 5人時は他者の農地が20スロットになり、走査の負荷が最大（§14.3）。使用を任意にすれば、操作の増加を、卓で選べる |
| Impact on Figma | `SeasonCropPanel` の `tallyMode=pegs|none` の両方を作る |

## D-03：所持コインの公開方法

| 項目 | 内容 |
|---|---|
| Decision | 公開情報の所持コインを、どう読み取れるようにするか |
| Option A | **C2**：手元のコイン＋CoinLedger（ラウンド末に更新） |
| Option B | **C1**：手元のコインのみ |
| Option C | **C3**：コインを廃止して、カウンタのみ（GRC-03が必要） |
| Trade-offs | A：比較と手番順の判定が容易。更新の操作が、プレイヤーごとに1回/ラウンド増える。B：操作は増えないが、他者の総額が読めない。C：操作量は最小だが、ルールの変更 |
| Your recommendation | **A** |
| Why | 手番順（コイン昇順。RULE §4）の判定に、他者の総額が必要 |
| Impact on Figma | `CoinLedger` を作る。Bの場合は不要。Cの場合は `CoinArea` を廃止 |

## D-04：手番順の表示

| 項目 | 内容 |
|---|---|
| Decision | CoinLedgerとTurnOrderTrackを、別部品にするか、統合するか |
| Option A | 別部品（`CoinLedger` ＋ `TurnOrderTrack`） |
| Option B | **ALT-13**：PlayerPawn（コインダイヤル付き）で統合 |
| Option C | `CoinLedger` のみ（手番順は、各自が計算） |
| Trade-offs | A：構造が単純。更新が2回。B：更新の操作が減るが、ダイヤル付きの部品が必要。C：部品が減るが、並べ替えの誤りが起きやすい |
| Your recommendation | **A**（最初の試作） |
| Why | Figmaの第一稿が単純。Bは、プレイテストの後に評価できる |
| Impact on Figma | Bの場合、`PlayerPawn` を追加し、`CoinLedger` を廃止 |

## D-05：EventRailの形

| 項目 | 内容 |
|---|---|
| Decision | イベントの列を、どの形で並べるか |
| Option A | 6ゾーン（捨て札｜継続中｜今｜次｜次の次｜山札） |
| Option B | 4ゾーン（継続中｜今｜次｜次の次）。山札・捨て札は別置き（ALT-09） |
| Option C | 縦方向の列 |
| Trade-offs | A：全カードが1方向に流れるコンベアで、直感的。幅が約320 mm必要。B：共通エリアは小さくなるが、コンベアの連続性が弱まる。C：横幅は節約できるが、時間の方向の文法（左が過去、右が未来）と衝突 |
| Your recommendation | **A** |
| Why | 更新の手順（RULE §4）と、視覚の動きが一致する |
| Impact on Figma | Bの場合、`EventRail` の子から `EventDeck` と `EventDiscard` を外す |

## D-06：イベントカードの寸法

| 項目 | 内容 |
|---|---|
| Decision | イベントカードの大きさ |
| Option A | ミニサイズ 45×63 mm |
| Option B | 作物カードと同じ 63×88 mm（ALT-12） |
| Trade-offs | A：山札の取り違えを防ぎ、レールが短くなる。製造が別になる。B：製造が単純だが、取り違えのリスクが上がる |
| Your recommendation | **A** |
| Why | RULE §8：作物とイベントの山札・捨て札は、混ぜない |
| Impact on Figma | `EventCard` の寸法（180×252 px → 252×352 px）と、`EventRail` の幅が変わる |

## D-07：向かい側・横側の数値の読み取り

| 項目 | 内容 |
|---|---|
| Decision | 共通エリアの数値を、全員が読める工夫 |
| Option A | 上下の両向き印刷（REC-20） |
| Option B | 座席別のリファレンス：各プレイヤーに、今シーズンの4作物の参照タイルを置く（ALT-10） |
| Option C | A＋B |
| Trade-offs | A：部品が増えないが、東西の席（90°回転）には効かない（OQ-15）。B：全員が自分向きで読めるが、部品と準備が増える（4枚×人数）。C：最も読みやすいが、両方の負担 |
| Your recommendation | **A**（3・4人）。**5人の東の席のみ、Bを検討** |
| Why | 4人・5人の東西の席のみが、Aで読めない。全体にBを課すのは過剰 |
| Impact on Figma | Bの場合、`SeasonCropPanel` の `scale=mini` を、`PlayerBoard` の脇に追加 |

## D-08：作物の識別に使う色

| 項目 | 内容 |
|---|---|
| Decision | 作物の識別を、機能色（タイプ帯）にするか、作物固有色にするか |
| Option A | グリフ＋頭字＋**タイプ帯（機能色）** |
| Option B | グリフ＋頭字＋**作物固有の色**（ALT-07） |
| Trade-offs | A：状態色と衝突せず、タイプ（戦略的な性質）が伝わる。作物と色の直感的な結びつきはない。B：直感的だが、状態色（緑・赤）と衝突し、10色の識別は困難 |
| Your recommendation | **A** |
| Why | G7（色だけに依存しない）と、状態色の専用化 |
| Impact on Figma | Bの場合、`TypeBand` の代わりに、作物ごとの色スタイルを10個追加 |

## D-09：デジタル版の補助の既定レベル

| 項目 | 内容 |
|---|---|
| Decision | 自動計算・合法手表示などの補助を、既定でどこまで出すか |
| Option A | **A0**：物理相当（補助なし） |
| Option B | **A1**：表示補助（公開情報の再表示。合法手のハイライトを含む） |
| Option C | **A2**：導出情報（収穫予定・初収入ラウンド・収入予測を含む） |
| Trade-offs | A0：物理版と同じ条件。計算の負荷が高い。A1：負荷を下げるが、計算力の差を縮める。A2：ケースCの負荷を下げるが、計算力の差（RULE §14.7で問題にした優位）に大きく影響する |
| Your recommendation | **A1を既定**。A2は「学習モード」として、任意 |
| Why | 物理版でも、印刷物（IncomeLadderなど）で、A1相当の補助を得ている。A2は、判断の一部を代行する |
| Impact on Figma | Variablesの `assistLevel` で、表示レイヤーを切り替える（§21） |

## D-10：進行係の担当

| 項目 | 内容 |
|---|---|
| Decision | 進行係（イベント更新、収穫の集計、台帳の更新）を、誰が担当するか |
| Option A | そのシーズンのスタートプレイヤーが兼任 |
| Option B | 固定の1人（またはCPU席の代行）が担当 |
| Option C | 全員で分担（作業ごとに担当を決める） |
| Trade-offs | A：シーズンごとに交代し、マッチ全体で担当が均等になる。B：慣れるが、負担が偏る。C：1人の負担は減るが、更新の抜けが増える |
| Your recommendation | **A** |
| Why | マッチの構造（スタートプレイヤーが、シーズンごとに1人ずつ移る。RULE §4）と一致する |
| Impact on Figma | 注釈のみ（`PlayerIdentity` の `isStart` に、「進行係」のラベル） |

---

# 25. GAME RULE CHANGE CANDIDATES（今回は実施しない）

**この章の項目は、ルール変更（またはルール本文の変更）を伴う。今回は実施していない。§24の判断事項とは別枠である。**

| ID | 候補 | 動機（UI上の課題） | 影響の見込み | 必要な作業 |
|---|---|---|---|---|
| GRC-01 | 手番順の簡略化（例：座席順の固定、または前シーズンの成績順） | 毎ラウンドの並べ替えと、前ラウンド順の記憶（OQ-05）の負担をなくす | 手番順は、バランスに影響する（RULE §14）。**再シミュレーションが必要** | シミュレーション、ルールの改訂 |
| GRC-02 | RULE §7に、収入フェーズの処理順（計数→受取→更新）を明記する | 更新を先に行うと、緑を置いたばかりの農地を数えてしまう（OQ-04） | **ゲームの結果は変わらない**（現在の記述と同じ意味の明確化） | ルール本文の追記のみ |
| GRC-03 | コインを、実体のある額面（1／5／10／50）から、抽象的なカウンタに変更する | 公開情報の読み取りと、受取・支払いの操作量（OQ-02） | 結果は同じ見込み（値は変わらない）。部品の変更 | ルール本文（§2、§10）の改訂 |
| GRC-04 | 白・緑・赤のトークンを、時間軸上の進行マーカーに置き換える（ALT-01） | トークン操作を約4割減らす（§8.2） | **状態機械（§6.2、§6.3）が同値**であることの証明が必要。ゲームの結果は変わらない見込み | ルール本文の書き換え、同値性の検証（シミュレーション上のテスト） |
| GRC-05 | 同種の収穫中の農地数を、共通ボードの集計を**公式の判定**とする | 全農地の走査の負担（ケースC・D）。現在は、各農地のトークンが権威 | 集計の誤りが、そのまま結果になる。物理では、進行係の責任が重くなる | ルール本文（§7）の改訂 |

---

# 26. Self-Review（セルフレビューの結果）

指示書の11項目について、本文書を点検した。**判定**は、合格／条件付き合格（残る課題あり）で示す。数値の検算（トークン移動数、IncomeLadder、F1・F3のフィクスチャ、寸法の整合）も実施した。

## 26.1 点検結果

| 項目 | 判定 | 根拠 | 残る課題 |
|---|---|---|---|
| **Rule integrity** | 合格 | 作物・コスト・イベント・手番順・公開性などの値と規定は、RULE v0.6のまま。UI提案のうち、ルールに近いもの（SlotCoverの取得順、CoinLedger、TurnOrderTrack、HarvestTally、IncomeLadder、EventRailの捨て札位置、再シャッフルのリマインダー）は、いずれもRULEの規定の**再表示**であり、権威（トークン、RULE §4の手順）を変えない。ルール本文の変更を伴うものは、§25に分離した | 進行係（OQ-03）は、ルールにない役割の導入になりうる。進行マーカー方式（ALT-01）は、§25のGRC-04として別枠 |
| **Scanability** | 条件付き合格 | 収穫レーンを中央に固定し（180°回転しても不変）、形・アイコン・穴の形状で識別。作物はグリフの輪郭で識別。集計は作物パネルと、デジタルの収穫マトリクスで支援 | 東西の席（90°回転）から見る北・南の板は、レーンの左右が90°回転して見える（形で判別）。**ケースC（将来の競合）は、物理では高負荷のまま**（OQ-17） |
| **State clarity** | 合格 | 5状態（未取得／待機中／収穫中／空き・休耕中／空き・自由）を、レーンの占有で一意に決めた。未取得は、SlotCoverの有無で、空きと区別できる。占有と連作制限を別軸で説明（§6.2） | 空きは、トークンの**不在**で表すため、遠距離では「積極的な合図」がない（追加のフラグは、二重の真実を避けるため設けない。OQ-01） |
| **Event clarity** | 合格 | EventRailで、有効（継続中＋今）と予告（次・次の次）を、実線／破線のブラケットと見出しで分離。F2で、終盤の短縮も表現 | 東西の席から、EventCardの数値は読みにくい（OQ-15）。グリフと▲▼は読める |
| **Economy clarity** | 条件付き合格 | 取得コストを各スロットの下に常時印刷し、最左のSlotCoverで「次」を明示。コインは手前の帯（EconomyStrip）。ケースEを、視線移動4〜5回に削減 | 栽培コストは中央の場札にあるため、場札と自分のボードの間を、視線が往復する。**この往復は、配置では解消できない** |
| **Temporal clarity** | 合格 | 時間の共通文法（1セル＝1ラウンド、中抜き／塗り／斜線）を、作物カード・レーン・RoundIndicator・EventRailで共有。読取規則（§6.3）を、フィクスチャF3で検算 | ケースCの将来の収穫窓の暗算は、UIでは消せない（OQ-17） |
| **Physical usability** | 合格 | トークンは、カードの外（レーン）に置くため、最大4個でも、カードの数値を隠さない（テストT5）。穴の形状ロックで、レーン違いと動きを防止 | 遠側のレーンへの腕の伸び（約90 mm）は、試作で確認（OQ-18） |
| **5-player usability** | 条件付き合格 | 標準卓 1200×950で、3・4・5人とも収まる。5人は、備品を西端に移す（§22.3に座標）。板の角の間隔は約60 mm | 5人の他者農地は20スロット（走査量が最大）。山札への到達距離（東の席から約1.0 m。OQ-19）。トークンの数（OQ-08） |
| **Accessibility** | 合格 | 状態は、位置・形・アイコン・穴の形で冗長化。イベントの極性は、▲塗り／▼輪郭＋符号。タイプ帯は、模様で区別。プレイヤー色は、限定した範囲にのみ使用し、形＋番号を併用 | 色の実際の見え方は、Figma上のシミュレーション（テストT3・T4）で、実施時に確認 |
| **Digital portability** | 合格 | §3.2の状態モデルだけから、UIを描画できる。補助を、A0〜A3に分けた（表示補助か、新情報かを区別） | 補助レベルの既定は、人間の判断（D-09、OQ-11） |
| **Figma readiness** | 条件付き合格 | 全コンポーネントに、12項目の仕様を記述。寸法（mm・px）、座標（§22.3）、フィクスチャ（§22.4）、受け入れテスト（§22.5）、未決事項の既定値（§22.7）を用意した | 作物グリフ10種の絵柄と、時間軸のセルの細部は、仮のシルエットで足りる範囲で、Cursorの補完に任せる（逸脱は報告：§22.6） |

## 26.2 点検で見つけて修正した問題

| # | 問題 | 修正 |
|---|---|---|
| 1 | 初稿で、収穫を終えた前作カードを90°横向きにする案があった。横向きのカード（88 mm）が、スロット幅（66 mm）を超えて、隣に重なる。180°回転は、向かい側から見ると正立に見えて誤読を生む | 却下。追加のフラグは設けない（REC-03）。カードをずらす案は、ALT-03（任意）に（§6.4） |
| 2 | レーンブロックの高さが、ヘッダー込みか否かで、§8・§10・§19と§20で食い違っていた | 60×72 mm（ヘッダー8＋穴64）に統一。FarmSlotは66×180、PlayerBoardは380×225。卓の余裕（幅30、奥行き60）と、板の角の間隔（約60 mm）を、再計算 |
| 3 | §19に、番号付けされていない「REQ-xx」への参照があった | 参照の文言を、内容そのものに置き換えた |
| 4 | §11.7の配置例と、RoundIndicator・PhaseRail・SeasonIndicatorの寸法が矛盾していた（縦方向に入りきらない） | SeasonIndicatorを、TurnOrderTrackの下に移し（200×40 mm）、共通エリア内の座標表を追加 |
| 5 | §10に、同じ番号の節が2つ。§7.7の頭字の説明が冗長 | 番号と文言を修正 |
| 6 | ケースFに、UIが「間に合う」という結論を印刷するように読める記述があった | 「結論を印刷しない」という趣旨に修正（判断はプレイヤーが行う） |

## 26.3 この文書が検証していないこと

- **実物の試作による確認はしていない。** 視線移動の回数、到達距離（OQ-18、OQ-19）、卓のサイズ（ASM-05）は、いずれも設計上の見積もりであり、実測ではない。
- 操作量の推定（1プレイヤー・1シーズンのトークン移動数：基本約84回、上級約190回）は、平均値（14.4回）と、RULE §14.1の行動数から算出した概算である。作物の組み合わせで変動する。
- 色覚シミュレーションは、パレットの選定（§19.4）に基づく設計であり、Figma上での実施は、Cursorの作業（テストT3・T4）に含めている。
