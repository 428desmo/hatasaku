# 畑作 Web IA v0.1（スマホ主）

対象ルール：`hatasaku_rules_v0.6.md`（ゲーム挙動の正）  
物理UI仕様（`hatasaku_ui_spec_v0.6.2.md`）は**先送り**。情報階層と状態モデル（§3.2、§17）だけ借りる。  
見た目の美麗さは対象外。配置・露出・タップ階層が対象。

例示データはフィクスチャ **F1**（基本・4人・R5・手番開始。自分＝P3、8G、手番）。

---

## 1. 方針

1. 物理卓を画面にコピーしない。スマホでは「全ボードを同時に見る」はできない。
2. 中心課題は変わらない：**今、作物Xは全体で何枚収穫中か**（RULE §7）。
3. その置き換えは **収穫マトリクス**（行＝4作物、列＝プレイヤー）。公開情報の再配置（A1）。将来の収穫窓は出さない（A2は後）。
4. 補助は **A1を既定**（D-09）。マッチ内は全員同じ。
5. 今の模擬プレイHTML（全員の農地フルスペックを縦に並べる）は、意思決定画面としては捨てる。

---

## 2. 画面の骨格（常時）

```
┌ 固定ヘッダー ─────────────────┐
│ S1/3  R5/10  手番  あなた 8G   │  ← 今どこか・自分の金
├ 本文（1画面スクロール）─────────┤
│ イベント（コンパクト 2〜4チップ）│  ← 有効が主、予告は薄い
│ 収穫マトリクス 4×人数           │  ← 走査の本体
│ 場札 3〜4（コンパクト）         │  ← 買うもの
│ 自分の5スロット（横一列）       │  ← 植える先
├ 固定フッター ─────────────────┤
│ [パス]  （手番中のみ）          │
│ タブ: 手番 | 記録               │
└────────────────────────────────┘
```

タブは **2つだけ**。物理仕様§17.4の「卓／自分／相手／記録」は採用しない。相手の詳細はマトリクスのセルをタップする。記録は得点・ログ。

---

## 3. 露出の三層

| 層 | いつ見える | 中身 |
|---|---|---|
| **L0 常時** | 手番画面を開いた瞬間 | シーズン／ラウンド／フェーズ／手番誰／自分のコイン。有効イベント（作物＋符号）。予告は作物名だけ（数値は任意で薄く）。収穫マトリクス（作物×人、状態略号）。場札の識別＋栽培コスト＋W/H。自分スロットの状態＋作物頭字。パス |
| **L1 タップ** | 1対象を開く。同時に開くのは1つ | 場札のフルスペック（収入・最低保証・C・タイプ）。スロットの前作／合法かどうか。マトリクスセル → その人の5農地。イベント帯 → 6ゾーン（捨て／継続／今／次／次の次／山）。ヘッダーのコイン → 全員の所持（手番順） |
| **L2 埋める** | 「記録」タブ、またはL1の奥 | 捨て札一覧、山札枚数、得点表、ラウンドログ、作物図鑑。山札の中身は出さない（非公開） |

**出さない（この版）**

- 初収入ラウンド・将来の収穫人数（A2）
- 推奨手（A3）
- 除外6枚の中身
- 物理のレーン穴・両向き印刷・卓の実寸

---

## 4. 略号（マトリクスとスロット）

状態は色だけにしない。文字＋形。

| 表示 | 意味 |
|---|---|
| `収3` | 収穫中・緑3（収入に入る） |
| `待1` | 待機・白1 |
| `休4` | 空き・休耕（赤4。別作物は可、同種は不可） |
| `空` | 空き・自由（前作カードあり、C=0または赤0） |
| `—` | 未取得 |
| `.` | その人はその作物を植えていない（取得済みスロットでも別作物） |

1セルに複数農地がありうる（例：P3はトマトが収3と待1）。セル内は縦に積む。収穫中があるセルは枠を太くする。

---

## 5. 場札カード（コンパクト → 展開）

**閉じた場札（L0）**

- 作物名
- 栽培コスト（大きい）
- `待W 収H`（休Cは閉じた状態では出さない。植え替え判断のときだけ要る）
- その作物の「今の1農地収入」（A1。マトリクスと同じ式）

**開いた場札（L1）**

- 閉じた内容に加え、最低保証、C、タイプ、時間軸セル、イベント補正の内訳
- 「新しい農地へ（取得n＋栽培m）」／空きスロットへ、の行き先は **カード側ではなくスロット側** で選ぶ（カードを開いたままスロットをタップ）

理由：場札3枚×スロット最大5の組み合わせを、ボタンの羅列にしない（今のUIの問題）。

---

## 6. 自分のスロット（L0）

横5つの箱。中身は頭字＋略号だけ。未取得は取得コストだけ（次の1枠を「次」と書く）。

タップでL1：前作、合法/非合法の理由（金不足、同種CD）、植える確定。

トークン個数のピップはL1。L0は略号の数字で足りる。

---

## 7. イベント

L0：有効2枚を上段（実線・大きい）、予告2枚を下段（破線）。390幅に4チップ横並びは文字が潰れるので、2×2にする。山札枚数は出さない（L1）。

有効2枚が収入に効くことだけ、常に読めるようにする。6ゾーンのコンベアはL1。

---

## 8. 画面一覧（ワイヤー）

| ID | 画面 | 目的 |
|---|---|---|
| W1 | 手番・既定 | 判断に必要なL0が1画面に載るか |
| W2 | 場札を開く | 詳細を出してもマトリクスが残るか |
| W3 | スロットを選ぶ | 植える先の選択 |
| W4 | マトリクスのセル | 相手の農地を、必要なときだけ |
| W5 | イベント帯を開く | コンベア全体 |
| W6 | 待ち（他人の手番） | 操作を消す |
| W7 | 収入確認 | ホールド（次へ） |
| W8 | 記録 | L2 |

ワイヤー：`wireframes/mobile/index.html`

---

## 9. 今の模擬プレイUIから変える点

| 今 | このIA |
|---|---|
| 全員の農地を全文で縦積み | マトリクスが主、詳細はタップ |
| 場札にスペック全文＋見通し | 閉じると名前・コスト・今の収入 |
| 合法手をボタン列 | 場札タップ → スロットタップ → 確定 |
| イベントは文章1行 | 有効チップ＋薄い予告 |
| タブなし・1文書 | 手番／記録の2タブ |
