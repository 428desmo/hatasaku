# 畑作 ソフトウェア仕様書 v0.14

作成日：2026-09-23  
規範：`hatasaku_rules_v0.14.md`  
対象：ローカルネット上のブラウザ対局と、コンピュータプレイヤーを含む模擬プレイ評価

ルールと食い違う場合は **ルール仕様書 v0.14 を正** とする。

---

## 1. 目的

1. 同じ LAN 上のブラウザで、3〜5人の卓を進行できる。
2. 席の一部または全部を CPU にできる。CPU は通常の席である。
3. 進行は決定的なエンジンに閉じ、人間 UI と CPU は同じ合法手 API を使う。
4. マッチ（複数シーズン）を扱い、得点表と合計で勝敗を決める。

### 非目標（この版）

- インターネット越しのマッチング、アカウント、永続ランキング
- ルール §15 の未採用案
- 2人プレイ
- 高品質な演出（付録Cの季節表示は後でよい）
- 同点タイブレーク（共同勝者のまま）

---

## 2. マッチとシーズン

| 概念 | 実装 |
|---|---|
| シーズン | `GameState`。農地・カード・コインを持つ1ゲーム |
| マッチ | `Table`。シーズンの列、得点表、開始プレイヤーの回転 |
| シーズン数 | 省略時は人数。`--seasons=N` で上書き。1でもよい |
| 開始プレイヤー | マッチ開始時にランダム。以降、座席順で1人ずつ移る |
| 手番順 | シーズン中は `clockwiseOrder(startSeat, n)` のまま。ラウンドでは並べ替えない |
| 初期コイン | シーズン数が人数の倍数なら全員8。それ以外は手番順に 3人 `[8,9,9]`、4人 `[8,9,9,9]`、5人 `[8,8,9,9,9]` |
| シーズン終了 | 最終ラウンドの収入後。所持コインを得点表へ。盤面は次シーズンで初期化 |
| マッチ終了 | 全シーズン終了後。得点表の合計が最大の席が勝者（同点は共同） |

エンジンの `phase === "gameOver"` は **シーズン終了** を表す。マッチ終了は `Table.matchOver`（最終シーズンの `gameOver` かつホールドなし）。

---

## 3. 定数

| 定数 | 値 |
|---|---|
| `Mode` | `"basic"`（基本）／`"advanced"`（上級）。CLI の `tutorial`/`full` は別名 |
| ラウンド数 | 基本 10、上級 18 |
| イベント列の最大 | 3（左端＋先読み2） |
| イベント持続 | 2ラウンド（左端になったラウンドと次の1ラウンド） |
| イベント累計上限 | 基本 10、上級 18 |
| 有効イベント | 左端＋発動中、最大2 |
| イベント再シャッフル | 上級のみ。山札が空かつ累計が上限未満のとき、捨て札（効果切れ）を山札に戻す |
| 農地スロット | 5 |
| 場札 | 3〜4人は3、5人は4 |
| 農地取得コスト | `[0,1,2,3,4]` |
| 作物の束 | 14（共通6＋基本専用4＋上級専用4）。シーズンはモードの10種から4束 |
| 呪い | `CURSE_DELTA = -6`、`CURSE_DURATION = 4`（使うラウンドの次から。シーズン終了で打ち切り） |
| specVersion | `"0.14"` |

上級は16枚のイベントをすべて山札にし、R14で累計16、R15の更新で再シャッフル、R15とR16の補充で累計18に達する。R17〜18は列が 2→1 枚。

作物の `pool`：`common` は両モード、`basic` は基本専用（ネギ・カボチャ・トマト・スイカ）、`advanced` は上級専用（アスパラガス・ゴボウ・ピーマン・メロン。対応する基本専用と同じ待／収／休／コストで、基本収入だけ低い）。`pickCrops` は `cropsForMode(mode)` から4種。テストの `forcedCrops` はモード検査をしない。

---

## 4. ドメイン（ホストのみ）

`GameState` の要点：

```
specVersion: "0.14"
mode, seed
season, seasonCount, startSeat
round, lastRound, phase, actingSeat, turnIndex, turnOrder
players: [{ seat, kind, name, coins, plots[5], cpuStrategyId? }]
crop: { deck, discard, market, nextId }
event: { deck, discard, row, active, excluded, drawnCount, nextId }
activatedRound: { [instanceId]: round }
winnerSeats                 // そのシーズンのコイン最大
lastPayouts, roundActions, seasonActions, lastHarvest
startCoins, roundLog          // 終了サマリー用。seasonActions はシーズン中の全行動（終了表用）
curseReadySeats               // そのシーズンの呪い権利（未使用）
curses: [{ cropId, from, to, bySeat, revealed }]
```

`Action` は `pass` / `plant` / `curse`。`curse` は手番を消費しない（`roundActions` に入れない、ポインタを進めない）。収入式の `eventSum` は通常イベントに呪いを合算する。最低保証は保つ。**各作物はシーズン中に1回しか呪いの対象になれない**（最大4回。同じ作物への重ねは不可）。対象は使ったラウンドの収穫フェーズ終了まで `revealed: false`。公開ビューは、すでに `revealed` のもの、自分の未公開、自分が今シーズンに呪いを使ったならそれ以前の未公開、を出し、他は `hiddenCurseCount` にする。

`createGame(config, rng)` がシーズン準備（§3.2）を行う。`startSeat` と `evenStartCoins` はマッチ側が渡す。2シーズン目以降、`Table` が `curseSeats` に、前シーズンまでの合計で**最も多い席を除いた中の最下位全員**（同点含む。最上位の同点は全員除く）を渡す。残る席がなければ（全員同点）誰にも渡さない。n人なら権利は最大 n−1。1シーズン目は空。

公開ビューは山札の中身と `excluded` を出さない。ルール §10 どおり。

---

## 5. エンジン API

```
createGame(config, rng) -> GameState
listLegalActions(state) -> Action[]
applyPlayerAction(state, action, rng) -> GameState   // 手番のみ。収入はホストが適用
applyAction(state, action, rng) -> GameState         // テスト用。収入とイベント更新まで進める
applyHarvest / concludeRound / applyEventUpdate
getPublicView(state, viewerSeat | "spectator")
```

合法手・植えの処理順・トークン状態機械・収入式はルール付録Aおよび §5〜§7。呪い（§9.4）は付録Aに無いが、手番の任意・非消費として §5 が正。同じ作物への重ねはシーズン中できない。

`applyPlayerAction` のあと、フェーズが `income` でそのラウンドの行動がすべてパスなら `sweepMarketIfAllPassed`（場札を捨て札へ、`refillMarket`）。ホスト（`Table`）が CPU を進め、全員終了で `applyHarvest` →（人間がいれば結果画面）→ `concludeRound` → 次ラウンドなら `applyEventUpdate`。シーズン開始時（人間がいれば作物確認）。CPU `irr` は、他人の収穫ヒット − 自分×2 が正なら呪いを先に使う。

---

## 6. ホスト進行

`Table` がロビー、人間席の割り当て、CPU、ホールド、マッチを持つ。

ホールド：

| hold | タイミング | Next の効果 |
|---|---|---|
| `intro` | シーズン開始時（人間がいるとき） | 1ラウンド目の手番開始。CPU を進める |
| `result` | 各ラウンドの収入直後 | `concludeRound`。最終ラウンドならシーズン記録。次ラウンドならイベント更新のあと手番 |
| `season` | シーズン終了（最終シーズンも含む） | 行動リストのあと `mix` |
| `mix` | シーズンの手の内訳（帯グラフ） | 残りシーズンがあれば次の `intro`。最終なら `honor` |
| `honor` | マッチ終了の総合優勝 | 接続中の人間全員が［もう一度］で、同じ設定（モード・人数・CPU・シーズン数）の新しいマッチ。シードは別 |
| `null` | 手番中、または人間0人のマッチ終了 | — |

人間0人の評価卓はホールドを飛ばし、全シーズンを通す。

乱数はマッチ開始時のシードから連続して使う（シーズンをまたいでも同じ `rng`）。CPU 用は `${seed}-cpu`。［もう一度］の新マッチは `${元シード}-r{n}`。

---

## 7. UI

主UIはグラフィカル（`/`）。テキストUIは `/text`。学習モードは設定ONのときだけ。推奨手は出さない。今収入（N込み）は場札に出さない。

ヘッダー：`S{s}/{S}  R{r}/{R}`、自分の席、手番行、学習ON/OFF、手番／記録タブ。プレイヤーの並びは手番中 `turnOrder`（シーズンのスタートから時計回り。ラウンドでは動かない）。シーズン／マッチの表は最終コイン降順。

### 7.1 シーズン開始（`hold === "intro"`）

人間がいる卓は、**シーズン開始時だけ**作物確認を出す（各ラウンドの開始では出さない）。農場・場札・イベント・凡例は出さない。

- 左：手番順の席（名・所持コイン。手番の席に 🖐️。2シーズン目以降は、前シーズンまでの合計が首位の席に 👑。同点なら全員）
- 中央：見出し「今シーズンの作物」。4作物をフルサイズ（名・栽培コスト、待○／収★／休△、基本／最低、タイプ、一言）
- 権利があるとき：「呪いの権利　名前」。席カードに 🤡（未使用のときだけ。使うと消える）
- 下：［開始］（濃い黒）。押すまで手番は進まない

### 7.2 手番

左＝全員の農場ミニ（5マス）、右＝イベント列＋今シーズンの作物ミニ4枚＋場札フル（2×2）＋パス。農場ミニと作物ミニの枠の高さは、今の状態の行だけに合わせる（余白の大きな固定高さは使わない）。作物ミニは略称（最大3文字）・栽培コスト、待○（個数の数字なし）、収★＋基本/最低（例：サツマ `待 ○○○○`／`収 ★ 19/8`）。タップするとフルサイズ（名・栽培コスト、待○／収★／休△、基本／最低、タイプ、一言）をロールオーバー。同時に1枚。もう一度タップで閉じる。2シーズン目以降、前シーズンまでの獲得コイン合計が首位のプレイヤー（同点なら全員）の名の横に 👑。今シーズンの所持は含めない。呪いの権利が残っている席は名の横に 🤡。手番の席は名の横に 🖐️。

場札：作物名・栽培コスト、待○／収★／休△、基本／最低。タイプ（一括型など）は出さない。3〜4人は3枚、5人は4枚。所持不足または合法手がない札はグレー（タップ不可）。場札を選ぶとパス枠がキャンセルになる。学習ONのときだけ、選んだ場札の上に見通しのロールオーバー。自分の手番で呪いの権利が残っていて、今シーズンでまだ対象にできる作物があるときは［呪い］。押すと作物ミニから1種を宣言する（手番は進まない）。今シーズン既に対象になった作物は選べない（グレー、「今シーズン対象済」）。対象は使ったラウンドの収穫まで伏せ札。パス枠はキャンセルになる。

植え：場札を選ぶ → 自分の合法マスだけが反応する（空いている取得済み、または次の未取得）。**合法以外の枠は、他プレイヤーの全農地も含めてグレーアウト**。合法マスをタップで植える（確定ボタンなし）。

ミニの表示：

| 状態 | 表示 |
|---|---|
| 未取得 | 次の枠だけ `次 nG`。それ以外は `—`。縁は破線 |
| 空き | 作物名、基本/最低、`空`。縁は破線 |
| 待機 | 作物名、基本/最低、待○（残。個数の数字は出さない） |
| 収穫 | 作物名、基本/最低、収★（残。個数の数字は出さない） |
| クールダウン | 作物名と `休▼…` だけ（個数の数字は出さない）。収穫期の長さなどは出さない。縁は破線 |

ミニをタップすると、そのマスの上に詳細を重ねる（同時に1枚）。場札選択中は非合法マスの詳細は出さない。

イベント：継続／今は実線、次／次々は破線。3行（ゾーンと適用R、作物名、±）。例：`今　R1-2` / `スイカ` / `+4`。適用Rはシーズン内だけ（上級の最終なら `R18-19` ではなく `R18`）。山札枚数は右端。有効または予告中の呪いは「呪い」ゾーン（−6）。複数なら並べる。

### 7.3 収穫（`hold === "result"`）

1. 全面に大きく「収穫タイム」（約1秒）。イベント列より手前の最前面
2. 収穫がある農地を手番順に約0.2秒間隔で巡回し、そのマスに収穫図を重ね、コインを足す
3. 巡回後、最前面に「収穫タイム」と、その少し下の［次へ］
4. 誰も収穫しなければ「収穫なし」ダイアログ

収穫図は緑。`formatHarvestFigure`：最低保証なら `5😞`、基本より増なら `9 △`（数字と△のあいだに空白、△は差の個数）、減なら `6▼▼▼▼▼`（空白なし、▼は差の個数）、同じなら数字のみ。

［次へ］と同じ進み方：収穫タイム、収穫なしダイアログ、収穫図の載ったマスをタップしても `next`。

### 7.4 記録・終了

- 記録タブ：得点表（S1..Sn と合計）、作物山／捨て、イベント山、今ゲームの作物。首位は 👑
- ラウンド結果（テキストUI）：手番（支出は負）・収穫（収入は正）・所持コイン
- シーズン終了：農場・場札・イベントは出さない。左に各プレイヤーの席カード（名・今シーズンの獲得コイン → マッチ合計。例：`75G → 156G`。今シーズン最多は太字。トップは薄いハイライト）と「今シーズン 1位」。右に3ラウンドずつの行動リスト（R1-3、R4-6…。基本の余りは `R10`）。各束は縦3行で、行頭にラウンド番号（`1 トマト` / `2 ゴボウ` / `3 パス`）。セルは作物略称（最大3文字。コマツナ→コマツ）または「パス」。植えた場所は出さない。束は左から約0.1秒ずつ表示。［次へ］で手の内訳（帯グラフ）。テキストUIはコイン推移、収穫中農地、今シーズンの合計、取得農地、最終盤面
- 手の内訳：プレイヤーごとに、今シーズンの作物とパスの比率を横帯で出す。色は今シーズンの4作物＋パス。凡例つき。［次へ］で次の `intro`、または最終シーズンなら総合優勝
- マッチ終了：総合優勝（得点表の合計最大。同点は共同）を席カードの薄いハイライトと「総合優勝」で表彰。今シーズン → マッチ合計を出す。［もう一度］。人間が複数なら接続中の全員が押すまで待つ（押した人は確認済み）。始まると同じモード・人数・CPU・シーズン数で作物確認へ。席はそのまま。シードは別

---

## 8. CLI

```
npm test
npm run eval -- --seed=... [--advanced] [--seasons=N]
npm run serve -- --humans=1 --cpus=2 --mode=basic [--seasons=N]
```

`eval` は人間0・CPU3。省略時のシーズン数は人数。

---

## 9. 受け入れテスト

ルール付録Bに加え、既存のライフサイクル・収入・合法手テストを維持する。v0.14 で追加・更新するもの：

1. 基本は R9〜10 で列 2→1。上級は R16 で3枚・累計18、R17〜18 で 2→1
2. 上級は R14 で累計16、R15 更新で再シャッフルして累計17、R16 で18。以降増やさない
3. 1シーズンのみの初期コインは手番順の補正表。倍数シーズンは全員8
4. 4人・開始席1の手番順は、そのシーズンの全ラウンドで `1,2,3,0`
5. 3人3シーズンのマッチは得点表が3行、合計でマッチ勝者を出す
6. シーズン2の開始時にコイン・農地・山札が初期化され、得点表のシーズン1だけが残る。開始席は1つ進み、手番順はそこから時計回り
7. 所持コインが変わっても、同じシーズンの R2 以降で `turnOrder` は変わらない
8. あるラウンドで全員がパスしたら、収入前に場札が捨て札へ移り、新しい場札が並ぶ
9. 人間がいる卓は、シーズン開始時に `intro` ホールドがある
10. 基本のシーズン4作物は共通6＋基本専用4からのみ。上級は共通6＋上級専用4からのみ。上級専用4種の基本収入は 5／9／6／12
11. 1シーズン終了時、`seasonActions` は人数×ラウンド数。`seasonActionRows` は手番順の行、セルは作物略称または「パス」（plotIndex なし）
12. 1シーズン目は呪いの権利なし。2シーズン目以降、前シーズンまでの合計で最上位を除いた最下位全員（同点含む）に1回分。全員同点なら誰にも配らない（最大 n−1人）。使うと手番は進まず、次ラウンドから4ラウンドその作物に −6（通常イベントと合算、最低保証維持）。各作物はシーズン中1回まで。対象は使ったラウンドの収穫終了まで伏せ、収穫後に公開。全員パス判定には含めない
13. マッチ終了の［もう一度］は、接続中の人間全員が押すと、同じ設定でシーズン1の `intro` に戻る。1人だけ押しても始まらない

---

## 10. 変更履歴

- 2026-09-23：収穫タイムを最前面に。巡回後の［次へ］はその少し下。席の手番は 🖐️、呪い権利は 🤡（未使用のみ）。
- 2026-09-23：農場ミニと作物ミニの枠を、今の行数に合わせて低くし、手番画面の縦スクロールを減らす。
- 2026-09-23：マッチ終了に［もう一度］。人間全員が押すと、同じ設定の新しいマッチ。
- 2026-09-23：シーズン終了の席カードは今シーズン得点 → マッチ合計。最多の今シーズン得点は太字。
- 2026-09-23：ルール v0.14。呪いはシーズン中作物1種1回。権利は最上位を除いた最下位（挙動は v0.12 の全員同点除外と同じ）。
- 2026-09-22：農場ミニと季節作物ミニから冗長な個数を外す。作物ミニは収★に基本/最低。シーズン終了に行動表（略称、列を0.1秒ずつ）。
- 2026-09-22：ルール v0.9。上級専用4種（アスパラガス・ゴボウ・ピーマン・メロン）。シーズンの4束はモードごとの10種から選ぶ。
- 2026-09-22：手番に今シーズンの作物ミニ4枚。タップでフルサイズをロールオーバー。
- 2026-09-22：空き農地も破線。2シーズン目以降は前シーズンまでの合計首位に 👑。シーズン終了・マッチ終了はトップを薄いハイライトで表彰。
- 2026-09-22：グラフィカルUI。シーズン開始は作物確認のみ（農場・場札・イベントを出さない）。収穫は自動巡回、ゼロならダイアログ、図は簡略記号。植え先以外はグレー。クールダウンミニは休▼と破線。［次へ］は濃い黒で、収穫オーバーレイのタップでも進む。
- 2026-09-22：ルール v0.8。シーズン開始の作物確認、全員パスの場札一掃、イベントの適用ラウンド表記。
- 2026-09-22：ルール v0.7。手番順はシーズン中固定。ラウンドごとのコイン昇順並べ替えを廃止。
- 2026-09-21：ルール v0.6。モード名を基本／上級、上級18ラウンド、マッチ／シーズン、開始プレイヤー回転、初期コインの分岐。
- 2026-09-20：ルール v0.3.1 に対応する初版（`hatasaku_software_spec_v0.3.1.md`）。
